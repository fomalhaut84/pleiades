# 인계 노트 — 2026-09-11, 1a-2 완료 · #51 완료(미러 포함) · E10 · 후속 #61·#62

직전 노트: `2026-09-10-1a-1-done.md`(보완 PR #57 머지 `b5f0b86`). **이 노트가 최신이다.**

## 이 세션에서 한 일

**대상 저장소 쓰기 3건 + 원본 하네스 동기화 1건 + 원본 미러 PR 1건.** 전부 사용자 승인(게이트 5항목 + G-1 + G-2 · 2026-09-11) 안에서:
- **myFitness** worktree `integration/feature-pleiades-1a-2` → **#374 머지 `3818208`**(8파일) · 머지 후 **원본 `~/workspace/myFitness/.claude/` 2파일 `git archive | tar -x` 동기화**(G-2 · 사전 사본 스크래치패드 · index 무변경)
- **myFinance** worktree `integration/chore-pleiades-51` → **#493 머지 `ef43c03`**(1파일) · **원본 `dev` 모드 S 미러** 이슈 myFinance#494 → **#495 머지 `8ed402c`**(봇 미실행 — 컷오프 전 사용자 머지)
- 서버·DB·텔레그램 무접촉. 빌드·`pm2 restart` 불필요(`integration/pleiades` 는 배포·CI 트리거 밖 · 실측). 세션 종료 시 네 체크아웃 전부 clean(worktree fin `ef43c03` · fit `3818208` · 원본 fin `dev 8ed402c` · fit `main 5809c48`).

순서:
1. PR #57 머지 정리(orphan 0 · #56 자동 종료).
2. **1a-2 착수** — `pleiades-orchestrator`: 실측(직접 · 스크래치 lock 재현 716→785 · audit 4=4) → 계획 `_workspace/1a-2/01_plan_1a2.md` → **착수 직전 재감사**(`03_auditor_1a2plan.md` · **정정 10 · 블로커 3**) → 2회차 계획 → 사용자 승인(Q16·Q17·Q18 권고안 + G-1·G-2) → `dual-repo-operator` 집행(fit E2~E6 · #51 fin E9 · 미러 E9′) → 9-1 사전 리뷰(fit 0/0/info 5 → 3 반영 · fin 0/0/info 2 → 1 반영) → PR → 봇 루프 → 머지 → 10절.
3. **E10 pleiades#60**(#59 · 머지 `b4255de`): measured-facts "1a-2 집행 실측" §1~§9 · 003 §9 U3 이행 + §5-2 집행 노트 · pleiades 하네스 stale 6곳(재감사 정정 6 + `dual-repo-change:86-89`·`dual-repo-operator` 의 *"fit worktree 에 `.claude/` 없다"*) · 계획·감사·롤백 문서. **Codex 4라운드**(P1 1+3+1 · P2 1+1 전부 반영).
4. 후속 이슈 **#61**(롤백 절차 작성 규칙 → `dual-repo-change` 스킬) · **#62**(fin·fit 8-4 `model` 예시 `gpt-4o` 미지원 · 대칭 변경) · 종료 #58 · #51 · #59 · #56 · myFinance#494.

## PR 현황 (전부 머지)

| PR | 브랜치 | 내용 | 이슈 | 리뷰 | 되돌리기 |
|---|---|---|---|---|---|
| ~~#57~~ `b5f0b86` | `chore/56-1` | 인계 보완 | #56 | 봇 미게시 | 즉시 |
| ~~myFitness#374~~ `3818208` | `integration/feature-pleiades-1a-2` | vitest 도입 · 테스트 45 · postcss 리터럴화 · 하네스 3곳 | #58 | 사전 0/0 · 봇 0/0 | **즉시**(revert 브랜치→PR + `npm ci`) · 원본 동기화분 중간 |
| ~~myFinance#493~~ `ef43c03` | `integration/chore-pleiades-51` | 8-4 문장 1 + 정정 블록 | #51 | 사전 0/0 · 봇 P2 1 → #62 | 즉시 |
| ~~myFinance#495~~ `8ed402c` | `chore/494-1`(원본 `dev` · 모드 S) | #493 미러 | myFinance#494 | self-review · 봇 미실행 | 즉시 |
| ~~#60~~ `b4255de` | `chore/59-1` | E10 | #59 | self-review · 봇 4회 | 즉시 |
| (이 노트) | `chore/63-1` | 인계 · CLAUDE.md · 003 완료 표기 | #63 | self-review | 즉시 |

## 결정된 것 (사용자 2026-09-11)

| | 결정 | 귀결 |
|---|---|---|
| **Q16** | fit `overrides.postcss` `"$postcss"` → `"^8.5.10"` 승인 | 해석값 8.5.25 동일 · 서비스 영향 0. **앞으로 postcss 범위를 올릴 때 devDeps 와 두 줄 동시 수정**(사전 리뷰 info) |
| **Q17** | `src/**/__tests__/` | fin 대칭 · fit `eslint src/ --max-warnings 0` 통과(U2 해소 · warning 0) |
| **Q18** | devDeps · **범위는 직접 편집 `^4.1.8`**(npm 이 적으면 `^4.1.11` 드리프트) | `package.json` md5 전후 동일 실측 |
| **G-1** | 1a-3 교체 대상 `bot/notifications/send.ts` 직접 baseline 포함 | 재감사 블로커 3 — 1회차 계획의 *"1a 추출 대상 = bot/utils"* 는 틀렸다. `send.test.ts` 11건(절단 4093+`...` · 재시도 `[2000,8000,30000]` 4회 · HTML→plain 무백오프 · `SendResult` · 로그 마스킹) |
| **G-2** | 머지 후 원본 하네스 동기화 둘 다 — fit `git archive` · fin 모드 S 미러 PR | 재감사 블로커 5 — 세션이 읽는 하네스는 원본이라 worktree 만 고치면 거짓이 남는다. **완료** |
| 구현 결정(승인 범위 안) | fit `test` = `vitest run && verify 2종`(이름 유지 · CI·하네스·스펙 9행 무변경 · 1회 실행형) · `test:run`·`test:coverage` 추가 · coverage include `src/lib`·`bot/notifications`·`bot/utils` · fit 하네스 정정 3곳(`workflow.md:165,275` · `branch-workflow:83` — 275 는 *"프레임워크 부재 시 스크립트로 대체"* 살아 있는 지시 교체) · `ci.yml` 주석·스텝 이름 제외(후속) | — |

## 결정되지 않은 것

| 이슈/질문 | 내용 | 언제 |
|---|---|---|
| **#62** | fin·fit 8-4 `model` 예시 `gpt-4o`(ChatGPT 인증 Codex 미지원) — 대칭 변경 · 지원 모델 실측 필요 | 별건 |
| **#61** | 롤백 문서 필수 항목(PR 경유 revert · 열린 PR close · 미러 revert)을 `dual-repo-change`·`dual-repo-operator` 에 | 다음 대상 저장소 집행 전 |
| **#48** | 1a-1 info 3 | 1a-3 인계 |
| **Q45** | 서버 https·node·git | 1a-3 착수 전(서버 접속 승인) |
| **첫 태그·릴리즈** | `v0.1.0` 릴리즈 PR — 봇 게이트 대체 없음 | 1a-3 전 |
| Q27 · `error.ts` shim · `sensitiveLogs` B′ · Q36 · Q38 · #17 · #11 · Q23 · Q12~Q14 · Q7·Q2·Q3 | 이전 노트 그대로 | 변동 없음 |

**단계 0 은 여전히 미착수, 단계 1 을 먼저 하기로 결정된 적도 없다.**

## 다음 세션의 첫 액션 후보

1. **1a-3 준비** — 첫 태그(릴리즈 PR `dev`→`main` · 봇 필수 · 대체 없음) + Q45 서버 측정(접속 승인) + #48 I1 `VERSION`. 003 §5-2 1a-3 행: fit `send.ts` 124줄 → 패키지 교체(호출 6건) · fin 흡수 · Q10-L 분할 통일 · `error.ts` shim 결정. 되돌리기: 태그 삭제 즉시 · 1a-3 코드는 중간(003 §8-1).
2. **#61** — 하네스 문서 · 즉시. 1a-3 집행 전에 넣으면 그 롤백 문서가 첫 적용 대상.
3. **#62** — 대칭 변경(이슈 1 · PR 2 + 미러 2) · 즉시. 지원 모델 실측이 먼저.

## 주의사항

- **두 저장소는 실서비스 중.** 쓰기 전 사용자 확인. 이번 세션 쓰기: fit 1(worktree) + 원본 하네스 동기화 1 · fin 1(worktree) + 원본 미러 1.
- **fit `npm run test` 는 이제 vitest 45건 + verify 2종**(`integration/pleiades` 기준). fit 원본 `main` 은 여전히 verify 뿐 — `integration/pleiades` 는 `dev` 로 가지 않는다. **원본 fit 하네스는 vitest 를 전제로 서술**(동기화됨)하지만 원본 `main` 코드에는 vitest 가 없다 — 원본에서 단독 작업 시 `npm run test` 는 verify 뿐이다.
- **fit 하네스 `workflow.md` 8-5 작성 원칙이 바뀌었다** — 회귀 테스트는 vitest 로. `scripts/*.ts` 대체는 예외.
- **`next build` 는 로컬 DB 를 요구하지 않았다**(로그 기준) — 단 로컬 postgres 가 LISTEN 중이었다는 한정. 확정하려면 postgres 를 내리고 재빌드(미실행).
- **fit `deploy/deploy.sh:49` `npm ci` 에 `--omit=dev` 없음** — 서버가 배포마다 dev 69개를 더 받는다(fin 동일 · 기능 위험 없음 · 별건 미등록).
- **Codex 봇 관측:** pleiades#60 은 오픈 직후 자동 리뷰 + `@codex review` 마다 재리뷰(4회). 대상 저장소 PR #374·#493 은 **30분 컷오프까지 미게시 → `@codex review` 후 3분 안에 응답**. #495 는 사용자가 컷오프 전 머지. 9-3 컷오프 절차가 실제로 작동한 첫 사례.
- **macOS bash 3.2 에는 `declare -A` 가 없고, zsh 는 `for p in $var` 를 단어 분리하지 않는다** — Monitor 스크립트는 python 으로.
- `git worktree` 의 `integration/pleiades` 에 upstream 이 없으면 `git pull --ff-only` 가 실패한다(fin 에서 발생 · `--set-upstream-to` 로 해결).
- **`MEMORY.md` 18줄**(한도 200).

## 정정된 기록 — 옛 문서를 그 전제로 읽지 말 것

- 003 §5-2 표 1a-2 행 *"scripts 3줄 · 4파일 revert"* → **`test` 1 수정 + 2 추가 · 8파일**(정정 ③ 아래 집행 노트 · 이 PR 에서 완료 표기)
- 003 §9 U2·U3 → **해소**(U3 는 재감사 스크래치 설치 · U2 는 E5 lint warning 0)
- `dual-repo-change`·`dual-repo-operator` 의 *"fit worktree 에 `.claude/` 없다"* → **#369 이후 거짓**(정정 블록 · PR #60)
- pleiades `workflow.md` 8절 정정 블록 *"1a-2 가 도입할 때까지"* → 소진. **표 fit 행 `npm run test` 는 무변경 유효**
- 1회차 계획 *"fit 의 1a 추출 대상 = `bot/utils`"* → 틀림(`send.ts`). `telegram.ts` 의 `splitMessage`/`replyLong` 은 인바운드
- 롤백 문서의 머지 후 revert 가 로컬 커밋으로 끝나던 형식 → **브랜치 → PR → 사용자 머지**(PR #60 P1 · #61 로 규칙화 예정)

## 재현이 필요한 절차

- **fit 원본 `.claude/` 동기화** — `git -C ~/workspace/myFitness archive integration/pleiades <paths> | tar -x -C ~/workspace/myFitness`(이번 2파일판 실행 검증)
- **1a-2 lock·audit·U3 재현** — measured-facts "1a-2 집행 실측" §1~§3 의 명령(스크래치 사본 · 대상 저장소 무변경)
- **fin 모드 S 미러** — 원본 `dev` · 이슈는 fin 저장소 · 문장 단위 적용(원본 `dev` 8절은 `integration/pleiades` 판과 다르다 · cherry-pick 불가)
