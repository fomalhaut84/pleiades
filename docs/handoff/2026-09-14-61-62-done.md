# 인계 노트 — 2026-09-14, #61·#62 완료 · 롤백 규칙 5-1 + 첫 적용 · 후속 #66·#67

직전 노트: `2026-09-11-1a-2-done.md`. **이 노트가 최신이다.**

## 이 세션에서 한 일

**대상 저장소 쓰기 3건(worktree 2 + 원본 미러 1) + 원본 fit 하네스 동기화 1건.** 전부 사용자 승인(게이트 5항목 · 2026-09-14) 안에서.
- **myFinance** worktree `integration/chore-pleiades-62` → **#496 머지 `5467407`** · 원본 `dev` 모드 S 미러 이슈 myFinance#497 → **#498 머지 `36ba209`**
- **myFitness** worktree 같은 브랜치명 → **#375 머지 `3182c1d`** · 머지 후 원본 `~/workspace/myFitness/.claude/rules/workflow.md` `git archive | tar -x` 동기화(사전 사본 → `_workspace/62/backup/` · 부재 목록 0 · index 무변경)
- 서버·DB·텔레그램 무접촉. 빌드·`pm2 restart` 불필요. 세션 종료 시 네 체크아웃 전부 clean(worktree fin `5467407` · fit `3182c1d` · 원본 fin `dev 36ba209` · fit `main 5809c48`).

순서:
1. `pleiades-resume` → **#61 착수**: `dual-repo-change` 5-1(롤백 문서 필수 항목) 신설 + `dual-repo-operator`·`pleiades-orchestrator` 동기화 + 옛 롤백 문서 점검(`harness/04_operator_rollback.md` 결함 3 → 정정 블록). PR #65 → **Codex 8라운드**(P1 9 · P2 6 · 전부 반영). 7라운드째 P1 에서 9-4 스코프 재점검 → **사용자 결정: 축소** — 5-1 은 골격(상태 판정 표 · 세 시점 · 원칙 4)만, 상황별 명령·반례 15건은 `_workspace/61/rollback-checklist.md` 초안 → **#66**. 8회차 P1 0 → 머지 `00544ac`.
2. **#62 착수**(오케스트레이터 파이프라인 축약형): 실측(`~/.codex/models_cache.json` · 쿼터 소비 없음) → 계획 `_workspace/62/01_plan_62.md` → 재감사(`03_auditor_62plan.md` · 정정 2 · 블로커 0) → 승인 → `dual-repo-operator` 집행(fin → fit · 추가 줄 byte-identical) → 9-1 사전 리뷰(major 1 · info 2 → 전부 반영 · amend) → PR 2 → 봇(둘 다 오픈 3분 뒤 자동 · P2 1 `CODEX_HOME` → 대칭 반영) → 머지 → 10절 → 원본 도달(fit archive · fin 미러 #498 · 봇 P2 `$HOME` → **#67**).
3. 롤백 문서 `_workspace/62/04_operator_rollback.md` — **5-1 형식 첫 실전 적용**(상태 판정 표 3행 · 세 시점 · 원본 도달분 · SHA 실값). 문제 없었다.

## PR 현황 (전부 머지)

| PR | 브랜치 | 내용 | 이슈 | 리뷰 | 되돌리기 |
|---|---|---|---|---|---|
| ~~#65~~ `00544ac` | `chore/61-1` | 5-1 신설 · 체크리스트 초안 · 옛 롤백 문서 정정 · `.gitignore` `_workspace/**/backup/` | #61 | self-review · 봇 8회(P1 9·P2 6 반영) | 즉시 |
| ~~myFinance#496~~ `5467407` | `integration/chore-pleiades-62` | 8-4 `model` 예시 → 로컬 캐시 출처 | #62 | 사전 0/1/2 반영 · 봇 0/0 · P2 1 반영 | 즉시 |
| ~~myFitness#375~~ `3182c1d` | 같은 이름 | 대칭 | #62 | 동일 | 즉시 · 원본 동기화분 중간 |
| ~~myFinance#498~~ `36ba209` | `chore/497-1`(원본 `dev` · 모드 S) | #496 미러 | myFinance#497 | self-review · 봇 P2 1 → #67 | 즉시 |
| (이 노트) | `chore/68-1` | 인계 · CLAUDE.md · measured-facts · `_workspace/62/` | #68 | self-review | 즉시 |

## 결정된 것 (사용자 2026-09-14)

| | 결정 | 귀결 |
|---|---|---|
| **#61 스코프 축소** | 5-1 은 "무엇을 담는가"(상태 판정 표 · 세 시점 · 원칙 4)만. "어떻게"(상황별 명령)는 초안 → #66 | 명령 목록으로 쓴 규칙은 상황마다 반례가 나온다(Codex 7라운드 P1 9). 규칙은 원칙, 명령은 참고 |
| **#62 문안** | 하드코딩 예시 제거 + 출처(`models_cache.json` 의 `visibility: "list"` slug) + 실측값·날짜·버전 병기 혼합안 | 다음 모델 개편에도 문안이 살아남는다 |
| **#62 게이트** | 5항목 승인 · 원본 도달(fit archive · fin 미러) 포함 | G-2 선례 그대로 |
| **#498 P2** | `~` → `$HOME` 은 #67 후속 · 미러 그대로 머지 | 세 벌(fin·fit 통합 + fin 원본)이 같은 문구로 유지된다 |

## 결정되지 않은 것

| 이슈/질문 | 내용 | 언제 |
|---|---|---|
| **#66** | 롤백 체크리스트 초안 확정 — 다음 대상 저장소 집행에 적용해 빈 상황 채우기 · 스킬 리소스 승격 여부 | 다음 집행 후 |
| **#67** | 8-4 캐시 경로 `${CODEX_HOME:-~/.codex}` → `$HOME/.codex` — 3벌 대칭(PR 2 + 미러 1 + fit archive) · 한 줄 | 다음 대상 저장소 집행에 묶어도 됨 |
| **#48** | 1a-1 info 3 | 1a-3 인계 |
| **Q45** | 서버 https·node·git | 1a-3 착수 전(서버 접속 승인) |
| **첫 태그·릴리즈** | `v0.1.0` 릴리즈 PR — 봇 게이트 대체 없음 | 1a-3 전 |
| #17 · #11 · Q27 · `error.ts` shim · `sensitiveLogs` B′ · Q36 · Q38 · Q23 · Q12~Q14 · Q7·Q2·Q3 | 이전 노트 그대로 | 변동 없음 |

**단계 0 은 여전히 미착수, 단계 1 을 먼저 하기로 결정된 적도 없다.**

## 다음 세션의 첫 액션 후보

1. **1a-3 준비** — 첫 태그(릴리즈 PR `dev`→`main` · 봇 필수 · 대체 없음) + Q45 서버 측정(접속 승인) + #48 I1 `VERSION`. 003 §5-2 1a-3 행: fit `send.ts` 124줄 → 패키지 교체(호출 6건) · fin 흡수 · Q10-L 분할 통일 · `error.ts` shim. 되돌리기: 태그 삭제 즉시 · 코드 중간. **이 집행의 롤백 문서가 #66 의 둘째 적용 사례**가 된다.
2. **#67** — 한 줄 · 3벌 · 즉시. 1a-3 fit/fin 집행 PR 에 묶으면 PR 을 따로 내지 않아도 된다(같은 파일은 아니지만 같은 브랜치 가능 — 단 이슈-PR 1:1 규칙상 별도 커밋·`Refs` 표기 필요).
3. **#66** — 1a-3 롤백 문서를 쓰면서 초안을 확정.

## 주의사항

- **두 저장소는 실서비스 중.** 쓰기 전 사용자 확인. 이번 세션 쓰기: fin worktree 1 + 원본 미러 1 · fit worktree 1 + 원본 하네스 동기화 1.
- **세션 시작 시 관측 상태:** 네 체크아웃 전부 기대 브랜치 · clean. 인계 노트보다 현재 상태를 신뢰.
- **롤백 문서는 5-1 형식이다** — 첫머리 상태 판정 표(PR 마다 한 행) → 세 시점 → 원본 도달분. 명령은 `_workspace/61/rollback-checklist.md` 참고(초안). 사전 사본은 `_workspace/<주제>/backup/`(gitignored · 세션 밖).
- **Codex 봇 관측(이번 세션):** pleiades#65 · myFinance#496 · myFitness#375 · myFinance#498 **전부 오픈 3분 뒤 자동 리뷰**. 1a-2 때(대상 저장소 PR 30분 컷오프까지 미게시)와 다르다 — 컷오프 절차는 유지하되 기대값은 "온다" 쪽. `@codex review` 뒤 재리뷰는 3~4분.
- **Codex 가 문서 규칙에 반례를 내는 패턴:** 명령 목록 형태의 규칙은 라운드마다 새 상황(머지 여부 · 배포 · 미러 · rename · tracked 화 · 병행 인스턴스 · 릴리즈 경유 · `CODEX_HOME` · 따옴표 `~`)으로 P1 이 나온다. 규칙은 원칙으로, 명령은 참고 문서로 — 그래야 루프가 끝난다(#61 · 8라운드 교훈).
- **codex-cli MCP 지원 모델 출처:** `${CODEX_HOME:-$HOME/.codex}/models_cache.json` 의 `visibility: "list"` slug(실측 2026-09-14: `gpt-5.5` · `gpt-5.4` · `gpt-5.4-mini` · 캐시 `fetched_at` 2026-07-02). 로컬 파일 읽기라 쿼터 소비 없음. `gpt-4o` 는 없다.
- **fin 은 `dev` 가 기본 브랜치가 아니다** — 원본 `dev` 로 보낸 미러 PR 도 `Closes` 가 자동 실행되지 않는다(#497 수동 종료). pleiades 는 `dev` 가 기본이라 자동 실행된다(#61).
- **`MEMORY.md` 18줄**(한도 200).

## 정정된 기록 — 옛 문서를 그 전제로 읽지 말 것

- `dual-repo-change` §5-4 *"롤백 절차를 남긴다"* → **5-1 필수 항목**이 생겼다. 옛 롤백 문서 `_workspace/harness/04_operator_rollback.md`(2026-09-07) 머지 후 절은 **실행하지 말 것**(브랜치 없는 revert · `gh pr create` 부재 · squash 에 `-m 1`) — 정정 블록 append.
- fin·fit `workflow.md` 8-4 `model` 예시 `"gpt-4o"` → 제거. 세 벌 모두 로컬 캐시 출처 문안(단 `${CODEX_HOME:-~/.codex}` 는 #67 에서 `$HOME` 으로).
- 1a-2 노트의 *"대상 저장소 PR 은 30분 컷오프까지 미게시"* → 이번엔 4/4 자동 게시. 봇 거동은 일정하지 않다.

## 재현이 필요한 절차

- **fit 원본 `.claude/` 동기화(5-1 원칙 3 포함)** — `_workspace/61/rollback-checklist.md` §3-1 의 사전 사본 블록 → `git -C ~/workspace/myFitness archive integration/pleiades <paths> | tar -x -C ~/workspace/myFitness`(이번 1파일판 실행 검증 · `_workspace/62/04_operator_rollback.md` §3-1).
- **fin 모드 S 미러** — 원본 `dev` · 이슈는 fin 저장소 · 문장 단위 적용 · 정정 블록 헤더에 `미러 fomalhaut84/myFinance#<issue>` + 말미 원 PR 문장(#494·#497 선례).
- **Codex 지원 모델 실측** — measured-facts 2026-09-14 절의 명령(auth.json 은 열지 않는다).
