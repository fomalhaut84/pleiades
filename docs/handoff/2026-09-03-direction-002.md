# 인계 노트 — 2026-09-03, 방향 확정 세션

직전 노트: `2026-09-03-from-myfinance.md` (기획 이관). 이 노트가 최신이다.

## 이 세션에서 한 일

`pleiades-resume` 로 시작 → 브리핑 → 사용자가 Q1·Q4 에 답하면서 **방향이 확정됐다.**
**두 대상 저장소는 한 줄도 건드리지 않았다.** 읽기만 했다.

1. Q1·Q4 답 수령 → Q5·Q6 신규 도출 및 확정
2. `docs/specs/002-platform-direction.md` 작성 (정본 방향)
3. 알림 채널 교체 비용 실측 → `measured-facts.md` 추가 (발견 4·5·6)
4. 단계 0 실제 범위 실측 → 001 정정 (발견 7)
5. `CLAUDE.md` 상태·문서 지도·핵심 전제 갱신
6. 아티팩트 갱신 (같은 URL) — https://claude.ai/code/artifact/88cd616a-efe0-4d84-b298-e6961675ae0d
7. **git init + 커밋 2개** (`main`). 소스는 `docs/artifacts/integration-artifact.html` 로 버전 관리 — 재발행 가능

## 결정된 것

| | 질문 | 답 |
|---|---|---|
| Q1 | "키운다"의 목표 | **개인 비서 플랫폼.** 도메인을 계속 늘린다 |
| Q4 | myFitness 테스트 | **붙인다** (범위는 추출 대상 코드로 한정) |
| Q5 | 도메인 #3 | **일정·캘린더** |
| Q6 | 알림 채널 | **Discord** |

이에 따라 001 의 권고 경로가 개정됐다. 요점 셋:

- **B2(모노레포)가 요구사항이 됐다.** 진입 조건: "크로스 저장소 변경 월 2회" → **"도메인 #3 을 붙일 때"**
- **알림 어댑터가 앞으로 당겨졌다.** 001 의 단계 3 일부 → 002 의 단계 1
- **B3 는 쪼갰다.** "한 곳에서 다 본다"는 Discord 채널로 단계 1 에 딸려온다. DB·단일 봇은 여전히 나중

## 결정되지 않은 것

- **단계 0 실행 여부** — 범위를 실측해 제시했고, 사용자가 "오늘은 문서까지"로 판단. 다음 세션 안건
- Q7 (도메인별 DB vs 단일 DB schema 분리) — 단계 4 선행
- Q2 (독립 배포 포기 가능한가) — 단계 3 이후
- Q3 (봇 인바운드 통합) — 단계 4 이후

## 다음 세션의 첫 액션 후보

1. **단계 0 실행.** 범위 선택지는 이미 정리돼 있다:
   - **myFinance 단방향 파일럿 (권고)** — 저장소 1개만. `mcp-config.json` 한 항목 +
     `ALLOWED_TOOLS` 에 `mcp__myfitness__*` 등재 + 시스템 프롬프트 한 줄. 반나절
   - 양방향 전체 — myFitness 는 `ensureMcpConfig()` 코드 수정 + 인라인 `--allowedTools`
     문자열 확장이 추가로 필요. 하루
2. **단계 1 착수** — `@pleiades/notify` 설계. 단계 0 없이도 독립적으로 가치가 있다
3. **myFitness 죽은 파일 정리** — `src/lib/ai/mcp-config.json` (별개 건)

## 주의사항

- **두 저장소는 실서비스 중이다.** 같은 Ubuntu 서버, PM2 6 프로세스. 쓰기 전 사용자 확인
- 세션 시작 시 두 저장소 모두 `dev` clean 이었다. 인계 노트보다 **현재 상태를 신뢰할 것**
- **단계 0 은 "JSON 두 줄"이 아니다** (발견 7). 코드 변경 + 빌드 + `pm2 restart` 다.
  001 을 읽고 그 전제로 움직이지 말 것 — 002 와 `measured-facts.md` 가 정정본
- myFitness `src/lib/ai/mcp-config.json` 은 **죽은 파일**. 고쳐도 반영 안 됨
- myFinance `MEMORY.md` 126줄 (한도 200). 도메인 3개면 부족 — 단계 2 전에 압축 필수
- myFinance PR #58 (`next 14.2.35 → 16.1.6`) 폐기 대상. 실제 현재 15.5.16 이라 베이스가 틀렸다
- myFinance `CLAUDE.md` 의 "Next.js 14" 표기 stale (실제 15.5.16)

## 아티팩트 재발행 방법

소스가 저장소에 있으므로 다음 세션에서도 갱신할 수 있다:

```
Artifact(file_path="docs/artifacts/integration-artifact.html",
         url="https://claude.ai/code/artifact/88cd616a-efe0-4d84-b298-e6961675ae0d")
```

발행 전 반드시 `action:"read"` 로 현재 버전을 먼저 읽어야 한다 (안 읽으면 발행이 거부된다).
