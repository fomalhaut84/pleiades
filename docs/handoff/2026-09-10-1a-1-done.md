# 인계 노트 — 2026-09-10, 1a-1 완료 · 아티팩트 v6 · #38 skill 충돌 실측 · #42 fit 8-4 집행

직전 노트: `2026-09-09-1a-1-prep-h1-h3.md`. **이 노트가 최신이다.**

## 이 세션에서 한 일

**대상 저장소 쓰기 1건 — myFitness (#42).** worktree `repos/myFitness` 에 `integration/chore-pleiades-42` → **myFitness#373**(머지 `8b7a224`) + **원본 `~/workspace/myFitness/.claude/rules/workflow.md` 1파일 동기화**(사용자 승인 · `git archive | tar -x` · 사전 사본은 스크래치패드). myFinance 는 읽기만(worktree·원본 무변경 · `node_modules/grammy` 읽기). 서버·DB·텔레그램 무접촉. 세션 종료 시 네 체크아웃 전부 clean(worktree 2 = `integration/pleiades` · fin `dev` · fit `main`).

사용자 지시 순서 **4 → 1 → 2 → 3** 그대로:

1. **4 · 아티팩트 재발행** (#45 → PR #46 머지 `86227cc`) — 2026-09-04 판(v4) 이후 이월된 005·1a-0·#37 반영 → **v6**(같은 URL). 003·004·005 헤더 "읽기용 아티팩트" 를 요약본 포인터로. 봇 P2 1(34파일 집계 문구) 반영.
2. **1 · 1a-1 착수·완료** (#47 → PR #49 머지 `c44f5c8`) — `pleiades-orchestrator`: 실측(직접) → 계획 `_workspace/1a-1/01_plan_1a1.md` → **착수 직전 재감사**(`03_auditor_1a1plan.md` · 정정 13 · 블로커 1 = `TelegramApi` 시그니처) → 2회차 계획 → **사용자 승인 4건(D-1~D-4)** → E1 게이트 → E2~E4 TDD → E5b 003 역반영 2건(`decision-writer`) → **E6 소비자 e2e 가 결함 1건 발견**(TS2580 · 수정) + grammy 프로브 → E7 사전 리뷰(major 5 반영 · info 5 중 2 반영 · 3 → #48) → PR → 봇 P2 1(서로게이트 분할) 반영. **테스트 93건 · 8절 4종 통과 · `dev` 재검증 통과.**
3. **2 · #38 skill 충돌 실측** (PR #50 머지 `c22b475`) — 읽기 전용 · N18 방식. **skill 충돌은 이름 단위 · 항목 1개만 남고 · `cwd` 쪽이 이긴다**(agent 와 반대). (ii) 개명 불필요. 005 §4-5 정정 · CLAUDE.md 하네스 절 1줄. 봇 P2 1(Q3 프롬프트 원문 보존) 반영.
4. **3 · #42 fit 8-4** (myFitness#373) — `dual-repo-change` 모드 I 승인 게이트 → `dual-repo-operator` 집행 → 9-1 사전 리뷰(major 1 = 정정 블록 인용 오류 · 제 지시문 실수 → 반영) → PR → 봇 P2 1("model not supported" 를 쿼터 초과로 안내 — **fin 도 같은 문장** → #51) 반영 → 머지 → worktree pull → **원본 동기화(승인)** → #42 종료.
5. 후속 이슈 신설 **#48**(1a-1 info 3 · 1a-3 인계) · **#51**(fin 8-4 같은 결함) · 종료 **#32**(I1·I3 는 #47 에서 이행) · **#45 · #47 · #38 · #42**.
6. `measured-facts.md` 3절 신설(1a-1 집행 실측 · E7 후속 · #38) · `_workspace/1a-1/`(계획·감사) · `_workspace/harness/04_operator_42_rollback.md`.

## PR 현황

| PR | 브랜치 | 내용 | 이슈 | 되돌리기 | 상태 |
|---|---|---|---|---|---|
| ~~#46~~ 머지 `86227cc` | `chore/45-1` | 아티팩트 v5→v6 · 스펙 헤더 3줄 | #45 | 즉시 | 10절 완료 · 봇 P2 1 반영 |
| ~~#49~~ 머지 `c44f5c8` | `feat/47-1` | 1a-1 구현 · 테스트 93 · 게이트 · 003 정정 2 · README | #47 | **즉시(1a-3 착수 전까지)** | 10절 완료 · 사전 major 5 · 봇 P2 1 반영 |
| ~~#50~~ 머지 `c22b475` | `chore/38-1` | #38 실측 · 005 정정 · CLAUDE.md 1줄 | #38 | 즉시 | 10절 완료 · #49 뒤 rebase(measured-facts EOF 충돌 해소) |
| ~~myFitness#373~~ 머지 `8b7a224` | `integration/chore-pleiades-42` | fit `workflow.md` 8-4 문구·순서 | #42 `Refs` (수동 종료함) | worktree 즉시 / **원본 중간**(git 이력 없음 · 폴백 `git archive 2195854 .claude/rules/workflow.md`) | 10절 완료 + 원본 동기화 완료 |
| (이 노트) | `chore/52-1` | 인계 · CLAUDE.md · 롤백 문서 | #52 | 즉시 | 열림 |

## 결정된 것 (사용자 2026-09-10)

| | 결정 | 귀결 |
|---|---|---|
| **D-1 `Content`** | `{ text, format: 'html'\|'plain', components? }` + `html()` — 003 이 참조만 하고 정의하지 않던 빈칸 | 003 §4-2 정정 블록으로 역반영. `Components = object`(사전 리뷰 M-3 — `Record<string, unknown>` 은 grammy `InlineKeyboard` 클래스를 거부) |
| **D-2 `Transport.maxLength`** | 포트 표면 1개 추가 · **유한 = 코어가 분할·재시도·폴백 소유(`send` = 청크 1건) / `Infinity` = 어댑터 소유** · 섞지 않는다 | §4-3 제약 2 둘째 형태. 감사 S-3(분할을 Transport 안에 두면 청크 중복 전송) |
| **D-3 grammy 무의존** | 구조적 `TelegramApi` — **`parse_mode?: string` · 메서드 단축 문법 필수**(감사 V1/V8 · 리터럴·프로퍼티 문법은 TS2322) | 대안(루트 devDep/peerDep)도 `prepare` 를 통과하지만(`--include=peer`) 구조적 타입이 가장 싸다(`npm ci` warm 1.56 s · 트리 20 K · 누수 0). **대입 가능성은 패키지 테스트가 못 잡는다** → grammy 프로브(measured-facts E6b) |
| **D-4 `Route → Transport` 매핑** | config 에 넣는다(`transport` 단일 또는 Route 맵) — 003 §1-4 요구사항 1 이행(새 결정 아님) | 안 넣으면 1b 되돌리기 즉시 → 중간 |
| **#42 집행 + 원본 동기화** | 승인 2회 | 완료 |
| **#38 (ii) 개명** | 측정 결과로 **불필요** 판정(제 권고 · 사용자 별도 확정 없음 — PR #50 머지로 수용) | 개명하면 pleiades 세션에 fit 판 `orphan-check` 가 살아나 두 벌 공존 |

**구현 세부 결정(오케스트레이터 · 승인 범위 안):** `csvEnv(name, { numericOnly? })` 로 fin 의 `.map(Number).filter(!isNaN)` 탈락 + 정규화 규칙을 옵션 보존(`total` = DB `recipientCount` 3 + API `totalChats` 1 의 의미 보존 · env 실값 미확인) · 폴백 판정 fit `isHtmlParseError`(fin 400 전체 규칙 미승계 — **관측 변경 후보 3번째** · 003 §5-2 정정) · 폴백 예산 보존(fit `attempt--`) · `components` 는 마지막 청크 · `ref` = 마지막 청크(`telegramMessageId` 읽기 0) · 빈 분할 → throw(M-1) · `maxLength` 0/음수/NaN → RangeError(M-2) · 서로게이트 쌍 보존 분할(봇 P2 — fin 정본과 다른 유일한 지점).

## 결정되지 않은 것

| 이슈/질문 | 내용 | 언제 |
|---|---|---|
| **#48** | 1a-1 후속 info 3 — `VERSION` 이중 관리 · `[bot]` 로그 prefix grep · fit keyboard 실패 로그 문자열 | 1a-3 인계 |
| **#51** | fin 8-4 같은 결함 문장 (모드 I · fin 1파일 · 승인 게이트) | 별건 · 즉시 |
| **Q45** | 서버 https·node·git | 1a-3 착수 전(서버 접속 승인) |
| **Q16·Q17·Q18** | 1a-2 (fit vitest) 집행 지시 | 1a-2 착수 전 |
| **Q27** | fin `rsu.ts:184` 파사드 흡수 | 1a-4 착수 전 |
| **`error.ts` shim** | 인바운드 6곳 import 경로 — 003 §5-1 "착수 직전 결정" | 1a-3·1a-4 착수 시 |
| **첫 태그·릴리즈** | `@pleiades/notify` 를 소비하려면 태그 필요(릴리즈 PR · `workflow.md` 릴리즈 절차 · 봇 게이트 대체 없음) | 1a-3 전 |
| **`sensitiveLogs` 가드 B′** | 호출부 4지점 `sanitizeMessage`(파사드 로그는 이미 sanitize) | 1a-4 |
| Q36 · Q38 · #17 · #11 · Q23 · Q12~Q14 · Q7·Q2·Q3 | 이전 노트 그대로 | 변동 없음 |

**단계 0 은 여전히 미착수, 단계 1 을 먼저 하기로 결정된 적도 없다.**

## 다음 세션의 첫 액션 후보

1. **1a-2 — myFitness vitest 도입** (003 §5-2 · devDep 3 + `vitest.config.mts` + scripts · 범위는 패키지 코드 테스트 기반) — Q16·Q17·Q18 답 필요(U1 blocker = fit `overrides` `$postcss` 1줄 · measured-facts 발견 23). 모드 I · 승인 게이트 · 되돌리기 **즉시**(4파일 revert + `npm ci`). 1a-3 의 전제.
2. **1a-3 준비 — 첫 태그 + Q45 서버 측정** — 릴리즈 PR(`dev`→`main` · 봇 필수) → `v0.1.0` 태그 → 소비자 표기 `git+https://…#v0.1.0`. Q45 는 서버 접속 승인 필요. 되돌리기: 태그 삭제 즉시.
3. **#51 fin 8-4 문장** — 모드 I · 1파일 · 즉시.
4. **#48 I1 `VERSION`** — 태그 시점에 함께.

## 주의사항

- **두 저장소는 실서비스 중.** 쓰기 전 사용자 확인. 이번 세션 쓰기: **myFitness 1건(#42 · 승인 2회)** · myFinance 0
- **세션 종료 관측:** worktree 2 = `integration/pleiades` clean(fin `6542152` · fit **`8b7a224`**) · 원본 fin `dev` / fit `main` clean · **fit 원본 `.claude/` 는 worktree 와 동일**(`settings.local.json` 만 차이). pleiades `dev` = `c22b475`(+이 노트 PR). **다음 세션은 이 노트보다 현재 상태를 신뢰하라**
- **`packages/notify` 는 이제 코드가 있다** — 8절 pleiades 행이 **`npm run typecheck && npm run typecheck:test`** 로 바뀌었다(#47). `npm --prefix packages/notify install` 선행. 테스트 93건.
- **소비자 임시 클론의 `prepare` 는 루트 devDep 만 설치한다** — 패키지 소스가 `@types/node`·grammy 에 기대면 로컬 4종은 통과하고 소비자 `npm install` 만 깨진다(E6 실증). 회귀 `build-config.test.ts` M3(빈 typeRoots)와 `telegram.test.ts`(grammy 문자열 0)가 지킨다. **새 소스 파일이 전역 `process`·`Buffer`·`node:*` 를 쓰면 같은 함정** — `targets.ts` 의 모듈 스코프 선언 패턴을 따른다.
- **grammy 대입은 패키지 테스트가 보증하지 못한다** — `TelegramApi` 를 바꾸면 measured-facts E6b 프로브(`repos/*/node_modules/grammy` · 두 버전)를 다시 돌린다. `parse_mode?: string` + 메서드 단축 문법이 계약.
- **skill 충돌 규칙(#38):** rule 공존 · agent 마지막 `--add-dir` 승 · **skill cwd 승**. `bin/claude-with fit` 은 pleiades 판 `orphan-check` 를 로드한다.
- **Codex 봇은 푸시마다 자동 재실행되지 않았다**(이번 세션 PR #46·#49·#50·#373 전부 첫 커밋만 리뷰 · P2 만 반영해 재요청 안 함) — 09-09 노트의 "푸시마다 자동" 관측과 **다르다**. 9-3 문구는 그대로 두고 두 관측을 병기해 둔다.
- **`sed -i` 치환문에 `#` 가 들어가면 구분자와 충돌한다**(PR #46 첫 커밋에서 003·004 헤더가 빠졌다) — python 치환을 쓴다.
- `AskUserQuestion` 의 이슈 인용에서 "옛" 이 "옷" 으로 보이는 표기가 있었다(입력기 오타 · 문서에는 없음).
- `MEMORY.md` 18줄(한도 200).

## 정정된 기록 — 옛 문서를 그 전제로 읽지 말 것

- 003 §4-2 *"정본 시그니처"* → `Content`·`maxLength`·`transport` 맵·`TelegramApi`·`csvEnv` 는 **2026-09-10 정정 블록과 함께가 정본**(맨 위 블록만 읽으면 `Content` 가 없다)
- 003 §5-2 정정 ① fin 관측 변경 **2건 → 3건 후보**(폴백 트리거 축소 · 빈도 미측정)
- 계획 1회차 S-2 *"grammy 를 `import type` 으로도 참조할 수 없다"* → **선언 없이 참조하면** 깨진다(peerDep/devDep 는 통과) — 감사 A-2
- 005 §4-5 *"skill 충돌 동작 미측정"* → 측정됨(cwd 승). 005 §2 표 · CLAUDE.md 갱신됨
- `build-config.test.ts:5-6` 주석의 "grammy peerDep .d.ts" 전제는 **성립하나 채택하지 않았다**(구조적 타입)
- 아티팩트 v5 의 *"34파일 중 2 가 pleiades 로"* → v6 에서 정정(원본 34 전부 잔존 · 산출물 2 는 분모 밖)

## 재현이 필요한 절차

- **원본 fit `.claude/` 복원·갱신** — `git -C ~/workspace/myFitness archive integration/pleiades .claude CLAUDE.md | tar -x -C ~/workspace/myFitness`(이번 세션 1파일판 실행 검증)
- **소비자 e2e + grammy 프로브** — measured-facts "1a-1 집행 실측" §E6·E6b 의 명령(스크래치패드 `e6/run.sh` 는 소멸 · 절차는 문서에)
- **#38 런타임 프로브** — measured-facts "#38" 절의 Q3 명령 블록(프롬프트 원문 포함)
