# 인계 노트 — 2026-09-08, 1a-0 집행 완료(PR #33) · Q44·Q46·Q47 확정 · Q45 보류(1a-3 전) · 봇 불가 대체 경로(#14)

직전 노트: `2026-09-07-harness-integration.md`. **이 노트가 최신이다.**

## 이 세션에서 한 일

**대상 저장소 두 곳은 읽기만 했다 — 쓰기 0.** 두 worktree(`repos/*` · `integration/pleiades`)와 원본(fin `dev` · fit `main`) 모두 세션 시작·종료 시 clean, HEAD 무이동. 서버·DB·텔레그램 무접촉. pleiades 저장소만 바뀌었다.

1. **PR #30 머지 정리** (10절) — 이슈 #29 종료 코멘트 · `chore/29-1` 삭제 · orphan 0
2. **1a-0 착수** (이슈 #31) — `pleiades-orchestrator` 파이프라인 전체를 한 번 돌렸다:
   - Phase 1 **실측 2건** (`repo-surveyor` ×2 병렬) → `_workspace/1a-0/01_surveyor_{db_writes,gitdep}.md` + `measured-facts.md` 2280행~ 2절
   - Phase 2·3 **초안 3회 ↔ 감사 3회** — 정정 **10 → 8 → 2**(마지막은 문안 4줄, 오케스트레이터가 직접 반영). 4회차 순환은 하지 않았다(스킬 기준 3회 — 축소한 것은 문서가 아니라 순환)
   - Phase 4 **사용자 결정 4건** (아래)
   - Phase 5 **집행** — E2 정본 정정(`decision-writer`) ∥ E3 스캐폴딩(직접) → E5 검증 → E4 소비자 검증(스크래치패드) → E6 사전 리뷰(`pr-review-toolkit:code-reviewer` · major 2 반영) → **PR #33** → 머지 → 10절 정리
3. **#14 룰 보완** — `chore/14-1` → **PR #35** (열림, 봇 대기)
4. 이 노트 + CLAUDE.md 동기화 — `chore/34-1` (이슈 #34)

## PR 현황

| PR | 브랜치 | 내용 | 이슈 | 되돌리기 | 상태 |
|---|---|---|---|---|---|
| ~~#30~~ 머지 | `chore/29-1` | Q42 확정 문서 | #29 | 즉시 | 10절 정리 완료 |
| ~~**#33**~~ **머지** `bfd951b` | `feat/31-1` (커밋 7) | **1a-0** — 실측 · 초안/감사 · 스캐폴딩 · 정본 정정 · 집행 기록 | #31 `Closes` | **즉시** (루트 `package.json` 은 이 PR 이 신규 생성) | 10절 정리 완료. **봇: 미실행 (머지까지 미게시, 2026-09-08)** — 사전 에이전트 리뷰 1회(major 2 반영)만. 9-6 body 에 그대로 기록 |
| **#35** | `chore/14-1` | #14 봇 불가 대체 경로 — 9-3 표 · 9-4/9-6 표기 · 핫픽스 명시 승인 | #14 `Closes` | 즉시 | **열림 · 봇 대기** |
| (이 노트) | `chore/34-1` | 인계 노트 · CLAUDE.md | #34 `Closes` | 즉시 | 열림 |

## 결정된 것 (사용자 2026-09-08) — Q45 는 결정이 아니라 **보류**다(아래 미결 표)

| | 결정 | 근거·귀결 |
|---|---|---|
| **Q47** 배포 형태 | **ALT-d 위임형** — 루트 `name:"pleiades"`·`private:true`, `main`/`types`/`exports`/`files`/`prepare` 가 `packages/notify/dist` 위임. **`workspaces`·`peerDependencies`·`engines` 는 1a-0 에 넣지 않는다** | npm git 의존성은 **저장소 루트만 설치**(서브디렉터리 문법 없음 — 실측). 003 §2-1 승계 주장은 "단계 4 에서 루트 6키 처리 + `workspaces` 1키 추가" 로 조건부 성립. `workspaces` 를 넣으면 소비자 `npm ci` 2.14 → 3.67~3.96 s · 캐시 19 → 134 MB(감사 실측) |
| **Q44** 검증 경로 | **γ 로컬 `repos/*` 개발 루프 + β2 서버 빈 스키마 DB 사본(`myfinance_int`·`myfitness_int`)** 도달 검증. α(RO 롤)·β1(실데이터 사본) 배제, δ(env 스위치) 보류 | RO 롤은 정상 본문 전송이 fin 15 모듈 중 10 · fit 4 중 1 뿐. 절단·plain 폴백 분기는 **1a-1 패키지 단위 테스트**가 커버. β2 서버 승인은 **1a-3 과 함께** |
| **Q46** | 검증용 텔레그램 봇 토큰 **별도 발급**(BotFather — 사용자, 병행 인스턴스 기동 전) | 봇 미기동으로는 못 막는다 — 웹만으로 전송 나가는 fin 경로 3개(`/api/alerts/history/[id]/retry` · `/api/deposits` 외) |
| **Q45** | 서버 github https 아웃바운드 · node/npm · git — **보류, 1a-3 착수 전** 측정(서버 접속 별도 승인) | 1a-0·1a-1 은 막지 않는다. 막혀 있으면 deploy key 가 되살아나고 `prepare` 가 매 배포마다 실패 |
| **Q28** | **소멸** — `github:`·`git+https`·`git+ssh` 세 형태 전부 자격 증명 불필요(pacote https 우선, ssh 호출 0회). 권고 표기 `git+https://github.com/fomalhaut84/pleiades.git#<tag>` (npm 이 `github:` 로 정규화하므로 1a-3 에서 직접 편집) | 003 §5-2 "딸린 소멸" 의 `git+ssh` 자격 요구 주장은 **lockfile 표기를 계약으로 읽은 오정정**이었다 — 정정의 정정 |
| Q42 조건표 | **6조건 → 10조건** (정본 003 §10-1 2026-09-08 정정 블록). 신설: **텔레그램 격리(최우선)** · 필수 분리 env fin 9 / fit 8 · MCP 포트·로그 · 선행 빌드 | 뒤집힌 전제 3: RO 롤 커버리지 · cron off 는 env 아님(플래그 0건, fit `startOrphanSweeper()` 는 코드) · fit 웹은 부팅만으로 `reportJob.updateMany`(쓰기 가능 롤이면 실서비스 job `failed` 마킹 가능 — **RO 롤이 안전장치**) |
| #14 | 권고 ① — 봇 불가 시 self-review 경로는 대기 없음 · 필수 경로는 9-1 사전 리뷰가 완료 판정 · 핫픽스는 + 사용자 명시 승인 · 표기 `봇: 미실행 (<사유>, 날짜)` · 소급 리뷰 없음 | PR #35 (봇 대기 — 이 PR 자체가 그 경우가 될 수 있다) |
| 결정으로 처리(질문 아님) | dist 미커밋(D-a · 절약 1.6 s) · `prepare` = `tsc -p packages/notify`(S-2) · `src/index.ts` = `VERSION` 상수(I-a) · `tsconfig` `exclude` 4패턴 + `skipLibCheck` · `npm test` 서브 위임 · 루트+서브 lockfile 커밋 · vitest `^4.1.8`(fin·1a-2 와 동일) | 초안 4-1 · 사전 리뷰 M1·M2 |

## 결정되지 않은 것

| 이슈/질문 | 내용 | 언제 |
|---|---|---|
| **Q45** | 서버 https·node·git 측정 | **1a-3 착수 전** (서버 접속 승인) |
| **#32** | 1a-0 사전 리뷰 info — 테스트 타입체크 게이트(`vitest --typecheck` 또는 `tsconfig.test.json`) · 패키지 README | 1a-1 착수 시 · 1a-3 전 |
| **RM-3·RM-4·RM-5·RM-7** | 실 패키지 `npm ci` 재측정(1a-1 후) · β2 빈 DB 에서 fit 리포트 본문(검증 단계) · fin 웹 트리거 동적 import 2건 · 측정 A M3-5 `CLAUDE_BIN` 등 분류 이견 | 초안 4-1 표 |
| **1a-1 선결** | #1 잔여 **H-1(Q41)** · **H-3(fit)** + **Q10 · Q19 · Q25 · Q26**(§4-2 시그니처) | 1a-1 착수 전 — **변동 없음** |
| Q36 · Q38 · #17 · #11 · #3 · Q23 | 이전 노트 그대로 | 변동 없음 |

**단계 0 은 여전히 미착수, 단계 1 을 먼저 하기로 결정된 적도 없다** (2026-09-04 노트 그대로). Q23 은 Q44 의 γ(로컬)가 "로컬 HTTP 상주" 전제 원문 그대로 풀 수 있는 유일한 옵션 — 아직 실행 안 함.

## 다음 세션의 첫 액션 후보

1. **PR #35 · (이 노트 PR) 머지 → 10절 정리.** 되돌리기 즉시. 봇이 안 오면 #35 자체가 9-3 봇 불가 표(self-review 경로)를 첫 적용한다
2. **H-3(fit)** — 2026-09-07 노트 후보 2 그대로(모드 I · `integration/chore-pleiades-8-fit` · 9-0 에이전트 필수 · fin #492 전파 교훈 4→10파일 처음부터). 되돌리기 즉시
3. **H-1** — Q41 이름 결정 후 신규 작성. pleiades 만. 되돌리기 즉시
4. **1a-1 준비** — Q25·Q26·Q19·Q10 을 한 장(§4-2 시그니처)으로 묻는 결정 문서. #32 I1 을 같이. 되돌리기 즉시(문서)
5. **γ 로컬 개발 루프 실제 기동** (Q44 앞부분 · Q23 겸) — 로컬 Postgres 5432 리슨 중, `repos/*` 에 `npm ci`·fit `prisma generate`·`build` 선행(양쪽 `.next` 없음). **로컬 DB 에 실서비스 `DATABASE_URL` 을 넣지 않는다.** 되돌리기 즉시(산출물 삭제)

## 주의사항

- **두 저장소는 실서비스 중.** 쓰기 전 사용자 확인. 이번 세션 쓰기 0
- **세션 시작·종료 관측:** worktree 2 = `integration/pleiades` clean · 원본 fin `dev` / fit `main` clean · fit 원본 하네스 존재. **다음 세션은 이 노트보다 현재 상태를 신뢰하라**
- **pleiades 에 이제 루트 `package.json` 이 있다.** `npm` 을 루트에서 돌리면 typescript 만 설치된다. **`npm test` 는 `npm --prefix packages/notify install` 이 선행**(`workspaces` 없음 — `workflow.md` 8절 정정). `repos/*` 는 상위 `package.json` 을 부모로 잡지 않는다(감사 F5 확인)
- **`packages/notify/tsconfig.json` 의 `exclude`·`skipLibCheck` 를 지우지 마라** — 소비자 `npm ci` 의 `prepare` 가 임시 클론(루트 devDeps 만)에서 돈다. 지우면 **두 실서비스 배포가 동시에 깨지고 원인이 pleiades 에 있어 대상 저장소 롤백으로 안 풀린다.** `build-config.test.ts` 가 단언한다
- **`peerDependencies` 는 1a-1 에서 넣을 때 하한을 `^1.41.1`** — 어긋나면 소비자 `ERESOLVE`(실측 `node_modules` 0)
- **fit 웹은 부팅만으로 DB 를 쓴다** (`sweepOrphanedJobs()` → `reportJob.updateMany`). 어떤 병행·로컬 인스턴스든 **`DATABASE_URL` 이 실서비스를 가리키는 순간 job 이 `failed` 로 마킹된다.** 기동 전 URL 확인을 절차로 — 되돌리기는 즉시가 아니다
- **봇이 머지까지 안 올 수 있다** (PR #33 이 그랬다). 9-6 body 에 `봇: 미실행 (<사유>, 날짜)` 로 적는다 — `0/0` 아님 (#35 룰)
- **감사 순환 3회 상한**을 이번엔 지켰다(10→8→2, 마지막은 문안). 4회차를 돌리지 않고 diff 확인으로 갈음한 판단은 `_workspace/1a-0/03_auditor_1a0.md` 말미에 감사관 권고로 기록돼 있다
- `MEMORY.md` 18줄 (한도 200)
- 스크래치패드(`scratchpad/{gitdep,audit,e4}`)는 세션 한정 — 재현이 필요하면 `_workspace/1a-0/04_operator_1a0.md` §2 · `03_auditor_1a0.md` 의 명령으로 다시 만든다

## 정정된 기록 — 옛 문서를 그 전제로 읽지 말 것

- 003 §1-1 *"`git+ssh://` — 서버 2대 deploy key 필요"* / §5-2 *"`git+ssh` 는 public 에도 SSH 인증을 요구"* → **아니다.** 세 형태 전부 https 로 간다(Q28 소멸). lockfile 의 `git+ssh://` 표기는 계약이 아니다
- 003 §2-1 *"단계 4 에서 `packages/notify/` 무변경 · 버려지는 것은 원격 발행 설정과 `prepare` 뿐"* → **루트 `package.json` 6키 처리 + `workspaces` 1키 추가**가 더 있다(ALT-d). 케이스 C(루트 = 워크스페이스 루트)는 git 의존성으로 **소비 불가**
- 004 §8-4 Q42 *"6조건"* → **10조건**, 정본은 003 §10-1. *"RO 롤로 1a 검증"* 은 fin 10/15 · fit 1/4 로 축소된다
- 003 §10-1 *"1a-0 은 Q42 가 막는다"* → **집행됐다** (PR #33)
- `workflow.md` 8절 *"pleiades 자체에는 아직 npm 프로젝트가 없다"* → **있다**. pleiades 행 4칸 확정
- 초안 1회차의 *"케이스 B 가 유일 해법"* · *"dist 커밋 절약 3.9~4.5 s"* · *"S-2 미측정"* → 감사가 전부 정정(대안 3개 실행 성립 · 1.6 s · S-2 1.99~2.15 s). `_workspace/1a-0/02_writer_1a0.md` §0-1 개정 이력

## 재현이 필요한 절차

- **아티팩트 재발행** — 005·1a-0 상태 미반영 (2026-09-07 노트와 동일). `docs/artifacts/integration-artifact.html` 수정 후 `Artifact(file_path=..., url="https://claude.ai/code/artifact/88cd616a-efe0-4d84-b298-e6961675ae0d")`. 발행 전 `action:"read"`
