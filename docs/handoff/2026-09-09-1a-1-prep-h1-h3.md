# 인계 노트 — 2026-09-09, 1a-1 준비 결정 7건(#37) · H-1(#40) · H-3(fit)(#8 종료) — 하네스 통합(#1) 집행 완료

직전 노트: `2026-09-08-1a-0.md`. **이 노트가 최신이다.**

## 이 세션에서 한 일

**대상 저장소 쓰기 1건 — myFitness (H-3(fit)).** worktree `repos/myFitness` 에 `integration/chore-pleiades-8-fit` → myFitness#372 (머지 `2195854`) + **원본 `~/workspace/myFitness/.claude/` 10파일 동기화**(사용자 승인 · `git archive | tar -x` · 사전 사본). myFinance 는 읽기만(worktree·원본 무변경). 서버·DB·텔레그램 무접촉. 세션 종료 시 네 체크아웃 전부 clean(worktree 2 = `integration/pleiades` · fin `dev` · fit `main`).

1. **#37 1a-1 준비 결정 문서** — `pleiades-orchestrator` 전체 파이프라인: 실측 3건(Q41 하네스 이름 전수 · #32 I1 게이트 · Q10/Q19 정적 근거) → 초안 3회 ↔ 감사 3회(정정 11 → 10 → 6, 마지막은 문안 수준 직접 반영) → **사용자 확정 7건** → 정본 반영(3안: 003 §4-2 재작성 + 대장 27행 append) → **PR #39** 머지(봇 P0/P1 0 · P2 2 반영) → 10절(#37·#3 종료)
2. **#40 H-1** — `.claude/skills/pleiades-codex-loop/SKILL.md` 신규(243줄 · fin 91 + fit 113 입력 · 봇 척도 `P0` 최고 통일 · pleiades Codex 정정 이력 14패턴) → **PR #41** 머지(봇 3회차 P1 2 · P2 6 전부 반영 — 전부 "명령이 실제 CLI 에서 도는가" 유형)
3. **H-3(fit)** — `dual-repo-change` 모드 I: 승인 게이트 → 착수 직전 재감사(**정정 4건 → 계획 개정 → 재승인**: 8 → 10파일 · `prisma generate` 선행 · 원본 동기화 별도 승인 · 숫자 35/54) → `dual-repo-operator` 집행 → 9-1 사전 리뷰(major 1 반영) → **myFitness#372** 봇 5라운드(P1 2 · P2 6 전부 반영) → 머지 → 원본 동기화 → **#8 종료**(fin #492 + fit #372)
4. 신규 이슈 **#38**(orphan-check skill 충돌 런타임 측정 · Q41 부수) · **#42**(fit 8-4 codex-cli MCP 문구 · #8 범위 밖) · 이 노트 이슈
5. `measured-facts.md` 5절 신설(Q41 · #32 I1 · Q10·Q19 · #37 감사 실측 · H-3(fit) 집행 실측) · `_workspace/1a-1-prep/`(surveyor 3 · writer · auditor) · `_workspace/harness/03_auditor_h3fit.md` · `04_operator_h3fit_rollback.md`

## PR 현황

| PR | 브랜치 | 내용 | 이슈 | 되돌리기 | 상태 |
|---|---|---|---|---|---|
| ~~#39~~ 머지 `533b921` | `chore/37-1` | 1a-1 준비 결정 7건 · 003 §4-2 재작성 | #37 `Closes` | 즉시 | 10절 완료. 봇 1회차 P2 2 반영 |
| ~~#41~~ 머지 `1f2f292` | `chore/40-1` | H-1 `pleiades-codex-loop` | #40 `Closes` | 즉시 | 10절 완료. 봇 3회차 |
| ~~myFitness#372~~ 머지 `2195854` | `integration/chore-pleiades-8-fit` | H-3(fit) 10파일 | #8 `Refs` (대칭 · 수동 종료함) | worktree 즉시 / **원본 중간**(git 이력 없음 · 폴백 `git archive 626a201`) | 10절 완료 + 원본 동기화 완료 |
| (이 노트) | `chore/43-1` | 인계 · CLAUDE.md · 005 정정 · bin 주석 | #43 `Closes` | 즉시 | 열림 |

## 결정된 것 (사용자 2026-09-09)

| | 결정 | 귀결 |
|---|---|---|
| **Q25** | ① `targets(route): string[]` 추가 | 파사드 필수 표면. 파싱 지점 5 → 0(ADMIN A 동반) |
| **Q25 하위 ADMIN** | **A. 실값 · fin 전용**(fit 미매핑 → `targetCount(ADMIN) === 0`) | B(fit 매핑)는 `Route.ADMIN` 의미가 저장소마다 갈려 기각 · C(자리만)는 루프 1 + catch 1 이 파사드 밖에 남아 기각. **1a-4 지시에 `targetCount(ADMIN) === 0` 가드 명시**(fin `advisor-monitor.ts:205-211` fail-safe 는 파사드가 복원 안 함) |
| **Q26 × Q19** | **Q26 ① `label` 만 + Q19 C 포트 raw**(`deliveries[].error?: string` 유지) | 결합 질문 — `onError`(Q26 ②) 없이는 Q19 B 불성립. B″(필드 객체화)는 별개 포트 변경으로 배제. `sensitiveLogs` 가드 별건: **(가) 금지 룰 또는 (나) 파사드 로그 1곳 `sanitizeMessage` + B′** — B′ 단독은 대안 아님(PR #39 Codex P2) |
| **Q10-L** | ① fit 절단 → 분할 통일(사용자 가시 변경 승인 · 1a-3) | Q14(실서비스 DB 읽기) **불필요**. 4096 초과 가능 호출은 fit 6 중 1(LLM 일일 리포트) |
| **Q10-P** | ① 태그-only · **코어 정규식 = fin `/<[^>]+>/g`**(PR #39 Codex P2 반영 · 오케스트레이터 판단) | fin 11 모듈 변화 0 · fit 은 리터럴 `<>` 입력 시에만(미측정). 003 §3-1 의 폴백 대비 구도는 오류였다(fin 두 파일 비교 · 디코드본은 죽은 export) |
| **Q41** | `pleiades-codex-loop` · 판정 기준 **S** · 기존 `orphan-check` 충돌은 **#38** 별건 | H-1 착수 → 완료(PR #41) |
| **#32 I1** | (b) `tsconfig.test.json` + `typecheck:test` · 선결 `@types/node ^20` 핀 · `noEmit` 파일 내 고정 | 도입은 1a-1 실테스트 유입 시. `workflow.md` 8절 타입 칸에 한 구 예고 |
| **H-3(fit) 범위** | 10파일(3-check 4종화 포함) + **머지 후 원본 동기화 승인** | 재감사 정정 4건 반영. 집행 완료 |

**정본 반영 형태(감사 판정 3안):** 003 §4-2 를 답 반영 시그니처로 **재작성**하고 옛 코드 블록 + 정정 블록 5개는 소진 헤더 아래 강등(삭제 0). 나머지 대장 27행 append(003 · 005 · measured-facts · `workflow.md` · CLAUDE.md).

## 결정되지 않은 것

| 이슈/질문 | 내용 | 언제 |
|---|---|---|
| **#38** | skill 이름 충돌 런타임 동작(이름 단위 vs 층 단위 · 공존 vs 드롭) — `orphan-check` 가 pleiades·fit 양쪽에 있다 | 별건. Q41 (i) 권고의 미측정 가정 |
| **#42** | fit `workflow.md` 8-4 codex-cli MCP "선택 대안" 문구 · 8-4 절 순서 | 별건 · 모드 I · 승인 게이트 |
| **#32 I3** | `packages/notify/README.md` | 1a-3 전 |
| **Q45** | 서버 https·node·git 측정 | 1a-3 착수 전(서버 접속 승인) |
| **Q27** | fin `rsu.ts:184` 파사드 흡수 | 1a-4 착수 전 (Q10-L·Q19 와 "동작 변경 0" 판정 공유 — 셋 중 Q10-L 이 ① 이라 이미 0 아님) |
| `sensitiveLogs` 가드 (가)/(나) | Q19 C 의 단서 | 1a-1 코어 구현 시 (나) 를 고르면 파사드 로그 1곳 |
| Q16·Q17·Q18 | 1a-2 (fit vitest) | 1a-2 착수 전 |
| 옛 척도로 분류된 fit 백로그(`docs/specs/backlog-code-review-issues.md` 등) | 머리말 주석 여부 | 후속 이슈 후보(미등재) |
| Q36 · Q38 · #17 · #11 · Q23 · Q12~Q14 · Q7·Q2·Q3 | 이전 노트 그대로 | 변동 없음 |

**단계 0 은 여전히 미착수, 단계 1 을 먼저 하기로 결정된 적도 없다.**

## 다음 세션의 첫 액션 후보

1. **1a-1 착수** — **선결 0**(하네스 #1 H-0~H-5 전부 머지 · Q25·Q26·Q19·Q10 확정). 003 §4-2 정본 시그니처로 `packages/notify` 코어·포트 구현 + #32 I1 (b) 게이트 도입 + 실테스트. 9-0 에이전트 필수(패키지 코드). 되돌리기 **즉시(1a-3 착수 전까지)**. 첫 단계로 `pleiades-orchestrator` 실측(현재 `packages/notify` 상태 · fin `sanitizeError`·`splitMessage` 원문 · fit `sendToAll` 원문) → 구현 계획 승인
2. **#38 측정** — `bin/claude-with fit` 세션 1회 런타임 관측. 읽기 전용 · 즉시
3. **#42** — fit 8-4 문구. 모드 I · 승인 게이트 · 문서 1파일 · 즉시
4. **아티팩트 재발행** — 003·005·1a-0·#37 미반영(2026-09-07 노트부터 이월). `docs/artifacts/integration-artifact.html` → `Artifact(url="https://claude.ai/code/artifact/88cd616a-efe0-4d84-b298-e6961675ae0d")`, 발행 전 `action:"read"`

## 주의사항

- **두 저장소는 실서비스 중.** 쓰기 전 사용자 확인. 이번 세션 쓰기: **myFitness 1건(H-3(fit) · 승인 2회)**, myFinance 0
- **세션 종료 관측:** worktree 2 = `integration/pleiades` clean(fin `6542152` · fit `2195854`) · 원본 fin `dev` / fit `main` clean · **fit 원본 `.claude/` 는 10파일이 worktree 와 동일**(`settings.local.json` 만 차이). **다음 세션은 이 노트보다 현재 상태를 신뢰하라**
- **fit 원본 `.claude/` 는 여전히 ignored 물리 파일이다**(원본 `main` 의 `.gitignore:35` = `.claude/` 전체). tracked 화(#369)는 `integration/pleiades` 에만 있다. **원본을 갱신하는 유일한 경로는 `git archive integration/pleiades <경로> | tar -x`** 이고 되돌리기 폴백은 `git archive 626a201 …`(사전 사본은 세션 스크래치패드라 사라졌다). `bin/claude-with fit` 이 읽는 것은 원본이다 — worktree 만 고치면 세션은 옛 룰을 읽는다(재감사 정정 3)
- **zsh 함정 재발**: `FILES="a b"; git archive ref $FILES` 는 단일 pathspec. 경로를 인자로 나열한다(measured-facts 방법론 주의)
- **`repos/myFitness` worktree 는 upstream 을 이번에 설정했다**(`integration/pleiades` → `origin/integration/pleiades`). **`repos/myFinance` 는 미확인** — `git pull --ff-only` 가 실패하면 `git branch --set-upstream-to=origin/integration/pleiades`
- **Codex 봇은 push 마다 자동으로 다시 돈다**(PR #41 4회차 · #372 4·5회차 — `@codex review` 없이). 9-3 의 "매 커밋마다 재실행되지 않는다"는 이 저장소들에서 **실측과 다르다** — 룰 정정 후보(9-3 문구). P2 만 반영한 커밋도 다음 자동 라운드가 리뷰하므로 "봇 미리뷰 커밋" 표기는 마지막 커밋에만 남는다
- **`pleiades-codex-loop` 스킬 사용 시** `gh api` 는 `-R` 없음(전체 경로) · `--jq` 에 `--arg`·`--slurp` 없음(jq 파이프) · PR body 갱신은 섹션 파일 + `$sec` 변수 치환 + `--body-file`(perl 치환부는 따옴표와 무관하게 보간 — `@codex` 소실)
- **9-4 반복 신호가 두 PR 에서 켜졌다** — 둘 다 "정본 룰을 고쳐도 **실행 스킬·템플릿이 안 따라오면** 결함이 살아 있다"(fin #492 2회차 P1 과 같은 유형). 다음에 룰을 고칠 때는 처음부터 **역할 짝 파일 전수**(grep 이 못 잡는 파일 포함 — `release-manager` 는 P 표기 0 이었다)
- `MEMORY.md` 18줄(한도 200)
- `CLAUDE.md` 의 *"fit `.claude/` 는 git 이력이 없다 (`.gitignore:35,36`)"* 는 **원본 기준으로만 참**(worktree 는 tracked) — 이 PR 에서 한정어를 붙였다

## 정정된 기록 — 옛 문서를 그 전제로 읽지 말 것

- 003 §3-1 *"어느 쪽을 고르든 한쪽 저장소의 폴백 출력이 바뀐다"* → **불성립**(두 구현 다 fin · 디코드본은 importer 0). 정본은 §3-1 2026-09-09 정정 블록
- 003 §4-2 맨 위 코드 블록 → **재작성됨**(`targets()` 필수 · `ctx.label` · raw error · ADMIN 실값). 그 아래는 소진 이력
- 003 §10 Q19 *"현행 유지 = 비밀 노출"* → 불성립(grammY message 에 payload·토큰 없음 · `sensitiveLogs` 기본 false). `sanitizeError` 는 문자열을 **확장**한다
- 003 §10-1 *"1a 착수를 막는 결정 8개"* → 남은 것 **Q16·Q17·Q18·Q27 4개**
- 005 §4-5 *"skill 층 충돌 0 · 첫 충돌 · 5번째"* → `orphan-check` 1건 기존재 · 6번째. §4-13 H-1 입력 200 → **204**(모드 I) · H-3(fit) **33 → 35줄**
- measured-facts H8 *"fit 26줄"* → **30** · C-7 *"A 와 B 는 결과가 같다"* → 정정
- `_workspace/1a-1-prep/02_writer_1a1prep.md` 1회차의 *"raw 는 상위집합"* · *"§4-3 제약 2 가 안쪽으로 못 박았다"* · *"(A) 정정 블록 append"* → 감사가 전부 정정(개정 이력 §0-1)
- 감사 2회차의 줄 번호 `003:721`·`:614`·`:606` → writer 값 `:719`·`:617`·`:610` 이 옳음(3회차 판정)

## 재현이 필요한 절차

- **아티팩트 재발행** — 위 후보 4
- **원본 fit `.claude/` 복원** — `git -C ~/workspace/myFitness archive integration/pleiades .claude CLAUDE.md | tar -x -C ~/workspace/myFitness`(#27 · 이번 세션 실행 검증됨 · 경로는 인자로 나열)
