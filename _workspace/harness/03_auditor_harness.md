# 03 — 하네스 통합 초안 감사 (Phase 3 · 1회차)

감사 2026-09-07 · 대상 `_workspace/harness/02_writer_harness.md` (653줄)
판정: **정정 10건 → 문서는 초안 상태를 유지한다** (`.claude/rules/workflow.md` 4절)

> **읽기 전용 세션.** 대상 저장소(`repos/*`, `~/workspace/myF*`)와 pleiades `.claude/`·`docs/` 에 **쓰기 0건**.
> 이 파일이 유일한 쓰기다. `git rm` 은 실행하지 않았다(초안의 dry-run 만 재현).
> 런타임 측정 4회는 `claude -p --permission-mode plan` (읽기 전용 · haiku).

---

## 0. 요약

| 구분 | 건수 |
|---|---:|
| **정정** | **10** |
| 확인 | 12 |
| 미확인 → 이 감사에서 측정으로 해소 | 2 |

**초안의 방향은 살아남고 산술이 무너졌다.** 처분 표(34+3행)의 LOC·파일 수는 **전수 대조에서 한 건도 틀리지 않았고**,
사용자 결정("공통은 pleiades, 특수는 그 저장소")을 파일에 적용한 결론도 유지된다.
무너진 것은 **그 결론을 뒷받침한다고 인용된 비율·건수**다 — 그중 정정 ①은 초안의 핵심 논거
*"11쌍 중 2쌍은 합칠 수 있다"* 를 **1쌍**으로 줄이고, 정정 ②는 H-1 작업량 추정의 단위를 무효화한다.

**초안이 스스로 꼽은 취약 주장 3개의 판정: 확인 1 · 정정 2.**

---

## 1. 지정 반증 대상 3건

### 주장 1 — §4-8 "이 PR 에는 `src/` 파급이 없다"

**판정: 확인** (단 §5 #3 의 출처 인용에 별건 정정 → 정정 ⑨)

```bash
git -C ~/workspace/pleiades/repos/myFinance grep --text -n "\.claude" integration/pleiades -- 'src/**' | cut -d: -f2-
#   src/app/api/alerts/history/export/route.ts:5: * 응답은 CSV 파일 (`csvResponse` — envelope 예외, `.claude/rules/api-routes.md` 참고).
sed -n '1,12p' ~/workspace/pleiades/repos/myFinance/src/app/api/alerts/history/export/route.ts
```

- `src/` 전체에서 `.claude` 참조는 **1건뿐**이고 그 1건이 `route.ts:5` 다.
- 그 줄은 파일 첫 줄에서 열린 **JSDoc 블록(`/** … */`, 1~7줄) 안**이다 — `import` 는 9줄부터. **런타임 코드가 아니다.**
- 부르는 대상은 `.claude/rules/api-routes.md` 이고 초안 §4-2 **5행의 처분은 "유지"** 다 → `git rm` 범위 밖.
- `git rm` 범위(`rules/workflow.md` + `skills/codex-response-patterns/`) dry-run 재현 → 2파일. `src/` 무관.

세 조건이 모두 성립하므로 주장은 참이다.

### 주장 2 — 병합 배율 1.60 (H-1 = 433 LOC)

**판정: 정정** → **정정 ②**. 파생 추적 결과는 아래.

**소요로 파생되지 않았다 (확인).** 초안 `02_writer_harness.md:494-497` 이
*"시간 환산은 적지 않는다 — 이 저장소에 실작업 시간 기록이 없다"* 로 **명시적으로 차단**했고,
`433` 은 문서 전체에서 §4-13 H-1 행과 산출식 두 곳에만 나온다. **되돌리기 표의 등급·행위,
부록 A(CLAUDE.md 동기화안) 어디에도 파생되지 않았다.** 표본 1 외삽이 소요 추정으로 번지는 것은
초안이 스스로 막았다 — 이 부분은 잘 되어 있다.

무너지는 것은 **433 이라는 숫자 자체**다. 상세는 정정 ②.

### 주장 3 — §4-5 "처분으로 이름 충돌 4건 전부 소멸"

**판정: 정정** → **정정 ⑧**. 좁은 의미(“pleiades `.claude/` 에 동명 파일이 새로 생기지 않는다”)로는 참이지만,
초안이 **"미측정"으로 남긴 우선순위를 이 감사에서 런타임으로 측정**했고, 그 결과가 §4-5 표를 무효화한다.

**측정 (런타임 4회, plan 모드 · haiku · 전부 읽기 전용):**

```bash
cd ~/workspace/pleiades && CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1 \
  claude -p '<질문>' --model claude-haiku-4-5-20251001 --permission-mode plan \
  --add-dir /Users/sagan/workspace/myFitness --add-dir /Users/sagan/workspace/myFinance < /dev/null
```

| 측정 | 결과 |
|---|---|
| rule `workflow.md` 동시 부착 | **3벌 전부 로드** — `pleiades/.claude/rules`, `myFitness/.claude/rules`, `myFinance/.claude/rules`. 드롭 없음 |
| rule `api-routes` / `components` | 각 **2벌 공존** |
| agent `release-manager` | **1개만 남는다 — 나머지는 조용히 사라진다** |

**agent 는 마지막 `--add-dir` 이 이긴다** (순서를 반대로 한 2회로 확정 · description 문자열이 frontmatter 와 verbatim 일치):

| `--add-dir` 순서 | 남은 `release-manager` description |
|---|---|
| `… myFitness … myFinance` | `"myFinance PR 생성 + Codex bot 리뷰 대응 …"` = **fin** `agents/release-manager.md:3` |
| `… myFinance … myFitness` | `"dev → main 릴리즈 PR 생성, 머지 알림 대기, …"` = **fit** `agents/release-manager.md:3` |

→ **충돌의 성격이 두 종류이고 초안은 한 종류로 뭉뚱그렸다.** rule 은 *드롭 없이 모순 규범이 동시 로드*되고(더 위험),
agent 만 *조용한 드롭*이다. §4-5 마지막 표의 `둘 다 붙이고 방치 | 4건 | … | 우선순위 미측정` 은 세 항목이 다 틀렸다.

---

## 2. 정정 10건

각 항목은 **모순되는 양쪽의 `파일:줄`** 과 정정값·근거 명령을 병기한다.

### 정정 ① [최중대] 드리프트 "공통줄 %" 는 사실상 **빈 줄 카운트**다 — "합칠 수 있는 2쌍" → **1쌍**

| 어디 | 무엇이라 적혀 있나 |
|---|---|
| `02_writer_harness.md:20` | *"10쌍 중 `rules/workflow.md`(공통 80.5%) 하나를 빼면 **전부 55% 미만**, 스킬 6쌍은 **14.6~34.5%**"* |
| `02_writer_harness.md:195` | Codex 쌍 **34.5%** → 처분 **이관(재작성 병합)** · §4-3 "합치기가 성립하는 쌍" 2개 중 하나 |
| `02_writer_harness.md:206` | *"'쌍둥이니 하나로 합친다'가 성립하는 쌍은 **11쌍 중 2쌍**"* |
| **출처의 경고** `01_surveyor_harness_content.md:196-197` | *"**빈 줄이 공통줄에 섞여 있으므로 위 공통줄은 상한이다.**"* ← **초안이 이 문장을 버렸다** |

서베이어의 지표는 `공통줄 = fin 총 줄수 − diff 의 fin 전용 줄수`(`01_surveyor_harness_content.md:175-178`)로,
**빈 줄과 위치 일치를 공통으로 센다.** 같은 쌍을 *비공백·정규화·중복제거* 집합으로 다시 재면:

```bash
comm -12 <(git -C repos/myFinance show integration/pleiades:<fin> | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | grep -v '^$' | sort -u) \
         <(sed 's/^[[:space:]]*//;s/[[:space:]]*$//' ~/workspace/myFitness/<fit> | grep -v '^$' | sort -u) | wc -l
```

| 쌍 | 초안 인용 % | diff 공통줄 | fin 빈 줄 | **실제 공유 비공백 유니크 줄** |
|---|---:|---:|---:|---:|
| rules `workflow` | 80.5% | 194 | 59 | **118** (fin 유니크 161 의 73.2%) |
| skill Codex | **34.5%** | 30 | 28 | **1** ← `---` (frontmatter 구분자) |
| skill 세션재개 | 33.7% | 33 | 29 | **3** |
| skill 릴리즈 | 33.6% | 45 | 29 | **6** |
| skill 인계 | 28.6% | 38 | 35 | **3** |
| skill 브랜치 | 18.7% | 23 | 14 | **2** |
| agent `release-manager` | 18.4% | 19 | 15 | **4** |
| skill 오케스트레이터 | 14.6% | 18 | 14 | **2** |
| rules `components` | 54.5% | 6 | 2 | **4** (유니크 9 중) |
| rules `api-routes` | 3.4% | 4 | 31 | **2** |

**`rules/workflow` 만 실질 중복이 있다 (118줄). 나머지 9쌍은 전부 ≤ 6줄이다.**
특히 **Codex 쌍이 공유하는 비공백 줄은 정확히 1줄(`---`)** — 내용상 공통이 **0** 이다.

**정정값:**
- `:20` "스킬 6쌍은 14.6~34.5%" → **실제 1.7~6.8%** (한 자릿수 배 과대)
- `:206` "11쌍 중 2쌍" → **1쌍** (`rules/workflow` 뿐)
- `:195` Codex 쌍 처분 근거 "공통 34.5%" → **성립하지 않는다.** 이것은 *병합*이 아니라
  §4-3 이 나머지 9쌍에 적용한 **"이름만 대응하는 재작성"과 동일 범주**다

**영향:** 초안의 결론(*"합치기는 병합이 아니라 재작성이다"*)은 **뒤집히지 않고 오히려 강화된다.**
그러나 §4-3 표 전체가 **잘못된 축으로 정렬돼 있고**, 정정 ②의 전제이기도 하다.

### 정정 ② 병합 배율 1.60 은 **단위 불일치** — 유니크 줄 비율을 raw LOC 에 곱했다

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:473` | `= (87 + 113) × 1.60   [입력 200 LOC × 병합 배율]` |
| `02_writer_harness.md:477-478` | *"근거: pleiades workflow.md 선례 — 원본 입력 **215 유니크 줄** → 산출 **344 유니크 줄** = 1.60"* |
| `01_surveyor_harness_content.md:216-222` | 215·344 의 정의 = **"빈 줄 제외 + 앞뒤 공백 정규화 + 중복 제거"** 집합 |

`87`·`113` 은 §4-2 표의 **raw LOC 열**(`wc -l`)이다. 배율은 **유니크 비공백 줄 공간**에서 유도됐다.
**두 공간이 다르므로 곱할 수 없다.**

```bash
wc -l ~/workspace/pleiades/.claude/rules/workflow.md        # 486  (raw)
git -C repos/myFinance show integration/pleiades:.claude/rules/workflow.md | wc -l   # 241
wc -l ~/workspace/myFitness/.claude/rules/workflow.md       # 264
```

**같은 선례를 raw LOC 로 재면 486 / (241+264) = 0.96** — 1.60 이 아니다.
0.96 을 쓰면 H-1 은 `200 × 0.96 + 113 =` **≈ 305 LOC**, 초안의 433 은 **약 42% 과대**다.

**그러나 배율을 갈아끼우는 것으로는 부족하다.** 정정 ①이 보인 대로 Codex 쌍은
**공유 내용줄이 0** 이므로 *"두 벌을 합쳐 중복을 걷어낸다"* 는 선례(공유 118줄)와 **성질이 다르다.**
표본 1 외삽 위에 단위 오류가 겹쳤고, 그 표본조차 대상과 같은 종류가 아니다.

**정정값:** `433 LOC` → **산출 불가.** `H-1 작업 표면 = 입력 313 LOC (3파일) · 산출 미확정`으로 적고,
초안 `:494-497` 이 시간에 대해 한 것과 **같은 처리**(측정 없음을 명시)를 LOC 에도 적용한다.

### 정정 ③ §4-4 형태 C 의 **"참조 재작성 0줄"** 이 틀렸다 — 절 번호가 함께 깨진다

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:221-225` | *"폐기 대상 `rules/workflow.md` 를 가리키는 참조가 유지 파일 안에 **13줄** … (fin 8줄 … / fit **5줄**) … C 는 경로가 그대로라 **0줄**이다"* |

```bash
git -C repos/myFinance grep --text -n "workflow\.md" integration/pleiades -- '.claude' 'CLAUDE.md' \
  | cut -d: -f2- | grep -v '^\.claude/rules/workflow\.md'          # → fin 8줄
cd ~/workspace/myFitness && grep -rn --binary-files=text "workflow\.md" .claude CLAUDE.md \
  | grep -v '^\.claude/rules/workflow\.md'                          # → fit 11줄
```

**(a) fit 은 5줄이 아니라 11줄이다.** 초안은 전체 경로(`.claude/rules/workflow.md`)만 셌고
**맨이름 참조 6줄을 놓쳤다**: `codex-liaison.md:23` · `workflow-conductor.md:3,20` ·
`branch-workflow.md:3,83` · `myfitness-orchestrator.md:103`. → 합계 **13줄 → 19줄**.

**(b) 더 중요한 것 — C 에서도 최소 6줄은 반드시 고쳐야 한다.** 놓친 줄들이 **fin·fit 의 절 번호**를 인용하는데
pleiades 는 그 번호를 재배치했다 (`01_surveyor_harness_content.md:237-238`: fin·fit `8. 코드 리뷰` → pleiades `9. 코드 리뷰 + PR`):

| 줄 | 인용 | pleiades 에서 그 번호는 |
|---|---|---|
| fin `quality-guardian.md:86` | `` `.claude/rules/workflow.md` §8 `` | §8 = **검증** (리뷰는 §9) |
| fin `project-verify/SKILL.md:98` | `` `.claude/rules/workflow.md` §7~8 `` | 한 절씩 밀렸다 |
| fin `spec-planner.md:19` | 10단계 열거에 **`UI 디자인`** 포함 | pleiades 가 **삭제**하고 `되돌리기 비용 산정`으로 교체 |
| fit `codex-liaison.md:23` | `workflow.md 8절` | 동 |
| fit `workflow-conductor.md:20` | `workflow.md 8-6` | pleiades 는 **9-6** |
| fit `branch-workflow/SKILL.md:83` | `workflow.md 8-1` | pleiades 는 **9-1** |

**정정값:** `C = 0줄` → **C = 최소 6줄** (절 번호·단계 목록 재작성). `A = 13줄` → **A = 19줄**.
C 를 A 보다 권고하는 근거는 약해지되 **뒤집히지는 않는다**(19 vs 6). 다만 **"0줄"은 삭제해야 한다** —
스텁이 경로를 살려도 **가리키는 문서의 절 번호가 달라** 조용한 오참조가 남는다.

### 정정 ④ auto memory **11파일(fin 6 · fit 3 · ple 2)** → **13파일(fin 9 · fit 2 · ple 2)**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:73` · `:432` | *"하네스 이름·경로를 부르는 memory 는 **11파일**(fin 6 · fit 3 · ple 2)"* |
| `01_surveyor_harness_refs.md` §3 요약줄 | *"fin 6파일 · fit 3파일 · ple 2파일 = 11파일"* |
| `01_surveyor_harness_refs.md` §3 **본문 열거** | fin 쪽에 **9개 파일**을 실제로 나열한다 (오탐 1 별도) |

```bash
cd ~/.claude/projects
grep -rlE --binary-files=text '\.claude/(rules|agents|skills)' -- ./-Users-sagan-workspace-myFinance/memory
grep -rlE --binary-files=text '<fin agent+skill 이름 전부>'   -- ./-Users-sagan-workspace-myFinance/memory
#  합집합 → 10파일. project_ai_session_resume.md 는 서베이어가 표시한 오탐 → 실질 9
```

**fin 9파일**: `MEMORY.md` · `feedback_session_management.md` · `feedback_always_follow_workflow.md` ·
`project_briefing_no_tool_detection.md` · `project_milestone9_complete.md` · `project_milestone13_complete.md` ·
`project_next_milestone_17.md` · `project_next_milestone_18.md` · `project_next_milestone_19.md` (각 참조줄 실물 확인)
**fit 2파일** (`feedback_release_merge_commit.md` · `feedback_review_policy.md`) — 서베이어 표는 3, **자기 열거는 2**
**ple 2파일** ✓

**서베이어 요약이 자기 열거와 모순**하고 초안이 요약 쪽을 인용했다. 정정 ⑤⑥⑦과 같은 **#11 유형**이다.
다만 **§4-12 의 조치 결론(사용자 세션 작업 4줄)은 유지된다** — 추가된 3파일은 전부 유지 대상(`components.md` ·
`api-routes.md` · `milestone-workflow`)을 가리키거나 이력성 기록이다. 틀린 것은 모수뿐이다.

### 정정 ⑤ H-1 참조 재작성 **13줄 → 14줄** (`CLAUDE.md:117` 누락)

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:484-485` | *"orphan-check inbound **5** (`codex-liaison:31` · `workflow-conductor:34` · `branch-workflow:175` · `myfitness-orchestrator:32,41`)"* |
| `02_writer_harness.md:89-91` (§3-3) | 같은 목록으로 *"9줄로 fit 클러스터에 묶여 있다"* |

```bash
cd ~/workspace/myFitness && grep -rn --binary-files=text "orphan-check" .claude CLAUDE.md
#   … CLAUDE.md:117:- "머지완료" / "릴리즈해줘" → `release-flow` (+ `orphan-check`)   ← 초안 목록에 없다
```

`CLAUDE.md:117` 은 **유지 대상 파일 안의 inbound 참조**라 이관 시 반드시 고쳐야 한다
(초안이 `codex-review-loop` 쪽에서는 `CLAUDE.md:115` 를 제대로 셌으므로 **비대칭 누락**이다).

**정정값:** §4-13 `H-1 참조 재작성 = 13줄` → **14줄** (fit 11 → 12) · §3-3 `orphan-check 9줄` → **10줄**.

### 정정 ⑥ fin `git rm` **활성 파급 5건 → 6건**, 그리고 초안 내부 모순

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:71` (§3-1 핵심 수치) | *"fin `git rm` 활성 파급 **5건** (`CLAUDE.md` **4줄** + `route.ts:5`)"* |
| `02_writer_harness.md:168` (§4-2 36행) | *"갱신 대상은 `:128`(workflow 포인터) · **`:145`**(rules 목록)"* |
| `01_surveyor_harness_refs.md` 가정 3 | *"`CLAUDE.md` **62·118·128·140** + `route.ts:5`"* — **`:145` 가 없다** |

```bash
git -C repos/myFinance show integration/pleiades:CLAUDE.md \
  | grep -n --binary-files=text -- "\.claude\|workflow\.md"
#   62, 118, 128, 140, 145   ← 5줄
```

`:145` = `` - `workflow.md` — 10단계 워크플로우 전문, 브랜치/릴리즈/코드리뷰 절차 `` 는 명백한 활성 참조다.
**실제 = CLAUDE.md 5줄 + route.ts:5 = 6건.**

초안은 `:145` 를 §4-2·§4-8·§4-13 에서 올바르게 쓰면서 **§3-1 표에는 서베이어의 틀린 "4줄/5건"을 그대로 실어**
같은 문서 안에서 두 값이 충돌한다.

### 정정 ⑦ H-0 범위 과소 — grep 문구 **4곳 → 6곳**, 체크아웃 게이트 **6줄 → 8줄**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:409-416` | *"정정 대상 **4곳**(전부 pleiades)"* — `CLAUDE.md:106` · `rules/workflow.md:91` · `agents/repo-surveyor.md:25` · `skills/repo-measure/SKILL.md:23` |
| `02_writer_harness.md:389` | 체크아웃 게이트 = `repo-measure:22,92,94` · `repo-surveyor:24,70,72` = **6줄** |
| `01_surveyor_harness_refs.md` §6 측정 명령 | `grep … .claude/skills/repo-measure/SKILL.md .claude/agents/repo-surveyor.md` ← **2파일로 범위를 한정해서 쟀다** |

```bash
cd ~/workspace/pleiades
grep -rn --binary-files=text -- "binary-files=text" CLAUDE.md .claude/
grep -rn --binary-files=text -- "체크아웃" .claude/
```

**동일 보일러플레이트가 2파일 더 있다:**

| 누락된 곳 | 내용 |
|---|---|
| `.claude/agents/reversibility-auditor.md:20` | `> **grep 에는 반드시 --binary-files=text …**` (문자 동일) |
| `.claude/skills/reversibility-audit/SKILL.md:18` | 동일 |
| `.claude/agents/reversibility-auditor.md:47` | *"…감사 전에 그 ref 를 체크아웃했는지 확인하고, 확인하지 못했으면 '미확인'으로 보고한다"* |
| `.claude/skills/reversibility-audit/SKILL.md:45` | 동일 |

**정정값:** §4-13 `H-0 = 12줄 + 4곳` → **14줄 + 6곳**. 부록 A-4 도 `CLAUDE.md:106` 만 교체안을 냈다.
4곳만 고치면 **측정 하네스는 새 규율을, 감사 하네스는 옛 규율을 갖게 된다** — 초안이 §4-10 ①에서
*"이 저장소가 이미 대가를 치렀다"* 고 지적한 바로 그 결함이 감사 쪽에 남는다.
(`/tmp` 6줄은 **전수 정확** — 하네스 전체에 `/tmp` 는 그 6줄뿐이다. 확인.)

### 정정 ⑧ §4-5 이름 충돌 — "우선순위 미측정" 해소 · 충돌의 **종류가 둘**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:243` | *"**→ 파일 이관 자체로는 충돌 4건이 전부 소멸한다.**"* |
| `02_writer_harness.md:248` | *"어느 쪽이 이기는지는 [참조] '못 잰 것' 에 **미측정**으로 남아 있다"* |
| `02_writer_harness.md:255` | `둘 다 붙이고 방치 \| **4건** \| 즉시 \| 플래그 제거 \| 조용한 오참조 — **우선순위 미측정**` |

런타임 측정(§1 주장 3)으로 **셋 다 정정된다:**

1. **미측정 → 측정됨.** rule 은 **전부 공존**(workflow 3벌·api-routes 2벌·components 2벌 동시 로드),
   agent 는 **마지막 `--add-dir` 이 이기고 나머지는 조용히 사라진다**(순서 반전 2회로 확정).
2. **"4건"이 한 종류가 아니다.** 실제로 무언가 *사라지는* 것은 **agent 1건**뿐이다.
   rule 3건은 사라지지 않고 **모순되는 규범이 동시에 로드된다** — 초안이 §4-6 에서 별도로 걱정한
   *"통합 작업 중 규범이 둘이 된다"* 와 **같은 사건**인데 §4-5 는 이를 "충돌"로만 적었다.
3. **`:243` 의 "전부 소멸"은 유지하되 범위를 한정해야 한다.** 소멸하는 것은 *pleiades `.claude/` 네임스페이스*
   이고, 그 상태는 초안 §4-6 이 **유지 29파일이 보이지 않는 상태**라고 규정한 바로 그 구성이다.
   29파일을 보이게 하는 순간(= `--add-dir`) 4건이 되살아난다. **소멸은 처분의 성과가 아니라
   부착하지 않았다는 사실의 재기술이다.**

**정정값:** `:255` 행 → `agent 1건 = 조용한 드롭(마지막 --add-dir 우선) · rule 3건 = 드롭 없이 모순 규범 동시 로드`.
`:248` 의 "미측정" 삭제. §3-1 표 `:74` 및 [참조] "못 잰 것" 에서 해당 항목 제거.

### 정정 ⑨ §5 #3 — 출처를 잘못 인용해 "판단이 바뀐 것"을 부풀렸다

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:507` | 이전 서술: *"`git rm` 활성 파급 5건 · 그중 1건이 `src/` **실행 파일**"* → 지금: *"…에이전트 리뷰 필수는 유지되나 **근거가 9-0 표의 '대상 저장소 변경'으로 바뀐다**"* |
| `01_surveyor_harness_refs.md` 가정 4 (원문) | *"그 5건 중 1건이 `src/` 실행 파일 **주석**이다. `.claude/rules/workflow.md` **9-0 표에서 '대상 저장소 변경 — 경로 무관'은 무조건 에이전트 사전 리뷰 필수**이므로…"* |

**서베이어는 이미 (a) '주석'이라고 적었고 (b) 리뷰 필수의 근거로 이미 9-0 '경로 무관'을 들었다.**
초안은 인용에서 "주석"을 빼고, 이미 9-0 이었던 근거를 *"근거가 바뀐다"* 로 기술했다. **바뀐 것이 없다.**

**정정값:** §5 3행과 §4-8 의 *"[참조] … 결론은 유지되고 근거만 바뀐다"* → **"결론도 근거도 그대로이고,
초안이 추가한 것은 `git rm` **범위를 2파일로 좁히면** 그 1건이 범위 밖이라는 사실뿐"**.
(주장 1 자체는 **확인**이다. 틀린 것은 공적의 크기다.)

### 정정 ⑩ 표 헤더·비율 라벨 2건 (경미)

| 어디 | 무엇 | 정정값 |
|---|---|---|
| `02_writer_harness.md:141` | 헤더가 `#### myFitness (19행)` — 실제 표 행은 **18** (표 블록에서 `^\| [0-9]` 행을 센 값) | **18행**. (총 37행 = 17+18+2 는 맞다) |
| `02_writer_harness.md:183` | *"**3파일(8.9%)** … **29파일(76.7%)** 이 저장소에 남는다"* — 8.9%·76.7% 는 **LOC 비율**인데 파일 수에 붙였다 | 파일 기준은 **8.8% / 85.3%**. §2-1(`:42-43`)은 LOC 를 병기해 옳다 |

---

## 3. 확인 12건

| # | 주장 | 근거 |
|---|---|---|
| 1 | **처분 표 34+3행의 LOC 전수** | `git ls-tree` + `wc -l` 로 36파일 개별 대조 — **한 건도 틀리지 않았다** |
| 2 | 파일·LOC 합계 | 이관 3/313 · 폐기 2/505 · 유지 29/2,696 · 합 34/**3,514** ✓ · fin 1,629 ✓ · fit 1,885 ✓ · 파일 검산 16/18 ✓ |
| 3 | LOC 비율 8.9% · 14.4% · 76.7% | 313·505·2,696 ÷ 3,514 재계산 일치 |
| 4 | **§3-2 정정 ① (#8 = 52줄)** | 재검산 완전 재현: fin **22** · fit **30**. 부록 B-4 의 줄 번호 열거 **52개 전부 일치** |
| 5 | #8 릴리즈 결함 줄 | fin **34,35,36** · fit **35,36,37** = `git merge dev` / `git tag` / `git push origin main --tags` ✓ → 58줄 ✓ (fin 25 · fit 33) |
| 6 | **fit 하네스 = 편도** | `git -C ~/workspace/myFitness log --all -- .claude CLAUDE.md` → **출력 0** · `ls-files` → **0** · `.gitignore:35,36` = `.claude/` · `CLAUDE.md` ✓ |
| 7 | fit `.claude/` 는 worktree 에 없다 | `ls repos/myFitness` → `.claude` 없음 (§4-6 의 *"`--add-dir` 대상은 항상 원본"* 성립) |
| 8 | 결합/도메인 열 전수 | §4-2 의 36행 값이 `01_surveyor_harness_content.md` §3 3등급 표와 전부 일치 |
| 9 | 파생 비율 | 70.5%(182/258) · 80.3%(61/76) · 8.3%(3/36) · 90.7%(136/150) · 85.7%(90/105) · 67줄(31+16+20) 전부 재현 |
| 10 | **Q29~Q38 번호 충돌 없음** | 001 Q1-4 · 002 Q1-7 · 003 **≤Q28** · 004 Q20-24 → 기존 최대 **Q28** ✓ |
| 11 | 렌더링 (T7) | 표 행 열 수 불일치 **0건** (`awk -F'\|'` 블록별 검사). 행 안 줄바꿈 없음 |
| 12 | 부록 B 명령 재현 | B-1 dry-run 2파일 ✓ · B-2 프로세스 치환 ✓ · B-3 grep 래퍼 0/3/3/3 ✓ · B-4 ✓ · B-5 3저장소 PUBLIC ✓ |

**#10 `/tmp` 6줄**(`repo-measure:117-119` · `repo-surveyor:84-86`)은 하네스 전체 전수 검색에서 **그 6줄이 전부**였다 — 열거 정확. **확인.**

---

## 4. 미확인 → 이 감사에서 해소된 것 2건

| 항목 | 초안 상태 | 측정 결과 |
|---|---|---|
| **Q37 전반부** — `--add-dir` 만으로 rules 가 로드되나 (환경변수 없이) | `:287-288` *"미측정"* | **로드되지 않는다.** 환경변수 없이 `--add-dir ~/workspace/myFitness` → `workflow_md_dirs` = **pleiades 하나뿐**. 환경변수를 켜면 2개. → **§4-6 ③④ 래퍼가 환경변수를 항상 켜야 한다는 초안의 처방은 옳다**(근거가 이제 측정이다) |
| **이름 충돌 우선순위** | `:248` *"미측정"* | 정정 ⑧ 참조 |

**여전히 미확인 (측정 못 함):**
- **§4-1 축 3 의 대칭 가정** — *대상 저장소 cwd 세션이 pleiades 하네스를 보는가*(`:111`). 대상 저장소를
  cwd 로 세션을 여는 것은 이 감사의 읽기 전용 범위를 벗어나지 않으나(plan 모드), **그 저장소의 하네스가
  세션 훅을 갖고 있어** 부작용 여부를 사전 판정할 수 없었다. Q37 후반부로 남긴다.
- **Q33 fit 민감 문자열 스캔** — 초안 B-6 과 동일 사유. 미측정 유지.

---

## 5. 그 밖에 보고할 사항 (정정 아님 · 판단 필요)

**(1) H-2 의 모드 표기가 7절 표로 집행 불가능하다.**
`02_writer_harness.md:457` 은 H-2 를 *"fin `git rm` 2파일 + 스텁 **2** + `CLAUDE.md` **4줄** (**모드 I** · 승인 게이트)"*
로 적고, 같은 행이 갱신 대상에 **`fit CLAUDE.md:92,108`** 과 fit 스텁을 포함한다.
그런데 `.claude/rules/workflow.md` 7절 표의 **모드 I = `repos/*` worktree · base `integration/pleiades`** 이고,
**fit `.claude/` 는 worktree 에 없으며 fit `CLAUDE.md` 는 gitignored** (확인 6·7).
→ H-2 의 fit 절반은 **모드 I 로 집행할 수 없고, PR 도 revert 도 존재하지 않는다.**
되돌리는 행위 열(*"머지 전: 브랜치 폐기. 머지 후: revert 커밋"*)도 fin 절반에만 적용된다.
초안은 **§4-9(#8)에서는 이 비대칭을 정확히 짚었으면서**(`:361-363` *"fit 은 PR 로 고칠 수 없다"*)
**§4-13 H-2 에서는 같은 비대칭을 반영하지 않았다.** H-2 를 fin/fit 두 행으로 분리하고
fit 행의 되돌리기를 **Q32 미결 시 편도**로 적을 것을 권한다(H-3 이 이미 그렇게 하고 있다).

**(2) `repos/myFitness/CLAUDE.md` 에 stale 사본이 있다.**
worktree 에 `CLAUDE.md`(8,621 B · 2026-09-03) 가 존재하는데 fit `.gitignore:36` 이 무시하는 파일이라
**tracked 가 아니다** — worktree 생성(09-04) 이전 날짜의 잔여 파일이다.
초안 §4-6 의 *"`--add-dir` 대상은 항상 원본"* 결론은 유지되나, **worktree 를 cwd 로 열면 이 stale 사본이 로드된다.**
Q30 부착 정책이 다룰 항목으로 추가를 권한다.

**(3) "쌍" 개수가 절마다 다르다.** `:20`·`:67` 은 **10쌍**, `:206` 은 **11쌍**.
차이는 §4-3 표의 `orphan 커밋 (짝 없음)` 행이다 — 짝이 없으므로 쌍이 아니다. `11쌍` → `10쌍 + 짝 없음 1`.

**(4) "3" 이 두 뜻으로 쓰인다.** `:66` 의 *"무조건 공통화(참조 0건) **3** / 36 파일 · 362 LOC"* 와
§2-1 의 *"pleiades 로 올라오는 것은 **3**파일 · 313 LOC"* 는 **다른 집합**이다
(전자 = fin `workflow.md` · fit `api-routes.md` · fit `orphan-check`, 교집합은 `orphan-check` 하나).
읽는 사람이 같은 3으로 오해하기 쉽다 — 한쪽에 각주를 권한다.

**(5) 되돌리기 3요소 중 "소요"가 전 표에서 비어 있다.** `.claude/rules/workflow.md` 4절은
*"등급 + 되돌리는 행위 + **소요**를 병기한다. 예외 없다"* 를 요구한다. 초안은 등급·행위는 전부 채웠고
(H-0~H-5 · ③선택지 4개 · ⑤(a)(b)(c) · §4-4 A/B/C · §4-5 3안 · §4-7 i~iv — **전수 확인**),
소요는 `:494-497` 에서 **사유를 명시하고 일괄 생략**했다. 규율 위반이지만 **근거 있는 이탈**이고,
등급 척도(즉시/중간/높음/편도) 자체가 시간 축이므로 **정정으로 세지 않는다.** 다만 4절 문구와의
충돌은 남으므로 — 4절에 *"실작업 시간 기록이 없는 단계는 등급으로 갈음하고 그 사실을 적는다"* 를
추가하는 것을 권한다(H-0 범위에 넣을 수 있다).

---

## 6. 결론

### (a) 정정 건수 합계 — **10건**

| # | 정정 | 무게 |
|---|---|---|
| ① | 드리프트 % = 빈 줄 카운트 → 합칠 수 있는 쌍 2 → **1** | **최중대** |
| ② | 병합 배율 1.60 단위 불일치 → H-1 433 LOC 산출 불가 | **중대** |
| ③ | 형태 C "참조 0줄" → 최소 6줄 · fit 참조 5 → 11 (절 번호 깨짐) | **중대** |
| ④ | auto memory 11파일 → 13파일 (fin 6→9 · fit 3→2) | 중간 |
| ⑤ | H-1 참조 13줄 → 14줄 (`CLAUDE.md:117`) | 중간 |
| ⑥ | fin `git rm` 활성 파급 5건 → 6건 (`CLAUDE.md:145`) · 문서 내부 모순 | 중간 |
| ⑦ | H-0 범위 12줄+4곳 → 14줄+6곳 (감사 하네스 2파일 누락) | 중간 |
| ⑧ | 이름 충돌 "미측정" 해소 · 충돌 종류가 둘 | 중간 |
| ⑨ | §5 #3 출처 오인용 ("주석" 누락 · 근거가 바뀌지 않았다) | 경미 |
| ⑩ | 표 헤더 19행→18행 · LOC 비율을 파일 수에 라벨 | 경미 |

**→ 문서는 초안이다.** `.claude/rules/workflow.md` 4절 *"정정이 하나라도 나오면 문서는 다시 초안"*.
같은 4절의 *"3회를 넘으면 스코프를 줄인다"* 는 **회차** 기준이므로 아직 해당하지 않는다(1회차).

**다만 ①②는 부분 수정으로 덮이지 않는다.** §4-3 표 전체와 §4-13 산출식이 같은 무효 축 위에 있으므로
**해당 두 절은 개정본 발행을 권한다** (§4-2 처분 표는 무결하므로 문서 전체 재작성은 불필요).

**#11 유형(집계 오류)이 이번 라운드에서 4건 더 나왔다** (④⑤⑥⑦). 초안이 스스로 1건(#8 43→52)을
잡아낸 것과 합치면 **같은 유형이 3라운드 연속**이다. `.claude/rules/workflow.md` 2절에
**"열거와 합계가 함께 있으면 합계를 신뢰하지 말고 열거를 센다"** 를 명문화할 것을 오케스트레이터에게 건의한다
— 서베이어 요약줄이 자기 열거와 어긋난 사례가 이번에만 3건(④의 fin·fit, ⑥)이다.

### (b) 다음 라운드에서 나올 가능성이 가장 높은 3건

| 순위 | 예상 정정 | 왜 |
|---|---|---|
| **1** | **§4-3 "기준(base)" 열 전체** — 정정 ①로 축이 무효화됐으므로, `Codex 대응`의 기준을 *"fit 113줄"* 로 둔 근거(*"더 길고 루프 절차가 대응된다"*)와 `양쪽 유지` 7쌍의 판정 근거를 **공통줄이 아닌 다른 축으로 다시 세워야 한다.** 재작성 과정에서 처분이 바뀌는 행이 나올 가능성이 높다 — 특히 **공유 1줄짜리 Codex 쌍을 굳이 "이관(병합)" 으로 묶을 이유가 남는지** | ①이 판정의 유일한 정량 근거였다 |
| **2** | **Q31 선행 측정(버린 181줄 분류)이 §4-2 의 9·25행을 뒤집는다** — 초안 `:524` 이 이미 *"pleiades 7절이 단독 작업에 대해 '그 저장소 자신의 workflow.md 를 따르라'고 **명시적으로 위임**한다 — 폐기하면 위임 대상이 사라진다"* 고 적었다. 정정 ③이 **그 위임 대상의 절 번호마저 어긋난다**는 것을 보였으므로, 폐기(505 LOC · 전체의 14.4%)가 **유지 + 부분 갱신**으로 바뀔 개연성이 커졌다. 그러면 H-2 가 거의 사라진다 | 초안 §7 이 스스로 유효기간 조건으로 등재한 항목 + ③의 새 증거 |
| **3** | **fit 참조·`CLAUDE.md` 계열의 추가 누락** — 정정 ③⑤가 **둘 다 fit 쪽에서, 둘 다 `CLAUDE.md` 또는 맨이름 참조에서** 나왔다. 초안의 fit 측 열거는 `.claude/rules/...` 전체 경로 패턴에 의존하는 자리가 더 있고(§4-12 · §4-4 · 부록 A-1 의 fit 파일 수 16), **fit 은 `git grep` 이 원리상 0건이라 교차 검증 수단이 하나뿐**이다 | 같은 원인(맨이름 참조 · gitignored 트리)이 이미 2회 발현 |

---

## 7. `decision-writer` 에게 — 고쳐야 할 문장

| 초안 위치 | 이렇게 고친다 |
|---|---|
| `:20` | *"스킬 6쌍은 14.6~34.5%"* → *"스킬 6쌍은 **공유 비공백 유니크 줄 1.7~6.8%**(서베이어의 `공통줄` 지표는 빈 줄을 포함하는 상한이다 — `01_surveyor_harness_content.md:196`)"* |
| `:195` | Codex 쌍 근거 *"공통 34.5%"* 삭제 → *"공유 내용줄 **1줄**(`---`). 병합이 아니라 **두 문서를 입력으로 한 신규 작성**"* |
| `:206` | *"11쌍 중 2쌍"* → *"**10쌍 중 1쌍**(`rules/workflow` 뿐) + 짝 없음 1" |
| `:471-478` | 산출식에서 `× 1.60` 삭제 → *"입력 313 LOC / 3파일. **산출 LOC 는 측정 근거가 없어 적지 않는다**(시간과 동일 처리)"* |
| `:221-225` | *"fit 5줄"* → *"fit **11줄**"* · *"C 는 … **0줄**"* → *"C 도 **최소 6줄**(절 번호 `8절`·`8-1`·`8-6`·`§8`·`§7~8`·10단계 목록이 pleiades 재배치와 어긋난다)"* |
| `:71` | *"활성 파급 5건 (`CLAUDE.md` 4줄 + …)"* → *"**6건** (`CLAUDE.md` **5줄** 62·118·128·140·**145** + `route.ts:5`)"* |
| `:73` · `:432` | *"11파일(fin 6 · fit 3 · ple 2)"* → *"**13파일**(fin **9** · fit **2** · ple 2)"* |
| `:389` · `:409-416` · `:455` | `12줄 + 4곳` → **`14줄 + 6곳`**. 누락 4줄 추가: `agents/reversibility-auditor.md:20,47` · `skills/reversibility-audit/SKILL.md:18,45` |
| `:484-485` · `:89-91` · `:480` | orphan-check inbound `5` → **6** (`CLAUDE.md:117`) · `9줄` → **10줄** · `13줄` → **14줄** |
| `:243` · `:248` · `:255` | "미측정" 삭제. `:255` 행 → *"agent **1건** = 조용한 드롭(**마지막 `--add-dir` 우선**) · rule **3건** = 드롭 없이 **모순 규범 동시 로드**"*. `:243` 은 *"pleiades `.claude/` 네임스페이스 한정"* 을 명시 |
| `:507` · `:328-336` | *"근거가 바뀐다"* → *"결론도 근거도 그대로다(서베이어가 이미 9-0 '경로 무관'을 들었고 '주석'이라 적었다). 새로 밝힌 것은 **범위를 2파일로 좁히면 그 1건이 범위 밖**이라는 사실"* |
| `:141` | `(19행)` → `(18행)` |
| `:183` | *"3파일(8.9%) … 29파일(76.7%)"* → *"3파일(파일 8.8% · **LOC 8.9%**) … 29파일(파일 **85.3%** · LOC 76.7%)"* |
| `:457` | H-2 를 **fin 행 / fit 행으로 분리**. fit 행: 모드 I 불가(worktree 에 `.claude/` 없음 · `CLAUDE.md` gitignored) · 되돌리기 **Q32 미결 시 편도** |
| §3-1 표 `:74` | `--add-dir` 대조 행에 **환경변수 없으면 rules 도 로드되지 않는다**(이 감사 런타임 1회) 추가 |

**`repo-surveyor` 에게 요청 (재측정 아님 · 합계 정정):**
① `01_surveyor_harness_content.md` §2 드리프트 표에 **공유 비공백 유니크 줄 열 추가** (빈 줄 상한 경고를 표 안으로)
② `01_surveyor_harness_refs.md` §3 요약줄 `fin 6 · fit 3` → **`fin 9 · fit 2`** (자기 열거와 일치시킨다)
③ 동 §6 표 합계 `17줄`·`26줄` → **`22줄`·`30줄`** (초안 §3-2 가 이미 요청한 건)
④ 동 가정 3 의 `CLAUDE.md 62·118·128·140` → **`62·118·128·140·145`**, `5건` → **`6건`**

---
---

# 2회차 감사 (2026-09-07)

대상 `_workspace/harness/02_writer_harness.md` **692줄** (커밋 `f7bec29`)
판정: **정정 8건 → 문서는 초안을 유지한다**

> **읽기 전용.** 대상 저장소·pleiades 어디에도 쓰기 0건 (`git status` 확인, 이 파일만 변경).
> `git rm` 미실행. 런타임 측정 없음(1회차 4회로 충분).

## 2-0. 요약

| 구분 | 건수 |
|---|---:|
| **정정** | **8** (1회차 10 → 8) |
| 확인 | 21 |
| 미확인 | 1 (skill 이름 충돌 동작) |

**1회차 정정 10건은 전부 반영됐고, 숫자 대장(N1~N22)은 22행 중 21행이 재계산에서 일치했다.**
1회차가 예고한 다음 라운드 3건도 **셋 다 해소**됐다. 남은 결함의 성격이 바뀌었다 —
1회차는 *산술 오류*였고 2회차는 **정정 전파의 누락**(정정 3·4·5·6)과 **자기 규율의 미적용**(정정 1)이다.

### 작성자 재정정 2건 — **둘 다 확인. 감사 1회차가 틀렸다.**

| | 판정 | 근거 |
|---|---|---|
| **정정 C** (지점 14 · **줄 13**) | **확인 — 1회차 감사의 오류** | `myfitness-orchestrator/SKILL.md:32` = `` **스킬**: `codex-review-loop`, `orphan-check` `` — **한 줄에 두 이름.** 양쪽 목록에 중복 계상된다. 1회차가 "14줄"이라 한 것은 지점 수를 줄 수로 적은 것이다 |
| **정정 D** (`repos/myFitness/CLAUDE.md` 는 stale 아님) | **확인 — 1회차 감사의 오류** | `md5` 양쪽 `2ac702871ddabfccdd99c2b598efa18d` · `diff` **0줄** · 크기 8,621 B · mtime 동일. 1회차는 mtime(Sep 3) < worktree 생성(Sep 4) 만 보고 "잔여 stale" 이라 단정했다. **작성자의 재서술("지금은 동일하나 untracked·ignored 라 앞으로 조용히 어긋난다")이 정확하다** |

## 2-1. 지정 반증 대상 3건

### 주장 1 — "Codex 쌍 유지 + pleiades 신규 작성" 은 역할 이관인가 중복 증식인가

**판정: 정정** (→ 정정 ⑥). 프레이밍 자체는 방어되나 **§4-5 가 H-1 의 산출물을 계산에 넣지 않았다.**

**역할 이관이라는 서술은 성립한다:**
- 공유 내용줄 **0**(N4 · 1회차 실측 재확인) → 생성 시점에 복제되는 텍스트가 없다
- 세 문서가 **서로 다른 세션 유형**을 섬긴다 (fin 단독 / fit 단독 / pleiades 통합). 004 §3-2 + N19 로
  플래그 없이는 서로 보이지 않는다
- `:448` H-1 = 되돌리기 **즉시** · **원본 무변경** — 정확하다

**그러나 두 가지가 빠졌다:**

1. **`--add-dir` 를 쓰는 순간 3벌이 한 세션에 들어온다.** §4-6 은 유지 33파일을 보려면
   `--add-dir` 가 **유일한 경로**라고 못박았고(`:321-326`), §4-5 는 rule 3벌 공존을
   *"가장 위험"*(`:306-308`)이라 했다. H-1 이 만드는 Codex 문서는 **같은 구조를 skill 층에 복제**한다.
2. **새 문서의 이름이 지정돼 있지 않다.** `:448` 은 *"Codex 대응 문서 1개"* 라고만 적는다.
   pleiades `.claude/skills/` 에는 현재 `codex-*` 이름이 없으므로(실측: `decision-doc` ·
   `dual-repo-change` · `pleiades-handoff` · `pleiades-orchestrator` · `pleiades-resume` ·
   `repo-measure` · `reversibility-audit`) **이름을 원본 둘 중 하나로 잡으면 5번째 이름 충돌**이 생긴다.
   그리고 **skill 충돌 시 동작은 측정된 적이 없다** — N18 은 rule(공존)과 agent(마지막 승)만 쟀다.
   [참조] 의 *"skill 23개는 이름이 전부 달라 충돌 0"* 은 **H-1 이전 상태의 값**이다.

### 주장 2 — 형태 B 의 *"드리프트 나면 그때 A·C 로 올라가고 비용이 오르지 않는다"*

**판정: 정정** (→ 정정 ⑦). **검증되지 않았고, 이 문서 자신의 중심 발견과 모순된다.**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:291` | *"드리프트가 실제로 문제가 되면 그때 A·C 로 올라간다 — **그 전환은 언제든 가능하고 비용이 오르지 않는다.**"* |
| `02_writer_harness.md:61` (N4) | *"10쌍 중 실질 중복은 `rules/workflow` 1쌍뿐. 나머지 9쌍의 공유 비공백 유니크 줄은 ≤6줄"* |
| `02_writer_harness.md:532` (§7) | *"Codex 대응 문서(H-1)를 쓴 뒤 fin·fit 원본이 갱신될 때 \| **3벌이 되므로 그때 처음으로 실질 드리프트가 생긴다**"* |

**N4 는 이 저장소가 관측한 유일한 자연 실험이다** — 두 저장소가 같은 하네스를 복사해 갈라진 결과가
**공유 ≤6줄**이고, 그래서 이 문서는 *"병합이 아니라 재작성"* 이라 결론지었다(§5 2행).
**형태 B 는 그 실험을 의도적으로 다시 돌리는 것이다.** 전환 비용을 시점별로 놓으면:

| | T0 (지금 A·C) | T1 (드리프트 후 A·C) |
|---|---|---|
| 참조 재작성 | N13 = **10줄** | 10줄 (동일) |
| fit 원본 삭제 | Q32-i 면 **편도** / ii 후 중간 | 동일 |
| **두 벌 재조정** | **0** — 두 벌이 아직 같다 | **N4 가 "재작성"이라 부른 그 작업** |
| C 의 fit 세션 운영 요건 | `--add-dir` + env (`:283`) | 동일 |

→ **T1 ≥ T0 이고 차액은 정확히 이 문서가 가장 비싸다고 측정한 항목이다.**
B 를 권고하는 나머지 근거(**되돌리기 즉시** · 113줄 단일 스킬 · `CLAUDE.md` 핵심 전제 2)는
**전부 유효하므로 권고 자체는 뒤집히지 않는다.** 삭제해야 하는 것은 *"비용이 오르지 않는다"* 한 문장이다.
정직한 서술은 **"B 는 지금 싸고, 전환을 미룰수록 비싸진다 — 그 대가를 알고 택한다"** 이다.

### 주장 3 — 부록 C-4 ※ 재산출 (형태 A·C = 10줄)

**판정: 확인.**

```bash
cd ~/workspace/myFitness && /usr/bin/grep -rn --binary-files=text "orphan-check" .claude CLAUDE.md
```

| 구분 | 줄 | 지점 |
|---|---|---:|
| inbound (fit 에 남아 orphan-check 를 가리킨다) | `codex-liaison.md:31` · `workflow-conductor.md:34` · `branch-workflow/SKILL.md:175` · `myfitness-orchestrator/SKILL.md:32` · `myfitness-orchestrator/SKILL.md:41` · `CLAUDE.md:117` · `codex-review-loop/SKILL.md:96` | **7** |
| outbound (옮겨간 orphan-check 가 fit 을 가리킨다) | `orphan-check/SKILL.md:96`(release-flow) · `:97`(codex-review-loop) · `:98`(workflow-conductor) | **3** |
| **합** | **10개 파일:줄 — 전부 서로 다른 줄** | **10** |

- `myfitness-orchestrator:32` 는 여기서 **한 번만** 등장한다(Codex 쌍이 유지로 바뀌어 그쪽 목록이 사라졌으므로).
  따라서 **지점 10 = 줄 10** 이고 C-4 의 13/14 같은 괴리가 없다. **재산출이 맞다.**
- outbound 3 줄이 재작성 대상인 것도 맞다 — `release-flow`·`codex-review-loop`·`workflow-conductor`
  셋 다 fit 에 남으므로 pleiades 로 옮겨간 `orphan-check` 에서는 가리킬 수 없다.
- 자기 참조(`orphan-check/SKILL.md:2,104,112`)를 제외한 것도 옳다 — 파일과 함께 이동한다.

## 2-2. 1회차 예고 3건의 해소 여부 — **셋 다 해소**

| 1회차 예고 | 2회차 | 판정 |
|---|---|---|
| **1순위** §4-3 기준 열 재구성 | 공통줄 축을 폐기하고 **4축**(① pleiades 역할 ② 결합 등급 ③ 참조 지점 ④ 공유 줄)으로 재구성. **Codex 행이 이관→유지로 뒤집혔다** — 예고대로 처분이 바뀌었다 | **해소** (단 ③ 열이 정정 ①) |
| **2순위** Q31 이 §4-2 9·25행을 뒤집는다 | 측정 수행 → **폐기 → 유지 + 부분 갱신.** 폐기 505 LOC → 0. H-2 소멸 | **해소 — 예고가 적중했다** |
| **3순위** fit 맨이름 참조 추가 누락 | N10 = **11줄**, 부록 C-2 에 전수 열거 · 명령 재실행 시 **11 재현** | **해소** |

## 2-3. 정정 8건

### 정정 ① §4-3 ③ "참조 지점" 열 — 대장 밖 숫자 20개 · 정의 2개 혼용 · 40/18 재현 불가

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:10-13` (자기 규율) | *"모든 요약 숫자는 **§3-1 숫자 대장에 한 번만** 두고 본문은 `N<번호>` 로 참조한다. 대장의 각 행에는 **열거의 출처**가 붙는다."* |
| `02_writer_harness.md:253-265` (§4-3 ③ 열) | `fin 8·fit 11` / `fin 2·fit 5` / `10` / `fin 5·fit 1` / `fin 6·fit 2` / `fin 40·fit 18` / `fin 2·fit 2` / `fit 3` / `fin 18·fit 7` / `fin 3·fit 2` / `fin 2·fit 2` — **약 20개 중 대장에 있는 것은 3개뿐**(N9·N10·N13) |

**(a) 같은 열에 정의가 둘이다.** Codex 행 `fin 2 · fit 5`:

```bash
git -C repos/myFinance grep --text -n "codex-response-patterns" integration/pleiades -- '.claude' 'CLAUDE.md' | grep -v '^integration/pleiades:\.claude/skills/codex-response-patterns/'
cd ~/workspace/myFitness && /usr/bin/grep -rn --binary-files=text "codex-review-loop" .claude CLAUDE.md | grep -v '^\.claude/skills/codex-review-loop/'
```

| | inbound | outbound | inbound+outbound |
|---|---:|---:|---:|
| fin `codex-response-patterns` | **1** (`session-boundary:49`) | 1 (`:8`) | **2** |
| fit `codex-review-loop` | **5** | 1 (`:96`) | **6** |

→ **fin 의 2 는 inbound+outbound, fit 의 5 는 inbound 전용이다.** 한쪽 정의로 통일하면
`1 · 5` 또는 `2 · 6` 이고, 표에 적힌 `2 · 5` 는 **어느 정의로도 나오지 않는다.**
(§4-2 28행 `:215` 도 *"참조 지점 5"* 로 같은 값을 반복한다.)

**(b) 오케스트레이터 `fin 40 · fit 18` 은 네 가지 계수법 어느 것으로도 재현되지 않는다.**

| 계수법 | fin `milestone-workflow` | fit `myfitness-orchestrator` |
|---|---:|---:|
| **초안 §4-3 ③** | **40** | **18** |
| [참조] 부록 엣지 리스트 전체 | 37 | 26 |
| 〃 `agent:` 엣지만 | 36 | 16 |
| 〃 distinct 줄 | 31 | 17 |
| 파일 직접 name grep | 30 | 9 |

```bash
grep -c '^\.claude/skills/milestone-workflow/SKILL\.md|'    01_surveyor_harness_refs.md   # 37
grep -c '^\.claude/skills/myfitness-orchestrator/SKILL\.md|' 01_surveyor_harness_refs.md  # 26
```

**(c) 이 열이 판정 규칙에 직접 쓰인다.** `:267-268` — *"파일이 pleiades 로 옮겨가려면
**①없음 + ②0건 + ③낮음**을 동시에 만족해야 하고… 그마저 ③이 10지점이라 형태가 갈린다"*.
**③의 임계값이 정의돼 있지 않은 채** 재현 불가능한 값으로 처분을 가르고 있다.

**정정값:** ③ 열의 모든 값을 **N 번호로 승격하거나 §4-3 에서 삭제한다.** 정의를
`inbound` / `inbound+outbound` 중 하나로 고정하고 부록 C 에 전수 열거를 붙인다.
1회차 정정 ①~⑦이 전부 **"열거 없는 요약 숫자"** 였고, 그 재발을 막으려고 만든 것이 §3-1 대장이다.

### 정정 ② N7 이 W-diff 를 손실 압축했다 — **fit 전용 4줄이 사라졌다**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:110` (N7) | *"(c) 35줄 = UI/UX 19 + 나머지 16. **귀속은 공통 30 · fin 전용 5**(디자인 시스템·텔레그램)"* |
| `01_surveyor_harness_workflow_diff.md` §5 | *"(c) 35줄의 귀속: **공통 24 · fin 전용 7 · fit 전용 4.**"* |
| 〃 (같은 절 결론) | *"저장소별로 남겨야 하는 것은 fin 의 …**5줄과 라벨 상한 표기**뿐이다."* |

출처에는 귀속이 **둘** 있다 — **줄 단위**(24 / 7 / 4)와 **주제 단위**(공통 30 · fin 고유 5).
N7 은 주제 단위 값을 취하면서 **(i) "주제 단위" 라는 한정을 지웠고 (ii) `fit 전용 4` 를 통째로 버렸으며
(iii) "라벨 상한 표기" 단서를 뺐다.** 줄 단위 fit 전용 4줄은 §3-2 표에서 확인된다:
`[fit] 라벨: phase-1~phase-N` · `8-0 DB 스키마/마이그레이션` · `8-0 디자인 시안 self-review` · `8-5 예외`.

**파급이 있다.** `:513` Q39 W-1 = *"pleiades 에 '단독 작업' 절 신설(**+공통 30줄, N7**) 후
**원본을 잔여 5줄로 축약**"*. **"원본"은 2개인데 잔여가 fin 것 하나만 적혀 있다** — fit 잔여가
0인 것처럼 읽히고, W-1 의 비용과 fit `workflow.md`(264 LOC)의 운명이 과소평가된다.

**정정값:** N7 → *"귀속(줄 단위) 공통 24 · fin 7 · fit 4 / (주제 단위) 공통 30 · 저장소 고유 5(fin 디자인 시스템)
+ 라벨 표기 각 1"*. Q39 W-1 의 잔여를 **fin·fit 각각** 적는다.

### 정정 ③ fit 유지 파일 수 **17 ↔ 18** 모순 — 권고 B 와 어긋난다

| 어디 | 값 |
|---|---|
| `02_writer_harness.md:346` | *"2회차에서 **유지 대상이 17파일(1,772 LOC)로 늘었으므로**"* |
| `02_writer_harness.md:350` (Q32-i 행) | *"fit 유지 **17파일** 무보호"* |
| `02_writer_harness.md:454` (H-4 행 앞부분) | *"fit 하네스 **17파일**이 이력을 갖는다"* |
| **`02_writer_harness.md:454` (같은 행 뒷부분)** | *"2줄 + **18파일** tracked 화"* |
| `02_writer_harness.md:475` | `H-4 = 2줄 + 18파일` |
| `02_writer_harness.md:564` (부록 A-1) | *"fit 특수 … (**버전 관리 밖 18파일**)"* |

`17 / 1,772` 는 `18 − 1(orphan-check)` · `1,885 − 113` 으로, **형태 A·C(이동) 를 전제한 값**이다.
그런데 §4-4 의 **권고는 B(복사)** 이고(`:282,285`), B 에서는 fit 이 `orphan-check` 를 그대로 보유한다.
**권고안 기준의 값은 18파일 / 1,885 LOC 이다.** 한 행(`:454`) 안에서 17과 18이 함께 쓰였다.

**정정값:** `:346`·`:350`·`:454` 를 **18파일 / 1,885 LOC (권고 B 기준. 형태 A·C 를 택하면 17 / 1,772)** 로.

### 정정 ④ [T6] **에이전트 사전 리뷰 필수 표기가 문서에서 사라졌다**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:28` · `:366` | 문서 전체에서 *"에이전트 리뷰 필수"* 가 나오는 **유일한 두 곳**이고, 둘 다 **소멸한 H-2** 를 설명하며 `:366` 은 *"**해당 없음**"* 으로 끝난다 |
| `02_writer_harness.md:452` (H-3 fin) | *"#8 fin 25줄 — 원본 `dev` PR (모드 S · 이슈는 fin 저장소)"* — **리뷰 요건 없음** |
| `02_writer_harness.md:454` (H-4) | *"fit `.gitignore:35,36` 2줄 제거 + `git add`"* — **모드·승인 게이트·`<issue-repo>` 없음** |
| `.claude/rules/workflow.md` 9-0 표 | *"**대상 저장소 변경 — 경로 무관**(`repos/**` **또는 원본 `~/workspace/myF*`**) → **에이전트 필수** \| 실서비스 영향"* |

**H-3(fin)은 myFinance `dev` 로 PR 을 연다 — 실서비스 저장소다.** 9-0 표의 필수 행에 정확히 걸리고,
같은 표의 주석은 *"이전 행은 `repos/**` 만 걸어 **원본에서 하는 모드 S·H 가 에이전트 리뷰 필수에서 빠졌다**"*
는 결함을 이미 한 번 고친 이력이 있다. **H-2 를 지우면서 그 표기가 함께 지워졌다.**
H-4 도 fit 소스 변경인데, 모드·게이트·이슈 저장소가 **§4-7 ii(`:351`)에만 있고 단계 표에는 없다.**

**정정값:** §4-13 H-3(fin)·H-3(fit)·H-4 행에 **`에이전트 사전 리뷰 필수(9-0)`** 를 명시하고,
H-4 에 **`모드 S · 승인 게이트 · 이슈는 fomalhaut84/myFitness`** 를 §4-7 ii 와 같이 적는다.

### 정정 ⑤ `measured-facts.md` 가 정정 전 값을 갖고 있고 **H-0 범위에 없다**

| 어디 | 값 |
|---|---|
| `docs/research/measured-facts.md:1887` | `### H4. fin `.claude/` 16파일 `git rm` 파급 — **활성 5건** · 빌드/CI 0` |
| `02_writer_harness.md:117` (N14) | **6건** (`CLAUDE.md` 62·118·128·**145** + `route.ts:5`) |
| `docs/research/measured-facts.md:1918` | `### H5. auto memory 의존 — **11파일**` |
| `02_writer_harness.md:120` (N17) | **13파일** (fin 9 · fit 2 · ple 2) |
| `.claude/rules/workflow.md` 2절 | *"`docs/research/measured-facts.md` 를 **먼저 읽는다. 있는 값은 재측정하지 않는다**"* |

**`measured-facts.md` 는 이 저장소의 실측 정본이고, 룰이 재측정을 금지한다.**
005 가 정본이 되어도 `measured-facts.md` 가 그대로면 **다음 세션은 5건·11파일을 읽고 재측정하지 않는다.**
초안의 H-0(`:447`)은 *"pleiades 룰 정정 — #10 14줄 + grep 6곳 + R1·R2"* 로 **`docs/research/` 를 포함하지 않는다.**

**정정값:** H-0 범위에 **`measured-facts.md` H4·H5 헤더와 본문의 정정 블록**을 추가한다
(이 저장소 규약대로 **덮어쓰지 않고 정정 블록으로**). 되돌리기는 **즉시 · `git revert` 1회**로 동일하다.
1회차가 `repo-surveyor` 에게 요청한 `_workspace/` 3건은 작업 파일이고, **정본은 `measured-facts.md` 다.**

### 정정 ⑥ §4-5 이름 충돌 목록이 **H-1 자신의 산출물**을 반영하지 않는다

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:300-304` | 충돌 표 3행 = `workflow`(rule) · `api-routes`·`components`(rule) · `release-manager`(agent). **H-1 이전 상태** |
| `02_writer_harness.md:448` (H-1) | *"pleiades **신규 작성** — Codex 대응 문서 **1개**"* — **이름 미지정** |
| `02_writer_harness.md:121` (N18) | 충돌 동작 측정 대상 = **rule · agent 뿐**. skill 은 없다 |
| `01_surveyor_harness_refs.md` 가정 2 | *"**skill 23개는 이름이 전부 달라 충돌 0**"* — 역시 H-1 이전 값 |

pleiades `.claude/skills/` 현재 목록(실측)에 `codex-*` 는 없다. 따라서 H-1 의 산출물을
`codex-response-patterns` 또는 `codex-review-loop` 로 이름 지으면 **5번째 충돌**이 생기고,
`--add-dir` 로 그 저장소를 붙인 세션에서 **skill 층 충돌이 처음 발생**한다.
**skill 충돌 시 rule 처럼 공존하는지 agent 처럼 조용히 드롭되는지는 미측정이다.**

**정정값:** H-1 에 **산출물 이름 제약**을 명시한다 — *"fin·fit 의 기존 skill 이름과 겹치지 않는 이름을 쓴다
(예: `codex-liaison-patterns` 는 fit agent 와 겹치므로 불가)"*. §4-5 표에 **H-1 이후 상태 행**을 추가하고,
겹치는 이름을 쓸 경우에 대비해 **skill 충돌 동작 측정 1회**를 미측정 항목으로 등재한다.

### 정정 ⑦ 형태 B 의 *"비용이 오르지 않는다"* — 주장 2 참조

**정정값:** `:291` 문장 삭제 → *"**B 는 지금 가장 싸고, 전환을 미룰수록 비싸진다** —
두 벌이 갈라지면 전환에 N4 가 '재작성'이라 부른 재조정이 더해진다. 그 대가를 알고 택한다."*
권고 B 자체는 유지된다(되돌리기 즉시 · 단일 113줄 스킬 · 핵심 전제 2).

### 정정 ⑧ [T5·경미] 부록 C-7 의 명령 2개가 기재된 값을 내지 않는다

```bash
cd ~/workspace/pleiades
/usr/bin/grep -rn --binary-files=text -- 'binary-files=text' CLAUDE.md .claude/   # 초안 "6곳" → 실제 10줄 출력
/usr/bin/grep -rn --binary-files=text -- '체크아웃' .claude/                      # 초안 "8줄" → 실제 18줄 출력
/usr/bin/grep -rn --binary-files=text -- '/tmp/' .claude/                         # 6 ✅ 정확
```

앞 둘은 **보일러플레이트 외에 명령 예시·다른 맥락의 줄까지 잡는다.** `:688-692` 의
**열거 자체는 전수 정확**(1회차 확인)하지만, 부록 C 의 목적이 *"열거의 출처를 재현 가능하게"* 이므로
**필터가 문서화되지 않은 명령은 그 목적을 달성하지 못한다.**

**정정값:** 두 명령에 필터를 붙이거나(예: `| grep '^\S*:[0-9]*:> \*\*'`), **"명령은 상위집합을 반환하며
아래 열거가 분류 결과"** 라고 한 줄 적는다.

## 2-4. 확인 21건

| # | 항목 | 근거 |
|---|---|---|
| 1 | **N1 = 34파일 / 3,514 LOC / 136,743 B** | `ls-tree -l` + `stat -f%z` → fin 68,822 + fit 67,921 = **136,743** 바이트 단위까지 일치 |
| 2 | **처분 표 37행 전수** | 36파일 LOC 개별 대조 오류 **0** (1회차 재사용 + 재확인). 헤더 fin **17행**/fit **18행**/CLAUDE **2행** = 37 ✓ |
| 3 | **N2 집계** | 이관 1/113 · 유지 33/3,401 · 합 34/**3,514** ✓ · fin 16 + fit 18 ✓ · 유지 16+17=33 ✓ |
| 4 | **N2 비율 라벨** | 2.94%/3.216%/97.06%/96.78% → `파일 2.9% · LOC 3.2% · 파일 97.1% · LOC 96.8%` **라벨이 정확**(1회차 정정 ⑩ 반영) |
| 5 | **N4 공유 줄 10행** | 1회차 실측표와 **전 행 일치** (118·4·4·6·3·3·2·2·2·1) |
| 6 | N5 (161·172·344·215·34·181) | 1회차 재현 + W-diff 재현 |
| 7 | **N6 등급 92/39/35/15** | W-diff 절별 교차표 23행을 열별·행별로 합산 → a92 b39 c35 d15, 행합 181 **전부 일치** |
| 8 | **Q31 hard-zero 근거** | `docs/designs`·`frontend-design`·`roadmap`·`마이그레이션`·`phase-` → pleiades `workflow.md` 에 **전부 0건** |
| 9 | **(c) 는 죽은 규약이 아니다** | fin `docs/designs` **25** · specs **126** · roadmap **419줄** / fit **8** · **95** · **381줄** — W-diff 값과 일치 |
| 10 | **N8 위임 1줄** | `/usr/bin/grep -n 'workflow\.md'` → `:5`(출처 표기) · **`:165`(위임)** 둘뿐 ✓ |
| 11 | 핫픽스 절 (c) = 0 | W-diff 교차표 `## 긴급 수정 (Hotfix)` 행 = a3 b1 c**0** d0 ✓ |
| 12 | N9 = 8 · **부록 C-1 명령 재현** | 명령 그대로 실행 → **8** |
| 13 | N10 = 11 · **부록 C-2 명령 재현** | 명령 그대로 실행 → **11** |
| 14 | N11 = 6 (절 번호 어긋남) | 1회차 실측 + pleiades 8절=검증/9절=리뷰 재확인 |
| 15 | N13 = 10지점/10줄 | 주장 3 참조 |
| 16 | N14 = 6 · **부록 C-6 명령 재현** | 명령 그대로 → `62·118·128·140·145` **5줄** + `route.ts:5` = 6 ✓ |
| 17 | N15 = 58 · N16 = 14줄+6곳 · N17 = 13 · N21 · N22 | 1회차 전수 확인. `/tmp` 6줄은 C-7 명령으로 **정확히 6** 재현 |
| 18 | **N19 (Q37)** | [MF] 표의 a/a2 행은 rules 를 *"변별 불가"* 로 남겼는데, **1회차 감사의 독립 측정이 그 칸을 채운다** — env 없이 `--add-dir fit` → `workflow_md_dirs`=pleiades **1개**, env 켜면 **2개**. ②(rules 는 env 필요)가 **양방향에서 확인** |
| 19 | **N20 (Q33)** | [MF] 스캔 패턴·매치 2건(curl UA · loopback) 확인. 비밀·자격증명 0 |
| 20 | **미결 번호 충돌 없음** | 003 ≤Q28 · 004 Q20~24 · 신설 **Q39·Q40** ✓. Q31·Q33·Q37 의 §2 이동과 Q35 삭제가 §6 머리에 명시(`:507`,`:520-521`) |
| 21 | **렌더링(T7)** | 표 열 수 불일치 **0건**. 1회차 정정 ⑩(헤더 19→18) 반영 확인 |

**되돌리기 3요소 전수:** §4-4 A/B/C · §4-5 부착 3안 · §4-6 ①~④ · §4-7 i/ii/iii · §4-9 a/a-2/b/c ·
§4-10 R1/R2 · §4-13 H-0~H-5 — **등급·행위 전부 존재.** `소요` 는 문서 정책(`:480-482`)으로 일괄 생략이며
R2 로 규율화를 제안하고 있다(1회차와 동일 판단, 정정 아님).
*경미:* Q39 W-2(`:513`)에만 되돌리는 **행위**가 없다(등급 "즉시"만). H-2 두 행(`:450-451`)의 `—` 는
범위 0 이므로 타당하다.

## 2-5. 미확인 1건

| 항목 | 왜 못 했나 | 무엇이 있어야 하나 |
|---|---|---|
| **skill 이름 충돌 시 동작** | H-1 산출물의 이름이 정해지지 않아 충돌 조건을 만들 수 없다. 임의로 pleiades `.claude/skills/` 에 파일을 만드는 것은 **읽기 전용 범위 밖** | 이름이 정해진 뒤 런타임 1회 (1회차 rule·agent 측정과 동일 방식). 겹치지 않는 이름을 쓰기로 하면 **불필요** |

## 2-6. 결론

### (a) 정정 건수 — **8건** (1회차 10 → 8)

| # | 정정 | 무게 |
|---|---|---|
| ① | §4-3 ③ 열 — 대장 밖 20개 · 정의 2개 혼용 · 40/18 재현 불가 · **판정 규칙이 이 열에 걸려 있다** | **중대** |
| ② | N7 이 W-diff 를 손실 압축 — fit 전용 4줄 소실 · Q39 W-1 비용에 파급 | **중대** |
| ③ | fit 유지 17 ↔ 18 (권고 B 와 어긋남) | 중간 |
| ④ | **T6 — 에이전트 사전 리뷰 필수가 H-2 와 함께 사라졌다** | **중대** |
| ⑤ | `measured-facts.md` 정본이 정정 전 값(5건·11파일) 유지 · H-0 범위 밖 | **중대** |
| ⑥ | §4-5 충돌 목록이 H-1 산출물 미반영 · skill 이름 제약 부재 | 중간 |
| ⑦ | 형태 B *"비용이 오르지 않는다"* — N4 와 모순 | 중간 |
| ⑧ | 부록 C-7 명령 2개가 기재값을 내지 않음 | 경미 |

**→ 문서는 초안이다.** 다만 **1회차와 성격이 다르다.** 1회차는 산술이 무너져 §4-3·§4-13 **개정본**을
권고했으나, 2회차의 8건은 **①을 뺀 7건이 국소 수정으로 닫힌다.** ①만 §4-3 ③ 열의 재측정 또는 삭제가 필요하다.
`.claude/rules/workflow.md` 4절의 *"3회를 넘으면 스코프를 줄인다"* 는 **2회차이므로 아직 아니다.**

**주목할 점:** 1회차의 #11 유형(합계≠열거)이 **2회차에는 0건**이다. 대장 22행 중 21행이 재계산에서 일치했고,
어긋난 1행(N7)은 합계 오류가 아니라 **출처의 두 값 중 하나를 한정 없이 취한 것**이다.
§3-1 대장 형식은 **효과가 있었다.** 남은 구멍은 **대장 밖에 새 숫자가 생기는 경로**(정정 ①)다 —
R1(`:413`)에 *"같은 숫자를 문서 안에서 두 번 적지 않는다"* 는 있으나
**"대장에 없는 숫자를 본문에 새로 만들지 않는다"** 가 없다.

### (b) 다음 라운드에서 나올 가능성이 가장 높은 3건

| 순위 | 예상 정정 | 왜 |
|---|---|---|
| **1** | **§4-3 의 판정 규칙 `①없음 + ②0건 + ③낮음` 자체가 재구성된다.** ③의 임계값이 정의돼 있지 않은데, **유일한 이관 대상 `orphan-check` 가 ③=10 으로 표 전체에서 가장 높다**(`:257`, `:267-268`). 정정 ①로 ③ 열을 다시 재면 *"③낮음"* 이 `orphan-check` 를 배제하거나, 규칙이 ③을 형태 선택(Q29)으로만 쓰도록 바뀔 것이다. 그러면 **이관 1파일마저 0** 이 될 수 있다 | 규칙의 세 번째 항이 재현 불가능한 값 위에 있고, 그 값이 결론과 역방향이다 |
| **2** | **Q39 W-1 의 비용이 자기 재귀로 커진다.** `:513` 이 이미 *"pleiades 에 절을 추가하면 **번호가 또 밀려 그 6줄이 다시 어긋난다**"* 고 적었으나, W-1 의 표기 비용은 `+공통 30줄`과 `N11 = 6줄`뿐이고 **재귀분과 fit 잔여(정정 ②)가 계상돼 있지 않다.** §7 유효기간도 *"pleiades 에 절이 추가되면 N11 이 커진다"* 로 같은 위험을 등재했다 | 문서가 위험을 두 곳에 적어놓고 비용표에는 넣지 않았다 — 1회차 Q31 과 **정확히 같은 패턴**(§6 에 적고 처분은 반대로) |
| **3** | **H-1 산출물의 이름·위치·중복 관리 규약**(정정 ⑥). H-1 은 순서 제약이 없어 *"어느 것을 먼저 해도 된다"*(`:459`)로 **즉시 착수 가능**으로 분류돼 있는데, 산출물 이름이 미정이고 skill 충돌 동작이 미측정이다. 착수 직전 감사(`dual-repo-change` §2)에서 걸릴 항목 | 착수 가능 판정과 미결 항목이 같은 단계에 공존한다 |

## 2-7. `decision-writer` 에게 — 고쳐야 할 문장 (2회차)

| 위치 | 이렇게 고친다 |
|---|---|
| `:253-265` (§4-3 ③) | ③ 열 값을 **N 번호로 승격**(정의 고정 + 부록 C 전수 열거)하거나 **열을 삭제**. `:267-268` 판정 규칙의 *"③낮음"* 에 **임계값**을 적는다 |
| `:110` (N7) | *"귀속은 공통 30 · fin 전용 5"* → *"**줄 단위** 공통 24 · fin 7 · fit 4 / **주제 단위** 공통 30 · fin 고유 5 + 라벨 표기 각 1"* |
| `:513` (Q39 W-1) | *"원본을 잔여 5줄로 축약"* → *"**fin 잔여 5줄 + 라벨 1 · fit 잔여 4줄**로 축약"*. 비용에 **절 번호 재귀분** 추가 |
| `:346` · `:350` · `:454` | `17파일 / 1,772 LOC` → **`18파일 / 1,885 LOC`**(권고 B 기준. A·C 면 17 / 1,772) |
| `:452` · `:453` · `:454` | 각 행에 **`에이전트 사전 리뷰 필수(9-0 "대상 저장소 변경 — 경로 무관")`** 명시. H-4 에 **`모드 S · 승인 게이트 · 이슈는 fomalhaut84/myFitness`** 추가 |
| `:447` (H-0) | 범위에 **`measured-facts.md` H4(`:1887` 5건→6건)·H5(`:1918` 11→13) 정정 블록** 추가 |
| `:300-304` · `:448` | §4-5 에 **H-1 이후 행** 추가. H-1 에 **산출물 이름이 fin·fit 기존 skill 과 겹치지 않아야 한다**는 제약과, 겹칠 경우 **skill 충돌 동작 측정 1회**가 선결이라는 단서 |
| `:291` | *"비용이 오르지 않는다"* 삭제 → *"지금 가장 싸고 미룰수록 비싸진다 — N4 의 재조정이 더해진다"* |
| `:684-685` | 명령에 필터를 붙이거나 *"명령은 상위집합을 반환하며 아래 열거가 분류 결과"* 를 명시 |
| `:413` (R1) | 문구에 **"대장에 없는 숫자를 본문에 새로 만들지 않는다"** 추가 (정정 ①의 재발 방지) |

---
---

# 3회차 감사 (2026-09-07 · 최종 순환)

대상 `_workspace/harness/02_writer_harness.md` **796줄** (커밋 `0e47bd3`)

## 정본 발행 가능 여부

> **발행 불가.** **A 2건 · B 3건**이 남는다. C 2건은 발행을 막지 않는다.
> **다만 5건 모두 국소 수정으로 닫히며, 처분(이관 1 / 유지 33)·권고(B·ii·④·W-2)는 흔들리지 않는다.**
> A-1 만 `orphan-check` 의 작업 표면 값(`0줄`)을 바꾼다.

| 등급 | 뜻 | 건수 |
|---|---|---:|
| **A** | 숫자·사실이 실물과 다름 | **2** |
| **B** | 절 간 모순 · 정정 전파 누락 | **3** |
| C | 표현·라벨 | 2 |

> **읽기 전용.** 대상 저장소·pleiades 쓰기 0건. `git rm` 미실행. 런타임 측정 없음.

## 3-0. 2회차 정정 ①~⑧ 폐쇄 확인 — **8건 전부 닫혔다**

| 2회차 정정 | 닫혔나 | `파일:줄` |
|---|---|---|
| ① §4-3 ③열 (대장 밖 20개 · 40/18 재현 불가) | ✅ **열 삭제** | `:296-302` 정정 E · `:304` *"축은 셋이며 **전부 대장 값이다**"* · 판정 규칙에서 *"③낮음"* 제거(`:319-323`) |
| ② N7 손실 압축 (fit 전용 4줄 소실) | ✅ | `:151` N7 = *"줄 단위: 공통 24 · fin 7 · fit 4 / 주제 단위: 공통 30 · 고유 5 + 라벨 각 1"* · Q39 `:610` = *"fin 잔여 5 + 라벨 1 · **fit 잔여 4 + 라벨 1**"* |
| ③ fit 17↔18 모순 | ✅ | `:287` · `:422` · `:426` · `:531` · `:661` 전부 **18파일 / 1,885 LOC**(권고 B 기준) + A·C 단서 |
| ④ T6 에이전트 리뷰 소실 | ✅ **9-0 판정 열 신설** | `:521` 헤더 · `:529` H-3(fin) *"모드 S · 이슈는 `fomalhaut84/myFinance`"* + 리뷰 필수 · `:531` H-4 *"모드 S · 승인 게이트 · 이슈는 `fomalhaut84/myFitness`"* + 리뷰 필수 |
| ⑤ `measured-facts.md` 정정 전 값 | ✅ **커밋 `2dfcf6f`** | `docs/research/measured-facts.md` 말미 `## 정정 — 하네스 참조 그래프 실측 H4·H5` (H4 5→**6** · H5 11→**13**, 원문 보존). 초안 `:537-543` 정정 I 가 참조 |
| ⑥ §4-5 가 H-1 산출물 미반영 | ✅ | `:369` skill 층 행 신설 · `:371-380` 정정 G(이름 제약) · **Q41 신설**(`:615`) |
| ⑦ 형태 B *"비용 안 오름"* | ✅ **문장 삭제** | `:355-360` 정정 F — *"B 는 지금 가장 싸고, 전환을 미룰수록 비싸진다"* |
| ⑧ C-7 명령이 상위집합 | ✅ | 부록 C-7 정정 J — *"명령은 후보를 모으는 데 쓰고, 아래 열거가 분류 결과다"* + 분류 기준 명시 |

**T6 승인 게이트 요구 전부 충족:** H-3(fin) = 모드 S + `fomalhaut84/myFinance` ✅ ·
H-4 = 모드 S + 승인 게이트 + `fomalhaut84/myFitness` ✅.

## 3-1. 지정 반증 대상 3건

### 주장 1 — 축3 의 fit `api-routes` 배제와 `orphan-check` 포함이 같은 강도인가

**판정: 정정 (A-1).** **같은 강도가 아니고, 약한 쪽이 포함이다.**

두 파일은 판정 규칙이 쓰는 **모든 측정 축에서 동일**하다 — ① pleiades 역할 **없음** · ② 리터럴 결합
**0건**(둘 다 N3-b 의 3파일에 든다). 따라서 갈림은 전적으로 규칙 3의 **질적 판정**에 있다(`:325`).
실물을 열어 보면 **두 파일 모두 ② 지표가 세지 않는 결합을 갖는다.**

**fit `rules/api-routes.md` (8 LOC · 표는 결합/도메인 `0 / 0`) — 배제는 옳으나 근거가 표에 없다**

```
:5  - 모든 route handler는 try-catch로 감싸고, 에러 시 `{ error: string }` 형태로 응답
:8  - 수치 데이터 응답에 항상 단위 포함 (km, kg, bpm, kcal 등)
```

`:8` 의 `km · kg · bpm · kcal` 은 **피트니스 도메인 단위**이고, `:5` 의 `{ error: string }` 은
fin·pleiades 가 규범으로 삼는 envelope `{ success, data?, error?, meta? }` 와 **다른 규약**이다.
서베이어의 도메인 성분 집계([내용] §3 성분표: 절대경로·앱경로·포트·pm2·저장소명·도메인 용어)가
이 토큰들을 세지 않아 **도메인 0** 이 됐다. → 규칙 3의 배제 판정은 **실물이 지지한다.**

**fit `skills/orphan-check/SKILL.md` (113 LOC · 표는 `0 / 0`) — 포함 근거가 실물과 어긋난다**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:325` | *"`orphan-check` 는 **git 절차라 저장소에 무관**하다"* |
| `~/workspace/myFitness/.claude/skills/orphan-check/SKILL.md` | **`dev` 가 12줄**(`3·8·13·19·22·29·30·57·58·69·79·106`) · `origin/dev` 3줄 · `:79` `gh pr create --base dev --head <type>/<issue>-<N+1>` |
| `.claude/rules/workflow.md:148-150` | pleiades 의 base 는 **셋** — 통합 `integration/pleiades` · 단독 `dev` · 핫픽스 `main` |

```bash
/usr/bin/grep -c --binary-files=text '\bdev\b' ~/workspace/myFitness/.claude/skills/orphan-check/SKILL.md   # 12
/usr/bin/grep -c --binary-files=text 'origin/dev' ~/workspace/myFitness/.claude/skills/orphan-check/SKILL.md # 3
```

**"저장소에 무관"은 문자 그대로는 참이다**(myFitness 리터럴 0). 그러나 이 스킬은 **브랜치 모델에 결합**돼 있고,
**pleiades 의 주 용도인 통합 작업은 base 가 `dev` 가 아니다.** 그대로 옮기면 통합 작업·핫픽스에서
**틀린 브랜치를 orphan 기준으로 삼는다** — 조용히 틀리는 종류다.

**N3-b 의 "리터럴 참조 0건"은 무결합의 증거가 아니다.** 성분 목록에 **브랜치명이 없다.**
규칙 1이 그 값을 *"출발점"* 으로 삼는데(`:320`), 출발 집합 3개 중 **분쟁 중인 두 파일 모두**
지표가 못 센 결합을 갖는다.

**작업 표면이 바뀐다 (이것이 A 등급인 이유):**

| 어디 | 값 | 실제 |
|---|---|---|
| `02_writer_harness.md:340` (형태 B) | 재작성 **0줄** | 형태 B 는 파일 복사라 참조는 0줄이 맞으나, **pleiades 사본이 `dev` 를 하드코딩한 채 들어온다** |
| `02_writer_harness.md:526` · `:566` (H-1b) | *"113 LOC 복사 · 참조 재작성 **0줄**"* | **브랜치 파라미터화 ~12줄**이 빠져 있다. 넣지 않으면 pleiades 통합 세션에서 오작동하는 스킬을 배치하는 것 |

**정정값:** `:325` → *"`orphan-check` 는 저장소 리터럴이 없으나 **`dev` 12줄로 브랜치 모델에 결합**돼 있다.
pleiades base 3종(7절 표)에 맞추려면 **base 를 인자화하는 ~12줄 수정**이 따른다."*
H-1b 작업 표면 → **`113 LOC 복사 + base 파라미터화 12줄`**. Q29 에 *"형태와 무관하게 12줄이 붙는다"* 를 명시.

### 주장 2 — §4-2-1 주석의 *이관* 과 §2-1 의 *역할 이관* 이 같은 것을 가리키는가

**판정: 정정 (B-1).** **가리키지 않는다. 라벨이 결과가 아니라 출처로 나뉜다.**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:283-285` | *"***이관*** 은 **역할이 pleiades 로 온다**는 뜻이고, 원본에서 파일이 빠지는지는 형태(Q29)가 정한다"* |
| `02_writer_harness.md:305` (§4-3 Codex 행) | *"▲ **원본 유지 · pleiades 신규 작성**"* — **역할이 pleiades 로 온다** |
| `02_writer_harness.md:255` · `:271` (§4-2 10·28행) | 처분 = **유지** |
| `02_writer_harness.md:277` (§4-2 31행) | 처분 = **이관** |

`:283` 의 정의를 적용하면 **Codex 쌍도 "이관"** 이다 — 역할이 pleiades 로 온다.
그런데 표는 **유지**로 적고, `orphan-check` 만 **이관**으로 적는다.
**권고 형태 B 에서 두 경우의 결과는 동일하다:** pleiades 가 산출물을 하나 얻고 원본은 그대로 남는다.
갈리는 것은 **바이트 복사냐 새로 쓰느냐라는 출처**뿐이고, 그것은 *처분*이 아니라 *제작 방법*이다.

**결과 두 가지:**
1. **N2 집계가 "pleiades 가 얻는 것"을 세지 않는다.** 이관 1파일 / 113 LOC 에 **H-1 산출물이 0 LOC 로** 들어간다.
   `:562-566` 확정값도 H-1 을 *"입력 200 LOC · 산출 미산출"* 로 두어, **pleiades 가 실제로 늘어나는 양이 문서 어디에도 없다.**
2. `:125` §2-1 은 *"파일째로 가져오는 것은 1개"* 와 *"역할 이관"* 을 한 문단에 놓아 **두 층위를 섞는다.**

**정정값:** 처분 열을 **결과 기준 2값**으로 정리한다 — **`원본 유지`**(34행 전부, 형태 B 기준) ·
그와 직교하는 **`pleiades 산출물`** 열(`복사 1(orphan-check)` · `신규 작성 1(Codex)`).
"이관"이라는 단어는 `:283` 의 정의대로 **역할 층에만** 쓰고, 파일 층에는 형태(A/B/C)를 쓴다.

### 주장 3 — H-3(fit) 의 9-0 에이전트 필수가 집행 가능한가

**판정: 정정 (B-3).** **판정은 옳고 집행 수단이 없다. Q32-ii 선행을 조건으로 걸어야 한다.**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:530` (9-0 열) | *"**에이전트 사전 리뷰 필수** — 같은 행 (PR 이 없어도 대상 저장소 변경이다)"* |
| `02_writer_harness.md:530` (등급 열, 같은 행) | *"**Q32-i 면 편도** · ii 이후 즉시"* — **Q32-i 실행 경로를 열어둔다** |
| `.claude/rules/workflow.md` 9-1 | ```Review branch <current> vs <base> in <repo path>``` — **브랜치·base·diff 를 요구** |
| [감사] 1회차 확인 6 | fit `.claude/` **tracked 0 · 커밋 이력 0** |

**9-0 판정 자체는 맞다** — 9-0 표의 *"대상 저장소 변경 — 경로 무관"* 은 PR 유무를 조건으로 걸지 않는다.
그러나 **Q32-i 에서는 `<current>`·`<base>` 가 존재하지 않아 9-1 을 호출할 수 없다.**
리뷰 대상 ref 가 없으므로 필수 게이트가 **선언만 되고 집행되지 않는다.**

**문서에 이미 답이 반쯤 있다.** `:548-549` 순서 제약 **`H-4 → H-3(fit)`** 이 그 상태를 만든다
(tracked 화 후에는 브랜치·diff·PR 이 생긴다). 그러나 그 제약의 사유가 **되돌리기 등급**으로만 적혀 있고
(*"tracked 화를 먼저 하면 fit 의 #8 수정이 편도에서 즉시로 내려간다"*), **리뷰 집행의 선결로는 걸려 있지 않다.**
그래서 등급 열이 여전히 Q32-i 경로를 허용한다.

**정정값:** 셋 중 하나.
① **H-3(fit) 을 Q32-ii 필수 선행으로 승격** (순서 제약 사유에 *"9-0 리뷰를 집행할 ref 가 생긴다"* 추가) — 권고.
② 등급 열의 `Q32-i 면 편도` 경로를 **삭제**한다.
③ ref 없는 리뷰 형태를 9-0 열에 명시한다(예: *"워킹트리 파일 내용 직접 리뷰 — 9-1 프롬프트의 `branch vs base` 를
`file content` 로 대체"*). ③을 택하면 `.claude/rules/workflow.md` 9-1 에 그 변형을 추가해야 하므로 **H-0 범위가 늘어난다.**

## 3-2. 정정 7건 (A 2 · B 3 · C 2)

### (A) 숫자·사실이 실물과 다름 — 2건

**A-1. `orphan-check` 의 `dev` 12줄 — 주장 1 참조.** `:325` · `:340` · `:526` · `:566` ↔ `orphan-check/SKILL.md:3,8,13,19,22,29,30,57,58,69,79,106` · `.claude/rules/workflow.md:148-150`

**A-2. §4-5 부착 정책 `rule 5벌` 산술 오류**

| 어디 | 값 |
|---|---|
| `02_writer_harness.md:388` | `한 번에 한 저장소만 \| rule **2벌** 공존(그 저장소 + pleiades)` |
| `02_writer_harness.md:389` | `둘 다 + fit **agent** 개명 \| rule **5벌** · agent 0` |
| `02_writer_harness.md:390` | `둘 다 방치 \| rule **7벌** · agent 1개 조용한 드롭` |

**389행과 390행은 `fit agent 개명` 여부만 다르다. agent 를 개명해도 rule 수는 변하지 않는다.**
검산은 같은 절 `:366-368` 이 준다 — `workflow` **3벌** + `api-routes` **2벌** + `components` **2벌** = **7**.

**정정값:** `:389` `rule 5벌` → **`rule 7벌`**. (열 제목이 *충돌*이므로, 개명이 줄이는 것은 agent 충돌뿐이다.)

### (B) 절 간 모순 · 정정 전파 누락 — 3건

**B-1. 이관/유지 라벨이 결과가 아니라 출처로 나뉜다 — 주장 2 참조.** `:283-285` ↔ `:305` · `:255`/`:271` · `:277` · `:125`

**B-2. H-3(fin) 착수 가능 여부가 네 곳에서 다르다**

| 어디 | 무엇 |
|---|---|
| `02_writer_harness.md:550` | *"H-0 · H-1b · **H-3(fin)** 은 서로 **독립**이며 어느 것을 먼저 해도 된다"* |
| `02_writer_harness.md:469` | *"권고: fin = a(**즉시 착수**)"* |
| `02_writer_harness.md:610` (Q39 우선) | *"**높음 — H-3 착수 전** (같은 파일을 건드린다)"* |
| `02_writer_harness.md:625` (§7) | *"Q29·Q30·Q32·**Q39**·Q41 에 답이 나올 때 \| H-1·H-1b·**H-3**·H-4 가 착수 가능해지고"* |

**Q39 가 실제로 H-3(fin)을 막는다.** W-1 은 fin `workflow.md` 를 잔여 5줄+라벨 1로 축약하고,
H-3(fin)은 **같은 파일**의 #8 25줄을 고친다. W-1 을 나중에 택하면 그 25줄이 축약과 함께 사라지거나 충돌한다.
Q39 자신이 *"같은 파일을 건드린다"* 로 그 이유를 적어 놓았다.
→ `:550` 의 *"독립"* 과 `:469` 의 *"즉시 착수"* 가 **Q39·§7 과 정면으로 어긋난다.**

**정정값:** `:550` 에서 **H-3(fin) 을 독립 목록에서 뺀다** → *"H-0 · H-1b 는 서로 독립. **H-3(fin) 은 Q39 선행**"*.
`:469` → *"fin = a — 단 **Q39 가 W-2(원본 유지)로 확정된 뒤**"*. (Q39 권고가 이미 W-2 이므로 확정만 하면 즉시 착수 가능하다.)

**B-3. H-3(fit) 9-0 리뷰가 Q32-i 경로에서 집행 불가 — 주장 3 참조.** `:530` 두 열 ↔ `.claude/rules/workflow.md` 9-1 ↔ `:548-549`

### (C) 표현·라벨 — 2건 (발행을 막지 않는다)

**C-1. §4-5 충돌 표의 `33줄`·`9줄`·`25줄` 이 대장 밖이고 출처 표기가 없다.**
`:366-368`. 합 67 = [참조] §1-F 의 *"이름 충돌 4건 / 참조 67줄(fin 31 · fit 16 · ple 20)"* 분해값이다.
3회차가 대장 밖 숫자를 삭제한 기준(정정 E, `:296-302`)에 비추면 **남은 예외**다.
→ **N23 신설**(이름 충돌 참조 67줄 = `workflow` 33 · `api-routes`+`components` 9 · `release-manager` 25) 또는 출처 표기.

**C-2. 회차 라벨 불일치.** `:343` *"**2회차** 권고는 B 다"* ↔ `:422` *"**3회차** 권고(형태 B)"*.
같은 권고를 두 회차로 부른다. → `:343` 을 *"권고는 B 다(2회차부터)"* 로.

## 3-3. 확인 (전수)

| 항목 | 결과 |
|---|---|
| **대장 N1~N22** | **22행 전부 재계산 일치.** N1 136,743 B(바이트 단위) · N2 비율 4개 · N4 10행 · N5 · N6 교차표 92/39/35/15=181 · N7(양쪽 귀속 이제 출처와 일치) · N8 `:165` 1줄 · N9 8 · N10 11 · N11 6 · N12 14/13 · N13 10/10 · N14 6 · N15 58 · N16 14+6 · N17 13 · N18 · N19 · N20 · N21 · N22 |
| **처분 표 37행** | fin **17행** · fit **18행** · CLAUDE **2행** = 37 ✓. LOC 36파일 개별 대조 오류 0 (1·2회차 재사용) |
| **집계** | 이관 1/113 · 유지 33/3,401 · 합 34/**3,514** ✓ · fin 16 + fit 18 ✓ |
| **T5 명령** | C-1 → **8** ✓ · C-2 → **11** ✓ · C-6 → **5**(+`route.ts:5`=6) ✓ · C-7 `/tmp` → **6** ✓ · C-7 앞 둘은 정정 J 가 상위집합임을 명시 ✓ |
| **T7 렌더링** | 표 열 수 불일치 **0건** |
| **Q 번호** | Q29·Q30·Q32·Q34·Q36·Q38·Q39·Q40·**Q41**. 003 ≤Q28 · 004 Q20~24 와 **충돌 0**. Q31·Q33·Q37 §2 이동 · Q35 삭제가 `:604-605` 에 명시 ✓ |
| **T6 승인 게이트** | H-3(fin) 모드 S + `fomalhaut84/myFinance` ✓ · H-4 모드 S + 승인 게이트 + `fomalhaut84/myFitness` ✓ · 9-0 판정 열 8단계 전부 채움 ✓ |
| **되돌리기 3요소** | §4-4 · §4-5 2표 · §4-6 · §4-7 · §4-9 · §4-10 R1·R2 · §4-13 · **Q39 W-1/W-2(3회차에 행위 추가)** — 등급·행위 전수 존재. `소요` 는 문서 정책(`:571-574`)으로 일괄 생략 + R2 로 규율화 제안 |
| **T1 상호 정합** | 단계 표 ↔ 부록 A(`:661` fit 18파일 · H-4 전까지 버전 관리 밖) ↔ §6 ↔ §7 — **B-2 를 제외하고 일치.** 부록 A-1 이 `orphan-check` 를 pleiades 공통에 넣지 않은 것도 형태 B 와 정합 |
| **2회차 정정 ⑤ 폐쇄 방식** | `measured-facts.md` 를 **덮어쓰지 않고 정정 블록 append**(커밋 `2dfcf6f`) — 이 저장소 정정 규약과 정합 ✓ |

**대장 밖 숫자 스캔** (`grep -nE '[0-9,]+ ?(줄|파일|건|LOC|벌|지점|쌍)'` 후 N 참조 없는 줄 열거):
본문 전 구간에서 대장에 없고 출처도 없는 것은 **C-1 의 3개(33·9·25)뿐**이다. 나머지는 전부
§4-2 표의 LOC 열, 대장 값의 재기술, 또는 형태별 조건값(18/1,885 · 17/1,772)으로 근거가 붙어 있다.
**2회차의 ③열 20개 같은 대량 유입은 재발하지 않았다.**

## 3-4. 결론

**A 2 · B 3 · C 2 — 정본 발행 불가.** 그러나 **성격이 세 회차 중 가장 가볍다.**

| 회차 | 정정 | 성격 |
|---|---:|---|
| 1회차 | 10 | 산술 붕괴 — §4-3·§4-13 **개정본** 권고 |
| 2회차 | 8 | 정정 전파 누락 + 자기 규율 미적용 |
| **3회차** | **7 (발행 저지 5)** | **국소 5건. 처분·집계·권고 불변** |

**닫는 데 필요한 것:**
- **A-1** — `orphan-check` 의 `dev` 12줄을 §4-3 규칙 3과 H-1b 작업 표면에 반영 (**측정 불필요**, 위 명령이 근거)
- **A-2** — 숫자 하나 (`5벌` → `7벌`)
- **B-1** — 처분 열을 결과/출처 두 축으로 분리
- **B-2** — `:550`·`:469` 를 Q39 와 맞춤
- **B-3** — H-3(fit) 에 Q32-ii 선행을 리뷰 집행 사유로 추가

**다섯 건 모두 새 측정을 요구하지 않는다.** 스코프 축소가 필요한 상황은 아니라고 판단한다 —
`.claude/rules/workflow.md` 4절의 *"3회를 넘으면 스코프를 줄인다"* 는 **정정이 줄지 않을 때**의 신호인데,
10 → 8 → 7(저지 5)로 줄고 있고 **처분 표·집계·권고는 2회차 이후 한 번도 바뀌지 않았다.**

**다만 A-1 이 시사하는 것은 기록해 둔다.** 세 회차 연속으로 **"측정 지표가 세지 않은 결합"** 이
결론을 흔들었다 — 1회차 빈 줄(공통줄 %), 2회차 정의 혼용(참조 지점), 3회차 브랜치명(리터럴 결합).
**N3-a 성분표(절대경로·앱경로·포트·pm2·저장소명·도메인)에 `브랜치명`·`도메인 단위`가 없다.**
정본화 시 §3-1 에 *"이 지표가 세지 않는 것"* 한 줄을 남기면 다음 사용자가 같은 함정을 피한다.

## 3-5. `decision-writer` 에게 — 고쳐야 할 문장 (3회차 · 5건)

| 위치 | 이렇게 고친다 |
|---|---|
| `:325` (규칙 3) | *"`orphan-check` 는 git 절차라 저장소에 무관하다"* → *"`orphan-check` 는 **저장소 리터럴이 0**이지만 **`dev` 12줄로 브랜치 모델에 결합**돼 있다(`:3,8,13,19,22,29,30,57,58,69,79,106`). pleiades 는 base 가 셋이므로(7절 표) **base 인자화 ~12줄**이 이관에 따라붙는다"* |
| `:340` · `:526` · `:566` | H-1b 작업 표면 → **`113 LOC 복사 + base 파라미터화 12줄`**. 형태 B 의 *재작성* 열은 `참조 0줄 / base 12줄` 로 분리 |
| `:389` | `rule 5벌` → **`rule 7벌`** |
| `:255` · `:271` · `:277` · `:283-285` | 처분 열을 **`원본 유지`(형태 B 기준 34행)** + 직교 열 **`pleiades 산출물`(복사 1 · 신규 작성 1)** 로 분리. *"이관"* 은 역할 층에만 |
| `:550` · `:469` | *"H-3(fin) 독립"* · *"즉시 착수"* → **`H-3(fin) 은 Q39 선행`**(Q39 권고 W-2 확정 시 즉시 착수) |
| `:530` | H-3(fit) 등급 열의 `Q32-i 면 편도` 경로를 닫거나, 순서 제약 `H-4 → H-3(fit)` 의 사유에 **"9-0 리뷰를 집행할 ref 가 생긴다"** 추가 |
| `:366-368` | **N23 신설**(충돌 참조 67줄 분해) 또는 [참조] §1-F 출처 표기 |
| `:343` | *"2회차 권고는 B 다"* → *"권고는 B 다(2회차부터)"* |
| `:141` (N3-a) | *(권고)* **"이 지표가 세지 않는 것: 브랜치명 · 도메인 단위 토큰"** 한 줄 추가 |

---
---

# 4회차 감사 (2026-09-07 · 범위 한정)

대상 `_workspace/harness/02_writer_harness.md` **836줄** (커밋 `f77a600`)
범위: **3회차 발행 저지 5건(A-1·A-2·B-1·B-2·B-3)의 폐쇄 확인만.** 그 밖은 A 등급만 보고.

## 정본 발행 가능 여부

> **발행 가능.** 발행 저지 5건 **전부 폐쇄** · 새 A 등급 **0건**.
> 3회차 C 2건도 함께 닫혔다. 남는 것은 **후속 B 2건**이며 정본 발행을 막지 않는다.

> **읽기 전용.** 대상 저장소·pleiades 쓰기 0건. `git rm` 미실행.
> 보고된 줄 번호는 4회차 편집으로 이동했으므로 **내용으로 조회**해 확인했다.

## 4-1. 발행 저지 5건 폐쇄 판정

| # | 판정 | 실물 확인 |
|---|---|---|
| **A-1** | ✅ **폐쇄** | 아래 5곳 전부 |
| **A-2** | ✅ **폐쇄** | `:421` |
| **B-1** | ✅ **폐쇄** | `:252` · `:277` · `:280` · `:299-310` · `:120-125` |
| **B-2** | ✅ **폐쇄** | `:586-589` · `:501` |
| **B-3** | ✅ **폐쇄** | `:497` · `:562` · `:580-584` |

### A-1 — `orphan-check` 브랜치 결합 · base 파라미터화 12줄

**다섯 지점 전부 반영됐고, 인용한 줄 번호가 실물과 정확히 일치한다.**

| 지점 | 반영 내용 |
|---|---|
| **`:350`** (§4-3 규칙 3) | *"`orphan-check` 는 저장소 리터럴이 0이지만 **`dev` 12줄(`:3,8,13,19,22,29,30,57,58,69,79,106`)로 브랜치 모델에 결합**돼 있다 — pleiades 의 base 는 셋이므로(`.claude/rules/workflow.md:148-150`) **base 인자화 ~12줄이 이관에 따라붙는다.**"* — *"git 절차라 저장소 무관"* 문구 **삭제됨** |
| **`:166`** (N3-b) | *"**이 지표가 세지 않는 것: 브랜치명 · 도메인 단위 토큰**(N3-a 성분표에 두 항목이 없다) — 그래서 3파일 중 둘이 지표 밖 결합을 갖는다"* — 3회차 권고(§3-5 마지막 행)를 그대로 수용 |
| **`:369-371`** (§4-4 A·B·C) | 세 안 전부 `재작성` 열이 **`참조 N줄` + `base 12줄`** 로 분리 (A: 참조 10 · base 12 / B: 참조 0 · base 12 / C: 참조 10 · base 12) |
| **`:558`** (H-1b) | *"`orphan-check` 를 pleiades 로 (형태 B = 복사) **+ base 파라미터화**"* · 작업 표면 **`113 LOC 복사 + base 파라미터화 12줄 · 참조 0줄`** |
| **`:606`** (확정값 블록) | `H-1b = 113 LOC 복사 + base 파라미터화 12줄 · 참조 0줄 (§4-4 형태 B · 정정 K)` |

**줄 번호 인용 재검증 — 정확히 일치:**

```bash
/usr/bin/grep -n --binary-files=text '\bdev\b' ~/workspace/myFitness/.claude/skills/orphan-check/SKILL.md | cut -d: -f1
#   3 8 13 19 22 29 30 57 58 69 79 106   ← 초안 :350 의 인용과 12개 전부 동일
```

`.claude/rules/workflow.md:148-150` 이 base 3종(`integration/pleiades` · `dev` · `main`)이라는 것도 재확인 ✅.
**추가로 `:355` 가 *"이관에는 형태와 무관하게 base 파라미터화 12줄이 붙는다"* 로 Q29 와의 독립성을 명시**했다 —
3회차가 요구한 문구다.

### A-2 — `rule 5벌` → `rule 7벌`

| 줄 | 값 |
|---|---|
| `:420` | `한 번에 한 저장소만 \| rule **2벌** 공존` |
| **`:421`** | `둘 다 + fit agent 개명 \| **rule 7벌** · agent 0` ← **5 → 7 수정됨** |
| `:422` | `둘 다 방치 \| **rule 7벌** · agent 1개 조용한 드롭` |

421 과 422 가 이제 같은 rule 수를 갖는다 — agent 개명이 rule 수를 바꾸지 않는다는 사실과 일치.
검산 `:366-368` 의 `workflow 3벌 + api-routes 2벌 + components 2벌 = 7` 과도 일치 ✅.

### B-1 — 이관/유지 라벨을 결과 축과 출처 축으로 분리

**처분 열이 세 행 모두 두 축을 병기하도록 재작성됐다:**

| 행 | 새 처분 열 |
|---|---|
| `:252` (fin 10 `codex-response-patterns`) | **원본 유지** · pleiades **신규 작성**(H-1) |
| `:277` (fit 28 `codex-review-loop`) | **원본 유지** · pleiades **신규 작성**(H-1) |
| `:280` (fit 31 `orphan-check`) | **원본 유지**(형태 B 권고) · pleiades **복사**(H-1b) — 형태는 Q29 |

**`:299-310` 정정 L** 이 두 축을 표로 분리했다 — `원본 파일`(형태 B 에서 34 전부 유지 · 집계 *이관 1/유지 33* 은 A·C 기준) ·
`pleiades 산출물`(복사 1 + 신규 작성 1). *"이관은 이후 역할 층에만 쓰고 파일 층에는 형태(A/B/C)를 쓴다"* 로 규약도 명시.
**`:120-125` §2-1 도 함께 재작성**되어 *"pleiades 가 얻는 것은 **산출물 2개**"* 로 시작한다 —
3회차가 지적한 *"H-1 산출물이 0 LOC 로 계상된다"* 가 해소됐다(산출 LOC 는 여전히 미산출이나 **개수로는 세어진다**).

### B-2 — H-3(fin) 착수 조건 통일

| 줄 | 내용 |
|---|---|
| **`:586-587`** | *"**H-0 · H-1b 는 서로 독립**이며 어느 것을 먼저 해도 된다. **H-3(fin) 은 Q39 선행이다**"* ← 3회차 지적한 *"H-3(fin) 독립"* **삭제됨** |
| `:588-589` | 사유 명시 — *"W-1 은 fin `workflow.md` 를 잔여 5줄+라벨 1로 축약하는데 H-3(fin)은 같은 파일의 #8 25줄을 고치므로 충돌한다. **Q39 권고는 이미 W-2 이므로 확정만 하면 즉시 착수 가능**"* |
| **`:501`** (§4-9 권고) | *"fin = a — **단 Q39 가 W-2(원본 유지)로 확정된 뒤**"* ← *"즉시 착수"* 단독 표현 **삭제됨** |
| `:665` (§7) | Q39 를 H-3 의 게이트로 유지 — 이제 위 셋과 정합 |

네 곳이 하나의 조건(**Q39 선행**)으로 수렴했고, `:501` 이 *"착수 조건의 정본은 §4-13 순서 제약"* 으로
**정본 위치까지 지정**했다 ✅.

### B-3 — H-3(fit) 9-0 리뷰 집행 가능성

3회차가 제시한 세 선택지 중 **① Q32-ii 필수 선행 승격**을 택했고, ②(`Q32-i` 경로 폐쇄)도 **함께** 적용했다.

| 줄 | 내용 |
|---|---|
| **`:497`** (§4-9 a-2) | 행 제목이 *"a-2 · fit 수정 (**Q32-ii 선행 필수**)"* 로 바뀌고 등급 **`즉시 (H-4 이후)`** · 비고 *"**Q32-i 경로는 닫혔다** — 9-0 리뷰를 집행할 ref 가 없다"* |
| **`:562`** (H-3(fit)) | *"#8 fit 33줄 — **Q32-ii(H-4) 선행 필수.** 그 뒤 브랜치·PR 경로로 수정"* · 9-0 열에 *"**Q32-i 에서는 집행 불가**(9-1 이 `branch vs base` 를 요구하는데 ref 가 없다)"* · 등급 **`즉시 (H-4 이후)`** — `Q32-i 면 편도` **삭제됨** |
| **`:580`** | 순서 제약 *"**H-4 → H-3(fit) — 3회차에서 권고에서 필수로 승격**"* |
| **`:584`** | *"따라서 Q32-ii 없이 H-3(fit) 은 착수할 수 없고, 등급 열의 '`Q32-i 면 편도`' 경로는 닫는다"* |

`.claude/rules/workflow.md` 9-1 이 `Review branch <current> vs <base> in <repo path>` 를 요구한다는
근거도 `:562` 에 직접 인용됐다 ✅.

## 4-2. 3회차 C 2건 — 요구하지 않았으나 함께 닫혔다

| | 3회차 지적 | 4회차 |
|---|---|---|
| C-1 | §4-5 충돌 표의 `33`·`9`·`25` 가 대장 밖·출처 없음 | ✅ `:390` 에 출처 추가 — *"줄 수(`33`·`9`·`25`)는 [참조] §1-F 의 **이름 충돌 4건 / 참조 67줄**(fin 31 · fit 16 · ple 20)을 이름별로 분해한 값"* |
| C-2 | `:343` *"2회차 권고는 B 다"* ↔ `:422` *"3회차 권고"* | ✅ *"**권고는 B 다(2회차부터)**"* 로 회차 라벨 제거 |

## 4-3. 새 A 등급 — **0건**

4회차 추가분(`git diff 0e47bd3 f77a600`)에서 숫자·사실을 담은 줄을 전수 추출해 실물과 대조했다.

| 확인 항목 | 결과 |
|---|---|
| `dev` 12줄 · 줄 번호 12개 | **실측과 완전 일치** (위 명령) |
| `.claude/rules/workflow.md:148-150` base 3종 | 일치 |
| `rule 7벌` = 3+2+2 | 일치 (`:366-368`) |
| `산출물 2개` = 복사 1 + 신규 작성 1 | 일치 (H-1b + H-1) |
| `18파일 / 1,885 LOC` · `17 / 1,772` | 일치 (1,885 − 113 = 1,772) |
| 집계 이관 1 / 113 · 유지 33 / 3,401 · 합 34 / 3,514 | 일치 |
| 처분 표 행 수 | fin **17** · fit **18** · CLAUDE **2** = 37 ✅ |
| `33`·`9`·`25` 합 = 67 = fin 31 + fit 16 + ple 20 | 일치 |
| 렌더링(T7) | 표 열 수 불일치 **0건** |

**→ 새 A 등급 0건.**

## 4-4. 후속 (B 등급 — 목록만 · 발행을 막지 않는다)

| | 내용 | 위치 |
|---|---|---|
| **후속 1** | **Q32 가 `i`(그대로)로 답하면 #8 fit 33줄이 영구 미수정으로 남는다.** B-3 수정이 `Q32-i` 경로를 닫으면서 생긴 귀결인데, 문서는 그 경우를 적지 않았다. H-4 는 여전히 **조건부**(`:563` *"(권고 · Q32-ii)"*)다. §4-9 c 행(*"방치"*)이 사실상 그 시나리오이므로 **Q32-i 선택 시 #8 fit 은 c 로 확정된다**는 한 줄이 §6 Q32/Q34 에 필요하다 | `:563` · `:497` · `:584` · §6 Q32·Q34 |
| **후속 2** | **N2(`:164`)는 여전히 파일 층에서 *이관* 을 쓴다** — 정정 L(`:310`)이 *"이관은 이후 역할 층에만"* 이라 규약했으므로 대장 자신이 그 규약의 예외로 남는다. 숫자(1 / 113 / 33 / 3,401)는 A·C 기준으로 **전부 정확**하고 출처 열이 §4-2-1 을 가리키므로 추적은 된다 | `:164` ↔ `:310` |

## 4-5. 결론

**발행 저지 5건 전부 폐쇄 · 새 A 0건 · 3회차 C 2건도 폐쇄.**

| 회차 | 정정 | 발행 저지 |
|---|---:|---:|
| 1회차 | 10 | 10 |
| 2회차 | 8 | 8 |
| 3회차 | 7 | 5 |
| **4회차** | **0** | **0** |

**이 문서는 정본으로 발행할 수 있다.** `docs/specs/005-harness-integration.md` 로 옮기고
`CLAUDE.md` 문서 지도에 부록 A-2 행을 추가한다. 후속 B 2건은 **정본 발행 후 한 줄씩 보완**하면 되고,
미결 Q29·Q30·Q32·Q39·Q41 은 문서가 스스로 등재한 항목이라 발행 요건이 아니다.

> **네 회차의 궤적을 한 줄로.** 처분 표의 **LOC·파일 수는 1회차부터 한 번도 틀리지 않았고**,
> 무너진 것은 언제나 **그 위에 올린 요약·비율·판정 축**이었다 —
> 1회차 빈 줄(공통줄 %), 2회차 정의 혼용(참조 지점), 3회차 지표 밖 결합(브랜치명).
> 셋 다 **"측정 지표가 세지 않은 것"** 이었고, 4회차가 그것을 N3-b 한 줄로 문서에 새겼다.
