# 005 (초안 3회차) — 하네스 통합 · Q20 / 이슈 #1

작성 2026-09-07 · **3회차 개정** (1·2회차 모두 2026-09-07) · 상태 **초안 · 감사 1·2회차 반영 · 정본 아님**
산출 위치 `_workspace/harness/02_writer_harness.md` · 정본 후보 위치 `docs/specs/005-harness-integration.md`

> **여전히 `docs/specs/` 에 없다.** 감사 3회차를 통과해야 정본이 된다
> (`.claude/rules/workflow.md` 4절 *"정정이 하나라도 나오면 문서는 다시 초안"*).
> 이 세션에서도 대상 저장소(`repos/*`, `~/workspace/myF*`)에 **쓰기 0건** — 실행한 명령은 부록 B·C.

> **이 문서의 숫자 규율 (2회차 신설 · 3회차 강화).** 모든 요약 숫자는 **§3-1 숫자 대장**에 한 번만 두고
> 본문은 `N<번호>` 로 참조한다. 대장의 각 행에는 **열거의 출처**(파일 · 절 · 행 범위)가 붙는다.
> 근거: 1회차에서 **합계와 열거가 어긋난 사례가 6건**(#8 43↔52 · memory 11↔13 · `git rm` 5↔6 ·
> H-0 4곳↔6곳 · H-1 13↔14 · 표 헤더 19↔18) 나왔고, 전부 **열거가 맞고 합계가 틀렸다.**
>
> **3회차 추가 규율: 대장에 없는 숫자를 본문에 새로 만들지 않는다.** 2회차는 대장을 만들어놓고
> §4-3 에 **대장 밖 숫자 20개**를 새로 썼고 그중 하나는 어떤 계수법으로도 재현되지 않았다(감사 2회차 정정 ①).
> **재현할 수 없는 숫자는 고쳐 쓰지 않고 문서에서 뺀다** — 이번 회차에서 삭제한 목록은 §0-0 에 있다.

---

## 0-0. 3회차에서 무엇이 바뀌었나

입력 1건: 감사 2회차 `_workspace/harness/03_auditor_harness.md` **정정 8건**
(+ 작성자 재정정 C·D 는 **둘 다 확인**되어 그대로 둔다 — 1회차 감사 쪽이 틀렸다).
**결론(처분·집계·권고)은 바뀌지 않았다.** 바뀐 것은 근거의 재현 가능성과 누락된 규율 표기다.

### 0-0-1. 이번 회차에서 **삭제한 숫자·주장**

| 삭제 | 어디 있었나 | 왜 |
|---|---|---|
| §4-3 **③ "참조 지점" 열 전체** (대장 밖 숫자 약 20개) | 2회차 §4-3 | `fin 40 · fit 18` 이 네 계수법 어느 것으로도 재현 불가 · fin/fit 정의 혼용(inbound+outbound ↔ inbound). **고쳐 쓰는 대신 삭제**하고 판정을 대장 값(N3-b·N8·N4)만으로 다시 유도했다 |
| 판정 규칙의 **"③낮음"** 항 | 2회차 §4-3 말미 | 임계값이 정의된 적이 없다. 유일한 이관 대상이 그 축에서 최고값(10)이라 규칙과 결론이 역방향이었다 |
| §4-2 28행의 **"참조 지점 5"** | 2회차 §4-2 | 같은 대장 밖 숫자의 반복 |
| 형태 B 의 **"전환 비용이 오르지 않는다"** | 2회차 §4-4 | 이 문서 자신의 N4 와 모순 (정정 F) |

### 0-0-2. 감사 2회차 정정 8건 반영 위치

| 정정 | 내용 | 반영 |
|---|---|---|
| ① | §4-3 ③ 열 — 대장 밖 20개 · 정의 혼용 · 재현 불가 · 판정 규칙이 여기 걸림 | **정정 E** — ③ 열 삭제 + 판정 규칙 4단계 재유도 |
| ② | N7 이 W-diff 를 손실 압축 — **fit 전용 4줄 소실** | **N7 재작성**(줄 단위/주제 단위 병기) · Q39 W-1 잔여를 fin·fit 각각 |
| ③ | fit 유지 **17 ↔ 18** 모순 (권고 B 와 어긋남) | §4-7 · H-4 행 → **18파일 / 1,885 LOC** (A·C 면 17 / 1,772) |
| ④ | **에이전트 사전 리뷰 필수 표기가 H-2 와 함께 사라졌다** | **정정 H** — 단계 표에 **9-0 리뷰 판정 열** 신설 |
| ⑤ | `measured-facts.md` 정본이 정정 전 값 | **정정 I** — 오케스트레이터가 커밋 `2dfcf6f` 로 정정 블록을 append. H-0 범위 추가 불필요 |
| ⑥ | §4-5 충돌 목록이 H-1 산출물 미반영 · skill 이름 제약 부재 | **정정 G** — §4-5 에 H-1 이후 행 + **이름 제약** · **Q41** 신설 |
| ⑦ | 형태 B *"비용이 오르지 않는다"* | **정정 F** — 문장 삭제, 권고는 유지 |
| ⑧ | 부록 C-7 명령 2개가 기재값을 내지 않음 | **정정 J** — "명령은 상위집합, 아래 열거가 분류 결과" 명시 |

### 0-0-3. 감사가 예고한 다음 라운드 3건에 대한 선반영

| 예고 | 이번 회차 |
|---|---|
| **1순위** 판정 규칙 `③낮음` 재구성 → 이관 1파일마저 0 이 될 수 있다 | **③ 을 판정에서 뺐다.** 이관 판정은 N3-b 3파일에서 축4·축3 으로 좁혀 `orphan-check` 1개가 남는다. **참조 결합은 형태(Q29)만 가른다** — 이관 수는 1로 유지되고 근거가 재현 가능해졌다 |
| **2순위** Q39 W-1 비용의 자기 재귀 · fit 잔여 미계상 | Q39 를 **세 겹 비용(ⓐ신설 30줄 ⓑN11 6줄 ⓒ재귀분)** 으로 다시 적고 **fit 잔여 4줄 + 라벨 1**을 넣었다 |
| **3순위** H-1 산출물 이름·중복 관리 규약 | §4-5 에 **이름 제약**을 명시하고 **Q41** 로 등재. 겹치지 않는 이름을 쓰면 측정 불필요 |

---

## 0. 2회차에서 무엇이 바뀌었나

입력 3건: 감사 `_workspace/harness/03_auditor_harness.md`(정정 10건) ·
측정 `_workspace/harness/01_surveyor_harness_workflow_diff.md`(Q31) ·
`docs/research/measured-facts.md` 후속 3절(Q37 · Q33 · workflow 버린 줄).

### 0-1. 결론이 바뀐 것 2건

| | 1회차 | **2회차** | 왜 |
|---|---|---|---|
| **처분** | fin·fit `rules/workflow.md` **폐기(대체)** · Codex 쌍 + `orphan-check` **이관** (이관 3 · 폐기 2 · 유지 29) | **폐기 0.** `workflow.md` 2파일은 **유지 + 부분 갱신**, Codex 쌍도 **유지**(pleiades 는 신규 작성). 이관은 `orphan-check` **1파일** (이관 1 · 유지 33) | Q31 측정 + 감사 정정 ① |
| **H-2 (fin `git rm`)** | 2파일 / 328 LOC · 승인 게이트 · 에이전트 리뷰 | **범위 0~1파일. 권고는 "지금 하지 않는다"** | 위와 같음 |

### 0-2. 감사 정정 10건 반영 위치

| 정정 | 내용 | 반영 |
|---|---|---|
| ① | 드리프트 "공통줄 %" = 빈 줄 포함 상한. 실질 공유 줄은 9쌍 모두 ≤6, Codex 쌍은 **1줄(`---`)** | §3-2 정정 A · §4-3 **축 전면 교체** |
| ② | 병합 배율 1.60 은 단위 불일치 (유니크 줄 비율 × raw LOC) | §4-13 **산출식 폐기**, "미산출" 로 |
| ③ | 형태 C 참조 0줄 → **최소 6줄** (절 번호 어긋남) · fit 참조 5 → **11줄** | §3-2 정정 B · N10 · N11 |
| ④ | auto memory 11 → **13파일** (fin 9 · fit 2 · ple 2) | N17 · §4-12 |
| ⑤ | H-1 참조 13 → 14 (`CLAUDE.md:117`) | N12 (**추가 정정**: 지점 14 · **줄 13** — §3-2 정정 C) |
| ⑥ | fin `git rm` 활성 파급 5 → **6건** (`CLAUDE.md:145`) · 문서 내부 모순 | N14 |
| ⑦ | H-0 범위 12줄+4곳 → **14줄+6곳** (감사 하네스 2파일 누락) | N16 · §4-10 · §4-11 |
| ⑧ | 이름 충돌 "미측정" 해소 — **rule 은 3벌 공존, agent 는 마지막 `--add-dir` 이 조용히 이긴다** | N18 · §4-5 |
| ⑨ | §5 #3 출처 오인용 ("주석" 누락 · 근거는 바뀌지 않았다) | §5 3행 |
| ⑩ | 표 헤더 19행 → 18행 · LOC 비율을 파일 수에 라벨 | §4-2 헤더 · N2 |

### 0-3. 감사 §5(판단 필요) 5건 반영

| | 지적 | 반영 |
|---|---|---|
| (1) | H-2 의 fit 절반은 모드 I 로 집행 불가 | §4-13 에서 **H-2 를 fin/fit 두 행으로 분리**. 단 2회차에서는 H-2 자체가 거의 소멸했다 |
| (2) | `repos/myFitness/CLAUDE.md` 사본 | §4-6 에 기록. **정정: 아직 stale 이 아니다** — 원본과 `diff` **0줄**(부록 B-8). untracked·ignored 라 **앞으로** 조용히 어긋난다 |
| (3) | "쌍" 개수 10 ↔ 11 | **10쌍 + 짝 없음 1** 로 통일 (N4) |
| (4) | "3" 이 두 뜻 | 대장에서 분리 — **N3-b**(참조 0건 3파일) vs **N2**(이관 1파일). 1회차의 "이관 3파일"은 2회차에서 사라졌다 |
| (5) | 되돌리기 3요소 중 "소요" 일괄 생략 | 규율 이탈을 유지하되 **H-0 에 룰 문구 추가안**을 넣는다 (§4-10) |

---

## 1. 무엇이 바뀌었나 — 004 Q20·Q21 의 전제

| | 004 의 전제 | 실측 | 대장 |
|---|---|---|---|
| ① | 두 저장소 하네스는 **역할 쌍둥이**라 합치면 한 벌이 된다 | **10쌍 중 실질 중복은 `rules/workflow` 1쌍뿐.** 나머지 9쌍의 공유 비공백 유니크 줄은 **≤6줄**, Codex 쌍은 **1줄** | **N4** |
| ② | 이관 단위는 fin 16 · fit 18 | fin 에 gitignored `settings.local.json` 1개가 더 있다. 실제 **fin 17 + fit 18 = 35** | N1 |
| ③ | 회피책은 `--add-dir` 뿐일지 모른다 | 맞다. 그리고 **rules·CLAUDE.md 는 환경변수까지 필요**하며 **방향은 대칭**이다 | **N19** |
| ④ | *(1회차가 세운 전제)* pleiades `workflow.md` 가 원본을 대체할 수 있다 | **불가.** 버린 181줄 중 **35줄이 단독 작업에 필요한데 pleiades 에 0건** | **N5 · N6 · N7** |

④ 가 2회차의 방향을 바꿨다. ①은 1회차 결론(*"병합이 아니라 재작성"*)을 **강화**하되,
그 결론이 향하는 곳을 *"pleiades 로 합친다"* 에서 *"애초에 합칠 것이 없다"* 로 옮긴다.

---

## 2. 확정된 답

| | 질문 | 답 | 근거 |
|---|---|---|---|
| **Q20** | 하네스 통합 범위 | **공통은 pleiades 로, 저장소별 특수는 그 저장소에 유지** | 사용자, 2026-09-07 |
| **Q31** | fin·fit `workflow.md` 를 폐기해도 되나 | **폐기 불가.** 버린 181줄 중 (c) 35줄이 단독 작업에 필요하고 pleiades 에 주제어 기준 0건. **막는 것은 단독 작업이고 핫픽스는 (c) 0건** | 측정 `01_surveyor_harness_workflow_diff.md` §2·§3·§4 |
| **Q33** | fit 하네스를 공개 저장소에 올릴 때 민감 문자열이 있나 | **없다.** 비밀·자격 증명·개인 식별자 **0건**, 무해 매치 2건 | [MF] "fit 하네스 민감 문자열 스캔" |
| **Q37** | `--add-dir` 의 로딩 범위와 방향 | **skills·agents 는 환경변수 없이 로드 · rules·CLAUDE.md 는 `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` 필요 · 방향 대칭**(대상 저장소 cwd 도 `--add-dir` 로 pleiades 하네스를 본다) | [MF] "`--add-dir` 로딩 범위와 환경변수" |
| — | 이름 충돌 시 CLI 우선순위 | **rule 은 3벌 전부 공존(드롭 없음) · agent 는 마지막 `--add-dir` 이 이기고 나머지는 조용히 사라진다** | 감사 §1 주장 3 (런타임 4회) |

### 2-1. 확정된 답을 파일에 적용한 결과

**34파일 중 pleiades 가 파일째로 가져오는 것은 `orphan-check` 1개뿐이고(N2), 나머지 33파일은 저장소에 남는다**
— 그리고 **권고 형태 B 에서는 그 1개도 사본이라 원본 34파일이 모두 남는다**(§4-2-1 주석).
"공통은 pleiades 로" 는 **파일 이동이 아니라 역할 이관**으로 실현된다 — pleiades 가 그 역할의 문서를
**원본을 입력 삼아 새로 쓰고**, 원본은 단독 세션의 살아 있는 절차로 남는다. 근거는 §4-3.

---

## 3. 실측 근거

| 출처 | 약칭 |
|---|---|
| `_workspace/harness/01_surveyor_harness_content.md` | **[내용]** |
| `_workspace/harness/01_surveyor_harness_refs.md` | **[참조]** |
| `_workspace/harness/01_surveyor_harness_workflow_diff.md` | **[W-diff]** |
| `_workspace/harness/03_auditor_harness.md` | **[감사]** |
| `docs/research/measured-facts.md` | **[MF]** |

### 3-1. 숫자 대장 — 이 문서의 모든 요약 숫자는 여기 한 번만 있다

| ID | 값 | **열거의 출처** (합계가 아니라 열거를 센 곳) |
|---|---|---|
| **N1** | 이관 대상 실체 = fin 16 + fit 18 = **34파일 / 3,514 LOC / 136,743 B**. `+ CLAUDE.md 2 + fin settings.local.json 1 = 37행` | §4-2 전수 37행 · [내용] §1-4 검산표 (감사 확인 1·2: 36파일 개별 대조 오류 0) |
| **N2** | 처분 = **이관 1파일 / 113 LOC (파일 2.9% · LOC 3.2%)** · **유지 33파일 / 3,401 LOC (파일 97.1% · LOC 96.8%)** | §4-2 전수 37행 · 검산 §4-2-1 |
| **N3-a** | 결합 성분 fin 106 + fit 152 = **258건**, 파라미터 흡수 가능 **182(70.5%)**, 도메인 **76** 중 **61(80.3%)이 4파일** | [내용] §3 성분표 |
| **N3-b** | **리터럴 참조 0건 파일 = 3 / 36** (fin `rules/workflow` · fit `rules/api-routes` · fit `skills/orphan-check`) — **N2 의 "1파일"과 다른 집합이다** | [내용] §3 무파라미터 공통화 표 |
| **N4** | **공유 비공백 유니크 줄 (10쌍 + 짝 없음 1)**: `rules/workflow` **118** · `components` 4 · `release-manager` 4 · 릴리즈 6 · 세션재개 3 · 인계 3 · 브랜치 2 · 오케스트레이터 2 · `api-routes` 2 · **Codex 1(`---`)** | [감사] §2 정정 ① 표 (10행 전수) |
| **N5** | `workflow.md` 3자 집합: fin 161 · fit 172 · ple 344 · fin∪fit 215 · **유지 34(실질 12)** · **버림 181 (84.2%)** | [W-diff] §1 · 부수발견 (34줄 성분 4행) |
| **N6** | 버린 181줄 등급: (a)흡수 **92** · (b)의도적 삭제 **39** · **(c)단독 작업에 필요·pleiades 0건 35** · (d)판단 불가 **15** | [W-diff] §2 절별 교차표 23행 |
| **N7** | (c) 35줄 = UI/UX 단계 **19** + 나머지 **16**. 귀속이 **두 가지다** — **줄 단위: 공통 24 · fin 전용 7 · fit 전용 4** / **주제 단위: 공통 30 · 저장소 고유 5**(fin 디자인 시스템·텔레그램) **+ 라벨 상한 표기 각 1**(`phase-1~6` vs `phase-1~N`). **핫픽스 절 (c) = 0** | [W-diff] §3-1 전문 · §3-2 16행 표 · **§5 (두 귀속이 모두 여기 있다)** |
| **N8** | pleiades → 원본 **위임 고리 = `workflow.md:165` 1줄** (단독 작업에만 걸린다) | [W-diff] §4 |
| **N9** | fin → `workflow.md` 참조 **8줄** | 부록 C-1 (8줄 전수) |
| **N10** | fit → `workflow.md` 참조 **11줄** (1회차 5줄 → 맨이름 6줄 누락) | 부록 C-2 (11줄 전수) |
| **N11** | 그중 **절 번호·단계 목록을 인용해 pleiades 재배치와 어긋나는 줄 = 6** | 부록 C-3 (6줄 전수) |
| **N12** | *(1회차 이관 범위 3파일 기준의 정정값 — 2회차에서는 쓰이지 않는다)* 재작성 = **참조 지점 14 · 실제 줄 13** (한 줄이 두 이름을 담는다) | 부록 C-4 (전수) |
| **N13** | `orphan-check` 총 참조 = **10지점 / 10줄** (inbound 7 · outbound 3) | 부록 C-5 |
| **N14** | fin `.claude/` **16파일 전부** `git rm` 시 활성 파급 = **6건** (`CLAUDE.md` 62·118·128·140·145 + `route.ts:5`) | 부록 C-6 · [감사] 정정 ⑥ |
| **N15** | #8 수정 표면 = **58줄** (P척도 fin 22 + fit 30 = 52 · 릴리즈 절차 fin 3 + fit 3 = 6) | 부록 B-4 (줄 번호 52개 전수) |
| **N16** | H-0 범위 = **체크아웃 게이트 8줄 + `/tmp` 6줄 = 14줄** · **grep 문구 6곳** | 부록 C-7 |
| **N17** | 하네스를 부르는 auto memory = **13파일** (fin 9 · fit 2 · ple 2). 세션 동작을 좌우하는 **트리거 4줄** | [감사] 정정 ④ 열거 (fin 9파일 실명) · [참조] §3 |
| **N18** | 이름 충돌 런타임: **rule 3벌 전부 공존** (workflow 3 · api-routes 2 · components 2) · **agent 는 1개만 남고 마지막 `--add-dir` 이 이긴다** | [감사] §1 주장 3 (순서 반전 2회) |
| **N19** | `--add-dir`: skills·agents **env 불요** · rules·CLAUDE.md **env 필요** · **방향 대칭** | [MF] Q37 절 4행 표 |
| **N20** | fit 하네스 민감 문자열 **0건** (무해 2: curl UA · loopback URL) | [MF] Q33 절 |
| **N21** | `settings.local.json` 저장소 무관 규칙 = fin **136/150(90.7%)** · fit **90/105(85.7%)** | [내용] §5 |
| **N22** | myFinance · myFitness · pleiades **전부 PUBLIC** | 부록 B-5 |

### 3-2. 2회차 정정 블록

> **정정 A (감사 정정 ①).** 1회차는 [내용] §2 의 *공통줄 %* 를 인용해
> *"스킬 6쌍 14.6~34.5%"*, *"합칠 수 있는 쌍은 11쌍 중 2쌍"* 이라 적었다. **틀렸다.**
> 그 지표는 빈 줄과 위치 일치를 공통으로 세고, [내용] `:196` 이 스스로 *"빈 줄이 섞여 있으므로 상한"*
> 이라 경고한 것을 1회차가 버렸다. 비공백·정규화·중복제거 집합으로 재면 **N4** 다 —
> **실질 중복은 `rules/workflow` 1쌍(118줄)뿐이고 Codex 쌍의 공유 내용줄은 0**(`---` 1줄).
> 방향(*"병합이 아니라 재작성"*)은 유지되고 **강화**된다. 판정이 바뀌는 행은 §4-3 에 표시했다.

> **정정 B (감사 정정 ③).** 1회차는 fit → `workflow.md` 참조를 **5줄**로 세고 형태 C 의
> 참조 재작성을 **0줄**이라 적었다. 둘 다 틀렸다. 실제는 **N10 = 11줄**이고(전체 경로만 세고
> **맨이름 참조 6줄**을 놓쳤다), 그중 **N11 = 6줄**이 `8절`·`8-1`·`8-6`·`§8`·`§7~8`·`10단계 목록(UI 디자인 포함)`
> 을 인용하는데 pleiades 는 그 번호를 재배치했다([내용] §2-2).
> **다만 2회차에서 이 비용의 귀속이 바뀐다** — Q31 로 원본이 **유지**되면 그 참조들은
> **자기 저장소의 문서를 그대로 가리키므로 재작성이 0줄**이다. N11 은 이제 **원본을 축약하는 안(W-1)의 비용**이다.

> **정정 C (감사 정정 ⑤에 대한 재정정).** 감사는 1회차의 13줄에 `CLAUDE.md:117` 을 더해 **14줄**이라 했다.
> **지점은 14가 맞으나 줄은 13이다** — `myfitness-orchestrator/SKILL.md:32` 한 줄이
> `codex-review-loop` 와 `orphan-check` **두 이름을 함께** 담아 양쪽 목록에 중복 계상됐다(부록 C-4).
> 1회차의 13은 **틀린 구성으로 우연히 맞은 값**이다. **N12 = 지점 14 · 줄 13.**

> **정정 D (감사 §5(2) 에 대한 재정정).** 감사는 `repos/myFitness/CLAUDE.md` 를 *"stale 사본"* 이라 했다.
> **현재는 stale 이 아니다** — 원본과 `diff` **0줄**, 크기·mtime 동일(부록 B-8).
> 정확한 서술은 *"untracked·ignored 사본이라 원본이 바뀌면 **조용히** 어긋난다"* 이다. 지적의 실질은 유효하다.

---

## 4. 경로

### 4-1. 처분 기준 — 4축 (2회차: 축 3 을 측정으로 교체, 축 4 신설)

| 순 | 축 | 판정 | 근거 |
|---|---|---|---|
| 1 | **도메인 지식** (도메인 용어 ≥ 3) | **유지** — 파라미터가 아니라 지식이다 | N3-a |
| 2 | **참조 결합** — 유지 확정 파일이 그것을 부르거나 그것이 유지 파일을 부른다 | **유지** (클러스터 무결성) | N13 |
| 3 | **세션 귀속** — 대상 저장소 단독 세션인가 pleiades 통합 세션인가 | 단독 → **유지** / 통합 → **역할 이관** | N19 (방향 대칭 확인) |
| 4 | **위임 여부** — pleiades 정본이 그 파일에 절차를 위임하고 있나 | **유지** — 위임 대상을 없애면 위임이 끊긴다 | **N8 · N6 · N7** |

> **축 4 가 2회차의 신설이고 결론을 바꿨다.** pleiades `workflow.md:165` 한 줄이
> 단독 작업 절차 전체를 원본에 위임하고 있으며, 그 위임 대상에 **(c) 35줄이 정확히 들어 있다**(N7·N8).
> 1회차는 이 고리를 §6 Q31 에서 *"폐기하면 위임 대상이 사라진다"* 로 **적어놓고도 처분은 폐기로 냈다.**
> 측정이 그 모순을 해소했다.

> **축 3 의 대칭 가정은 이제 미측정이 아니다.** N19 ③ — 대상 저장소 cwd 세션도 `--add-dir` 로
> pleiades 하네스를 본다. 즉 **한 파일이 두 세션 중 한쪽에서만 산다는 1회차의 서술은 과했다** —
> 정확히는 **플래그 없이는 한쪽에서만 산다.**

### 4-2. 파일별 처분 — 전수 37행

LOC·결합/도메인 열은 [내용] §1·§3 인용이며 [감사] 확인 1·8 에서 **36행 전수 대조 오류 0**.
**2회차에서 처분이 바뀐 행은 굵게 표시하고 사유를 적었다.**

#### myFinance (17행)

| # | 파일 | LOC | 결합/도메인 | **처분** | 근거 (축) · 2회차 변경 |
|---|---|---:|---|---|---|
| 1 | `agents/feature-implementer.md` | 81 | 7 / 4 | 유지 | 축1 도메인 4 · 축2 fin 에이전트 허브 |
| 2 | `agents/quality-guardian.md` | 87 | 2 / 0 | 유지 | 축2 클러스터 |
| 3 | `agents/release-manager.md` | 103 | 3 / 0 | 유지 | 축2 클러스터 · 이름 충돌(§4-5) |
| 4 | `agents/spec-planner.md` | 51 | 4 / 1 | 유지 | 축2 클러스터 |
| 5 | `rules/api-routes.md` | 119 | 2 / 0 | 유지 | 축3 fin 앱 규범 · `route.ts:5` 가 부른다 |
| 6 | `rules/components.md` | 11 | 3 / 2 | 유지 | 축1 |
| 7 | `rules/stock-trading-method.md` | 125 | 5 / 5 | 유지 | 축1 도메인 5 |
| 8 | `rules/tax-logic.md` | 28 | 7 / 6 | 유지 | 축1 도메인 6 |
| **9** | `rules/workflow.md` | 241 | 0 / 0 | **유지 + 부분 갱신** | **1회차 "폐기(대체)" 에서 변경.** 축4 — N8 위임 · N6 (c) 35줄. 갱신은 #8 25줄(§4-9) |
| **10** | `skills/codex-response-patterns/SKILL.md` | 87 | 6 / 1 | **유지** (pleiades 는 신규 작성) | **1회차 "이관(재작성 병합)" 에서 변경.** N4 — fit 짝과 공유 내용줄 0. 병합 대상이 아니다 |
| 11 | `skills/milestone-workflow/SKILL.md` | 123 | 3 / 0 | 유지 | 축2 fin 4에이전트 실행 엔진 |
| 12 | `skills/project-spec-writer/SKILL.md` | 110 | 3 / 0 | 유지 | 축2 · 산출물이 `decision-doc` 과 다르다 |
| 13 | `skills/project-verify/SKILL.md` | 98 | 5 / 0 | 유지 | 축2 · 축3 |
| 14 | `skills/release-publisher/SKILL.md` | 134 | 6 / 0 | 유지 | 축2 · 축3 |
| 15 | `skills/session-boundary/SKILL.md` | 133 | 2 / 0 | 유지 | 축3 — `pleiades-handoff` 가 갖지 않은 Step 3개 |
| 16 | `skills/session-resume/SKILL.md` | 98 | 4 / 0 | 유지 | 축3 · fin `MEMORY.md` 트리거(N17) |
| 17 | `settings.local.json` *(gitignored · 34 밖)* | 156 | 20 / 1 | 유지 + 부분 이식 보류 | N21 · Q36 |

#### myFitness (18행)

| # | 파일 | LOC | 결합/도메인 | **처분** | 근거 (축) · 2회차 변경 |
|---|---|---:|---|---|---|
| 18 | `agents/codex-liaison.md` | 45 | 1 / 0 | 유지 | 축2 클러스터 |
| 19 | `agents/db-migrator.md` | 45 | 3 / 0 | 유지 | 축3 Prisma 운영 |
| 20 | `agents/ops-analyst.md` | 45 | 9 / 0 | 유지 | 축3 pm2 |
| 21 | `agents/release-manager.md` | 46 | 1 / 0 | 유지 | 축2 · 이름 충돌 |
| 22 | `agents/workflow-conductor.md` | 50 | 2 / 0 | 유지 | 축2 fit 허브 |
| 23 | `rules/api-routes.md` | 8 | 0 / 0 | 유지 | 축3 fit 앱 규범 (fin 동명 파일과 공유 2줄 — N4) |
| 24 | `rules/components.md` | 9 | 2 / 2 | 유지 | 축1 |
| **25** | `rules/workflow.md` | 264 | 1 / 1 | **유지 + 부분 갱신** | **1회차 "폐기(대체)" 에서 변경.** 9행과 동일 사유. 갱신은 #8 33줄 |
| 26 | `settings.local.json` | 111 | 20 / 1 | 유지 + 부분 이식 보류 | N21 · Q36 |
| 27 | `skills/branch-workflow/SKILL.md` | 175 | 5 / 0 | 유지 | 축2 · 축3 |
| **28** | `skills/codex-review-loop/SKILL.md` | 113 | 4 / 0 | **유지** (pleiades 는 신규 작성) | **1회차 "이관(재작성 병합)" 에서 변경.** 10행과 동일 사유 (N4 = 1줄) |
| 29 | `skills/myfitness-orchestrator/SKILL.md` | 119 | 8 / 0 | 유지 | 축2 fit 5에이전트 |
| 30 | `skills/ops-diagnose/SKILL.md` | 155 | 37 / 10 | 유지 | 축1 도메인 10 |
| **31** | `skills/orphan-check/SKILL.md` | 113 | 0 / 0 | **이관 — 형태는 Q29** | 유일하게 축1·축3·축4 를 전부 통과. **단 축2 로 N13 = 10지점**이 걸린다 |
| 32 | `skills/prisma-drift-fix/SKILL.md` | 139 | 11 / 0 | 유지 | 축3 |
| 33 | `skills/release-flow/SKILL.md` | 223 | 4 / 0 | 유지 | 축3 |
| 34 | `skills/session-handoff/SKILL.md` | 122 | 3 / 1 | 유지 | 축3 |
| 35 | `skills/session-primer/SKILL.md` | 103 | 9 / 2 | 유지 | 축3 |

#### `CLAUDE.md` 2행

| # | 파일 | LOC | 결합/도메인 | **처분** | 근거 |
|---|---|---:|---|---|---|
| 36 | fin `CLAUDE.md` | 163 | 44 / 21 | **유지 · 갱신 0줄** | 축1. **2회차 변경** — 9·10행이 유지되므로 `:128`·`:145` 갱신이 불필요해졌다 |
| 37 | fit `CLAUDE.md` | 173 | 32 / 19 | **유지 · `orphan-check` 를 형태 A·C 로 옮길 때만 `:117` 1줄** | 축1 · N13 |

#### 4-2-1. 처분 집계 (N2 의 산출)

| 처분 | 파일 | LOC | 산출식 |
|---|---:|---:|---|
| **이관** | 1 | **113** | 113(#31) |
| **유지** | 33 | **3,401** | fin 1,629 + fit 1,885 − 113 = 3,401 |
| **합** | **34** | **3,514** | 113 + 3,401 = 3,514 ✅ |

파일 검산: fin 유지 16 + 이관 0 = **16** ✅ · fit 유지 17 + 이관 1 = **18** ✅.

> **이 집계는 형태 A·C 기준이다 (3회차 명시 · 감사 2회차 정정 ③의 연장).** *이관* 은
> **역할이 pleiades 로 온다**는 뜻이고, **원본에서 파일이 빠지는지는 형태(Q29)가 정한다.**
> **권고 형태 B(복사)에서는 원본 34파일이 전부 남고** pleiades 에 113 LOC 사본이 하나 생긴다 —
> 그래서 §4-7 의 fit 노출이 **18파일 / 1,885 LOC** 이다. A·C 를 택하면 그 값이 17 / 1,772 로 내려간다.
1회차 대비: 이관 3→**1**(−2) · 폐기 2→**0**(−2) · 유지 29→**33**(+4). LOC 이관 313→**113** · 폐기 505→**0**.

> **"공통은 pleiades 로" 가 파일 이동으로는 1파일밖에 되지 않는 이유**는 §4-3 이 설명한다.
> 결정이 무력화된 것이 아니라 **실현 형태가 파일 이동이 아니라 역할 이관**이라는 뜻이다 —
> pleiades 는 이미 자기 `workflow.md`(486줄)와 4에이전트·7스킬을 갖고 있고,
> 원본 34파일이 pleiades 세션에서 필요한 순간은 **`--add-dir` 로 해결된다**(N19).

### 4-3. 역할 쌍 — 판정 축을 **대장에 있는 값만으로** 재구성

> **정정 E (감사 2회차 정정 ①).** 2회차 §4-3 은 ③ *참조 지점* 열에 **대장에 없는 숫자 약 20개**를 새로 만들었고,
> 그중 `fin 40 · fit 18`(오케스트레이터)은 **어떤 계수법으로도 재현되지 않으며**(엣지 37/26 · agent 엣지 36/16 ·
> distinct 줄 31/17 · name grep 30/9), Codex 행의 `fin 2 · fit 5` 는 **fin 은 inbound+outbound, fit 은 inbound 전용**
> 으로 정의가 섞여 있었다. 그런데 판정 규칙(*"①없음 + ②0건 + **③낮음**"*)이 그 열에 걸려 있었고 **③의 임계값은 정의된 적이 없다.**
> **3회차는 ③ 열을 삭제한다.** 재현할 수 없는 숫자를 고쳐 쓰는 대신 **판정에서 뺀다** —
> 참조 결합은 이제 **처분이 아니라 형태(Q29)만** 가르고, 그 값은 대장 **N13**(전수 열거 부록 C-5) 하나뿐이다.

축은 셋이며 **전부 대장 값이다**: **① pleiades 에 그 역할이 있나**([내용] §4) · **② 리터럴 결합 등급**(N3-a·N3-b) ·
**③ 실질 공유 줄**(N4). **처분이 2회차에서 바뀐 행에 ▲ 를 붙였다.**

| 역할 | ① pleiades 역할 | ② 결합 등급 | ③ 공유 줄 (N4) | **처분** |
|---|---|---|---:|---|
| `rules/workflow` | **있음** (486줄) — 단 (c) 35줄 결손 (N6·N7) | 무파라미터(fin) / 파라미터(fit) | **118** | ▲ **원본 유지 + 부분 갱신** (1회차: 폐기) |
| Codex 대응 | **부분** — 절차는 9-3~9-6, 템플릿·루프 실행은 없음 | 파라미터화 | **1** | ▲ **원본 유지 · pleiades 신규 작성** (1회차: 이관 병합) |
| orphan 커밋 | **없음** | **무파라미터 (N3-b)** | 짝 없음 | **이관 — 형태는 Q29** |
| 세션 재개 | 있음 (`pleiades-resume`) — 교집합 1기능 | 파라미터화 | 3 | 양쪽 유지 |
| 세션 인계 | 있음 — pleiades 5절 중 대응 3절 | 파라미터화 | 3 | 양쪽 유지 |
| 오케스트레이터 | 있음 — 라우팅 대상 에이전트가 다르다 | 파라미터화 | 2 | 양쪽 유지 |
| 릴리즈 | 부분 (`workflow.md` 릴리즈 절) | 파라미터화 | 6 | 양쪽 유지 |
| 브랜치 | 부분 (브랜치 전략 절) | 파라미터화 | 2 | 양쪽 유지 |
| `agents/release-manager` | 없음 | 파라미터화 | 4 | 양쪽 유지 |
| `rules/api-routes` | 없음 | 무파라미터(fit) / 파라미터(fin) | 2 | 양쪽 유지 |
| `rules/components` | 없음 | 파라미터화 | 4 | 양쪽 유지 |

**판정 규칙 (3회차 · 대장 값만으로 유도한다):**

1. **출발점 = N3-b** — 리터럴 참조 0건 파일 **3개** (fin `rules/workflow` · fit `rules/api-routes` · fit `skills/orphan-check`).
2. **축4(위임)가 fin `rules/workflow` 를 뺀다** — pleiades `workflow.md:165` 가 단독 작업을 그 파일에 위임하고 그 대상이 (c) 35줄이다 (**N8 · N6 · N7**). fit `rules/workflow` 는 ②가 0건이 아니라 처음부터 후보 밖이다.
3. **축3(세션 귀속)이 fit `rules/api-routes` 를 뺀다** — fit **앱 코드**의 라우트 규범이라 fit 소스를 쓰는 세션에서만 의미가 있다. `orphan-check` 는 git 절차라 저장소에 무관하다.
4. **남는 것은 `orphan-check` 1개.** **참조 결합(N13 = 10지점)은 이 판정에 쓰이지 않고 형태(Q29)만 가른다.**

> **Codex 쌍의 처분이 바뀐 이유.** 공유 내용줄이 **1(`---`)**(N4)이므로 "두 벌을 합친다"가 성립하지 않는다.
> pleiades 에 없는 것은 **템플릿과 루프 실행 절차라는 역할**이지 그 텍스트가 아니다. 따라서 pleiades 는
> 두 문서를 **입력으로 새로 쓰고** 원본 2파일은 각 저장소의 단독 세션에 남는다 —
> `git rm` 도 참조 재작성도 없으므로 **되돌리기 즉시**다. 산출물의 **이름 제약은 §4-5** 에 있다.

### 4-4. 이관 형태 — `orphan-check` 1파일에만 적용된다 (Q29)

1회차의 A/B/C 사다리는 유지하되 **대상이 5파일에서 1파일로 줄었고**, N19(방향 대칭)가 C 를 실행 가능하게 만들었다.

| 안 | 내용 | fit 단독 세션 | **되돌리기 등급** | 되돌리는 행위 | 재작성 |
|---|---|---|---|---|---|
| **A · 이동** | pleiades 로 옮기고 fit 원본에서 삭제 | **잃는다** — inbound 7지점이 dangling | **편도** | fit `.claude/` 는 git 이력이 없다. pleiades 사본에서 되복사해야 한다 | **N13 = 10줄** |
| **B · 복사** (권고) | pleiades 에 두고 fit 원본도 그대로 | 그대로 동작 | **즉시** | pleiades 쪽 파일 삭제 | **0줄** |
| **C · 이동 + 포인터 스텁** | fit 에 포인터만 남긴다 | `--add-dir ~/workspace/pleiades` + **환경변수**를 fit 세션에도 요구한다(N19) | **중간** | pleiades 텍스트를 되복사 | 10줄 + fit 단독 세션 운영 규약 |

**2회차 권고는 B 다.** 1회차는 C 를 권고했는데, 근거였던 *"C 는 참조 재작성 0줄"* 이
정정 B 로 무너졌고(그건 스텁 경로가 살아 있는 `workflow.md` 얘기였다), `orphan-check` 는
**113줄짜리 단일 스킬**이라 두 벌이어도 관리 부담이 작다. 무엇보다 **A·C 는 fit 쪽 되돌리기가
편도·중간인데 B 는 즉시**다 — 되돌리기 비용 순 원칙(`CLAUDE.md` 핵심 전제 2)이 B 를 가리킨다.

> **B 의 대가는 명시한다: 113줄 두 벌.** 이것은 이 문서에서 **유일한 실제 중복**이다.
>
> **정정 F (감사 2회차 정정 ⑦).** 2회차는 *"전환은 언제든 가능하고 **비용이 오르지 않는다**"* 라고 적었다.
> **이 문서 자신의 N4 와 모순된다** — 두 저장소가 같은 하네스를 복사해 갈라진 결과가 **공유 ≤6줄**이었고,
> 그래서 이 문서는 *"병합이 아니라 재작성"* 이라 결론지었다(§5 2행). 형태 B 는 그 실험을 다시 돌리는 것이다.
> 지금 A·C 로 가면 두 벌이 아직 같아 **재조정 0**이지만, 갈라진 뒤에는 **N4 가 "재작성"이라 부른 작업**이 더해진다.
> **정확한 서술: B 는 지금 가장 싸고, 전환을 미룰수록 비싸진다. 그 대가를 알고 택한다.**
> 권고 B 자체는 유지된다 — 근거는 **되돌리기 즉시** · 단일 113줄 스킬 · `CLAUDE.md` 핵심 전제 2 이지 "나중에 싸다"가 아니다.

### 4-5. 이름 충돌 — 종류가 둘이고, 위험한 쪽은 rule 이다

> **정정 (감사 정정 ⑧).** 1회차는 *"처분으로 충돌 4건이 전부 소멸"*, *"우선순위 미측정"* 이라 적었다.
> **우선순위는 측정됐다(N18).** 그리고 *"소멸"* 은 **pleiades `.claude/` 네임스페이스에 한정**된 진술이며,
> 그 상태는 §4-6 이 *"유지 33파일이 보이지 않는 상태"* 라고 규정한 바로 그 구성이다.
> **소멸은 처분의 성과가 아니라 부착하지 않았다는 사실의 재기술이다.**

| 충돌 | 부착하지 않으면 | **`--add-dir` 로 둘 다 붙이면** |
|---|---|---|
| `workflow` (rule, 33줄) | pleiades 단독 | **3벌 전부 로드 — 드롭 없음.** 모순되는 규범이 동시에 규범이 된다 |
| `api-routes` · `components` (rule, 9줄) | 올라오지 않음 | **각 2벌 공존** |
| `release-manager` (agent, 25줄) | 올라오지 않음 | **1개만 남고 마지막 `--add-dir` 이 이긴다 — 나머지는 조용히 사라진다** |
| **skill 층 (H-1 이후)** | H-1 산출물이 pleiades 에만 있으면 **0** | **이름을 겹치게 지으면 5번째 충돌.** skill 충돌 동작은 **미측정**(N18 은 rule·agent 만 쟀다) — **Q41** |

> **정정 G (감사 2회차 정정 ⑥).** 2회차 §4-5 표는 **H-1 이 만들 문서를 계산에 넣지 않았다.**
> [참조] 의 *"skill 23개는 이름이 전부 달라 충돌 0"* 은 **H-1 이전 상태의 값**이다.
> pleiades `.claude/skills/` 에는 현재 `codex-*` 이름이 없으므로(`decision-doc` · `dual-repo-change` ·
> `pleiades-handoff` · `pleiades-orchestrator` · `pleiades-resume` · `repo-measure` · `reversibility-audit`),
> **H-1 산출물을 `codex-response-patterns` 나 `codex-review-loop` 로 이름 지으면 skill 층에 첫 충돌이 생긴다.**
>
> **H-1 의 이름 제약 (3회차 신설):** 산출물 이름은 **fin·fit 의 기존 skill·agent 이름 어느 것과도 겹치지 않아야 한다.**
> fin skill 7 · fit skill 9 · fit agent 5 · fin agent 4 를 전부 피한다 — 예컨대 `codex-liaison-patterns` 는
> **fit agent `codex-liaison` 과 겹치므로 불가**다. 겹치지 않는 이름을 쓰면 **Q41 측정은 불필요**하고,
> 굳이 겹치는 이름을 쓰려면 **skill 충돌 동작 런타임 측정 1회가 선결**이다.

**rule 쪽이 더 위험하다.** agent 는 *사라져서* 눈에 띌 여지라도 있지만, rule 은 **사라지지 않고
서로 어긋나는 절차 3벌이 동시에 활성**된다 — pleiades 7절 표(base `integration/pleiades`)와
fin·fit 의 `feat/<issue>-<n>` → `dev` 가 같은 세션에서 규범이 된다.

| 부착 정책 | 충돌 | **되돌리기** | 되돌리는 행위 |
|---|---|---|---|
| **한 번에 한 저장소만** (권고) | rule 2벌 공존(그 저장소 + pleiades) · agent 0 | **즉시** | 플래그 하나 제거 |
| 둘 다 + fit agent 개명 | rule 5벌 · agent 0 | **즉시** | 문자열 되돌림 (fit 7줄) |
| 둘 다 방치 | **rule 7벌 · agent 1개 조용한 드롭** | 즉시 | 플래그 제거 |

**한 저장소만 붙여도 rule 은 2벌이 된다** — 이것은 회피 불가이며, `--add-dir` 를 쓰는 모든 세션의 상수다.
완화는 래퍼가 그 사실을 세션 시작 시 알리는 것뿐이다(§4-6 ④).

### 4-6. "저장소에 유지"의 운영 비용 (Q30)

N19 로 로딩 규칙이 확정됐다. **유지 33파일(3,401 LOC)은 다음 없이는 pleiades 세션에 존재하지 않는다:**

```bash
CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1 \
  claude --add-dir ~/workspace/myFinance      # 한 번에 한 저장소 (§4-5)
```

- **환경변수를 빼면 skills·agents 는 보이지만 rules·CLAUDE.md 는 보이지 않는다**(N19 ①②).
  즉 `api-routes.md`·`tax-logic.md` 같은 **규범이 조용히 빠진 채 스킬만 도는 상태**가 만들어진다 —
  가장 나쁜 실패 형태다. **래퍼는 환경변수를 항상 켜야 한다.**
- 대상은 **항상 원본**이다. fit `.claude/` 는 worktree 에 없고 fin 것은 중첩이라 로드되지 않는다.
- **`repos/myFitness/CLAUDE.md`** 는 원본과 지금 바이트 동일하지만 untracked·ignored 사본이라
  앞으로 조용히 어긋난다(정정 D). worktree 를 cwd 로 열면 이 사본이 로드된다.

| 완화 선택지 | **되돌리기** | 되돌리는 행위 | 남는 비용 |
|---|---|---|---|
| ① 현행 — pleiades `CLAUDE.md` 안내만 | **즉시** (변경 0) | — | 세션마다 수동 · `--resume` 마다 재입력 |
| ② 셸 별칭 | **즉시** | 별칭 1줄 삭제 | 버전 관리 밖 — 리뷰 불가 |
| ③ pleiades tracked 래퍼 스크립트 | **즉시** | 파일 삭제 | PR·리뷰 대상이 된다 |
| **④ 저장소별 부착 래퍼** (권고) | **즉시** | 파일 삭제 | ③ + 인자 규약. **환경변수 고정 · 한 번에 하나 · rule 2벌 경고 출력**을 한곳에 담는다 |

### 4-7. fit 하네스가 버전 관리 밖이라는 사실 (Q32)

fit `.claude/` tracked 0 · `CLAUDE.md` ignored · **커밋 이력 없음**([감사] 확인 6).
→ **"fit 특수 하네스를 저장소에 유지"는 "버전 관리 밖, 원본 워킹 디렉터리에만 유지"를 뜻한다.**
**3회차 권고(형태 B)에서 fit 유지 대상은 18파일 / 1,885 LOC** 이므로 노출은 fit `.claude/` 전체다 (형태 A·C 면 17파일 / 1,772 LOC).

| 안 | **되돌리기** | 되돌리는 행위 | 비용·리스크 |
|---|---|---|---|
| **i · 그대로** | **즉시** (변경 0) | — | fit 유지 **18파일** 무보호(권고 B 기준). #8 수정(33줄)도 이력이 남지 않는다 |
| **ii · fit tracked 화** (`.gitignore:35,36` 2줄) | **중간** | revert 커밋 — 단 push 된 이력엔 영구히 남는다 | fit 소스 변경 = 모드 S · 승인 게이트 · 이슈는 fit 저장소. **Q33 해소(N20)로 공개 장애물은 사라졌다** |
| **iii · pleiades 사본** | **즉시** | 디렉터리 삭제 | 18파일 두 벌 드리프트. 공개 범위는 ii 와 같고 **N20 으로 무해 확인** |

> **2회차 변경: Q33 이 해소되어 ii·iii 가 실행 가능해졌다.** 1회차는 *"민감 문자열 미측정이라 판단 보류"*
> 였다. N20 이 **비밀·자격 증명·개인 식별자 0건**을 확인했고, 남는 노출은 경로의 사용자명뿐인데
> 그것은 fin `.claude/` 가 **이미 PUBLIC 으로 공개 중인 것과 같은 수준**이다(N22).
> **권고: ii.** 근거는 되돌리기가 아니라 **되돌릴 수 없는 쪽을 없애는 것**이다 —
> i 을 유지하는 한 fit 하네스에 대한 모든 변경(§4-9 의 33줄 포함)이 편도로 남는다.

### 4-8. fin `git rm` (Q21 / Q35) — **2회차에서 범위가 거의 사라졌다**

| | 1회차 | **2회차** |
|---|---|---|
| 범위 | `rules/workflow.md` + `skills/codex-response-patterns/` = 2파일 / 328 LOC | **0파일** (권고 B) 또는 **0파일** (9·10행 모두 유지) |
| 갱신 | fin `CLAUDE.md:128,145` + `session-boundary:49` | **0줄** |
| 모드 | 모드 I · 승인 게이트 · 에이전트 리뷰 필수 | **해당 없음** |

**Q21 에 대한 2회차의 답: 지우지 않는다.** fin `.claude/` 16파일은 tracked 인 채로 남고,
pleiades 세션에서 보이지 않는 것은 `--add-dir` 로 해결한다.

> **N14(활성 파급 6건)는 여전히 유효한 값이지만 이 문서의 경로에는 쓰이지 않는다.**
> **16파일 전부를 지우는 가상 시나리오의 비용**이며, `CLAUDE.md` **5줄**(62·118·128·140·**145**)과
> `route.ts:5` 로 구성된다. 1회차는 §3-1 에 서베이어의 "4줄/5건"을, §4-2·§4-8 에 "145 포함"을 실어
> **같은 문서 안에서 두 값이 충돌**했다(감사 정정 ⑥). **대장 N14 에 한 번만 둔다.**

> **정정 (감사 정정 ⑨).** 1회차 §5 3행은 *"에이전트 리뷰 필수의 **근거가 바뀐다**"* 라 적었다.
> **바뀌지 않았다** — [참조] 가 이미 (a) `route.ts:5` 를 *"주석"* 이라 적었고 (b) 리뷰 필수의 근거로
> 9-0 표의 *"경로 무관"* 을 들었다. 1회차가 새로 밝힌 것은 **`git rm` 범위를 좁히면 그 1건이 범위 밖**
> 이라는 사실 하나뿐이고, 2회차에서는 그 범위마저 0이 되어 **논점 자체가 사라졌다.**

### 4-9. #8 처리 — 2회차에서 더 중요해졌다 (Q34)

수정 표면 **N15 = 58줄**. **원본 `workflow.md` 2파일이 영구히 유지되므로**(§4-2 9·25행),
1회차가 걱정한 *"어차피 지울 파일을 고치는 낭비"* 는 **완전히 사라졌다.**

| 안 | fin | fit | **되돌리기** | 되돌리는 행위 | 판정 |
|---|---|---|---|---|---|
| **a · 원본 `dev` 단독 작업 PR** | 가능 (모드 S · 이슈는 `fomalhaut84/myFinance`) | **불가** — 파일이 git 밖 | fin **즉시** | `git revert` 1회 | **fin 채택** |
| **a-2 · fit 로컬 파일 직접 편집** | — | 유일한 수단 | **편도** (Q32-i 일 때) / **즉시**(Q32-ii 이후) | ii 이후엔 `git revert` | **Q32 를 먼저 답하면 등급이 내려간다** |
| b · 통합 브랜치에 넣어 1a 와 함께 | 가능 | 불가 | 중간 | revert 커밋 | 활성 결함을 1a 완료까지 방치 |
| c · pleiades 것만 고치고 방치 | — | — | 즉시 | — | pleiades 는 이미 두 결함이 없다 = 아무것도 안 함 |

**권고: fin = a(즉시 착수) · fit = Q32-ii 선행 후 a-2.**
**양쪽 다 에이전트 사전 리뷰가 필수다** — 판정과 근거는 §4-13 단계 표의 9-0 열에 한 번만 적었다.
**#8 결함 ②는 지금도 활성이고**(두 저장소의 모든 Codex 리뷰에서 최고 심각도를 버리라는 지시),
2회차에서는 그 파일이 **영구 유지**되므로 방치 기간에 끝이 없다.

### 4-10. #10 처리 + 룰 문구 추가안 (⑥ · 감사 §5(5))

**N16 = 14줄 + 6곳.**

| 항목 | 대상 줄 | 수정 방향 |
|---|---|---|
| ① 낡은 체크아웃 게이트 (**8줄**) | `skills/repo-measure/SKILL.md:22,92,94` · `agents/repo-surveyor.md:24,70,72` · **`agents/reversibility-auditor.md:47`** · **`skills/reversibility-audit/SKILL.md:45`** | 경고를 **워킹트리 전용 데이터**(`du`·`node_modules` 크기)로 한정 |
| ② 공유 `/tmp` 임시파일 (**6줄**) | `repo-measure:117-119` · `repo-surveyor:84-86` (하네스 전체 전수 — [감사] 확인 후단) | 프로세스 치환 (부록 B-2) |

> **정정 (감사 정정 ⑦).** 1회차는 ①을 6줄로 적었다. 서베이어가 **측정 범위를 2파일로 한정**해서 잰 값이고,
> **감사 하네스 2파일에 같은 보일러플레이트가 있다.** 4곳만 고치면 *"측정 하네스는 새 규율, 감사 하네스는 옛 규율"* 이 된다.

**H-0 에 함께 넣을 룰 문구 2건 (옵션 · 둘 다 되돌리기 즉시 · `git revert` 1회):**

| | 넣을 곳 | 문구 | 근거 |
|---|---|---|---|
| **R1** (권고) | `.claude/rules/workflow.md` 2절 + `skills/repo-measure` 헤더 | *"**열거와 합계가 함께 있으면 합계를 신뢰하지 말고 열거를 센다.** 요약 숫자에는 열거의 출처(파일 · 절 · 행 범위)를 붙인다. **대장에 없는 숫자를 본문에 새로 만들지 않는다.** 같은 숫자를 문서 안에서 두 번 적지 않는다."* | #11 유형이 **3라운드 연속 6건**. §3-1 대장이 그 형식의 첫 적용례이고 **2회차에 #11 유형 0건**을 냈다. 마지막 문장은 **감사 2회차 정정 ①**(대장 밖 숫자 20개)의 재발 방지다 |
| **R2** | `.claude/rules/workflow.md` 4절 | *"실작업 시간 기록이 없는 단계는 **등급으로 갈음하고 그 사실을 적는다.**"* | 4절이 *"등급 + 행위 + 소요를 병기, 예외 없다"* 를 요구하는데 이 저장소에 시간 기록이 없다 |

### 4-11. grep 룰 정정 (⑦)

[내용] §7-① · 부록 B-3 재현. **정정 대상 6곳**(N16):
`CLAUDE.md:106` · `.claude/rules/workflow.md:91` · `agents/repo-surveyor.md:25` ·
`skills/repo-measure/SKILL.md:23` · **`agents/reversibility-auditor.md:20`** · **`skills/reversibility-audit/SKILL.md:18`**.

**추가할 규율 3택 (하나는 반드시):** **(a) `--no-ignore-files`** · **(b) `/usr/bin/grep`** · **(c) 경로 직접 지정.**
**그리고 `git grep` 은 인덱스만 보므로 ignored 경로에는 원리상 쓸 수 없다** — fit `.claude/` 가 그 경우다.

**004 Q22 의 재검증 범위**는 *"ignored 경로를 포함하는 모든 grep 측정"* 으로 넓어진다 → **Q38**(이 PR 범위 밖).

### 4-12. auto memory (⑧)

**N17 = 13파일 · 트리거 4줄.** 2회차에서 **조치 대상이 4줄에서 2줄로 줄었다** — 9·10·25·28행이
유지로 바뀌면서 그것들을 가리키던 memory 줄이 stale 해지지 않는다.

| 대상 | 2회차 처분의 영향 | 조치 |
|---|---|---|
| fin `MEMORY.md:4,5,7` · ple `MEMORY.md:4` (트리거 4줄) | 없음 — 전부 유지 대상 | 불필요 |
| fin `project_briefing_no_tool_detection.md:45` (`codex-response-patterns`) | **없음** (1회차: stale) — 10행이 유지로 바뀜 | 불필요 |
| fin `feedback_always_follow_workflow.md:3` · fit `feedback_review_policy.md:41` (`workflow.md`) | **없음** (1회차: stale) — 9·25행이 유지로 바뀜 | 불필요 |
| fit `feedback_release_merge_commit.md:20` (`release-flow`) | 없음 | 불필요 |
| **fin `feedback_session_management.md:59`** | 처분과 무관하게 **이미 틀렸다** (fin `.claude/` 를 untracked 로 기술 — 실제 tracked 16) | **정정 1줄** |
| **fit `CLAUDE.md:117` 계열 memory** | `orphan-check` 이관(형태 B)이면 영향 없음. A·C 면 재확인 | **Q29 확정 후 판단 1건** |

**→ 사용자 세션 작업 = 확정 1줄 + 조건부 1건.** memory 는 git 밖이라 어떤 PR 로도 고칠 수 없다.

### 4-13. 단계 표 · 되돌리기 · 작업 표면

| 단계 | 내용 | 여기서 멈추면 남는 것 | **9-0 리뷰 판정** | **등급** | 되돌리는 행위 | **작업 표면** |
|---|---|---|---|---|---|---|
| **H-0** | pleiades 룰 정정 — #10 14줄 + grep 6곳 + R1·R2 | 측정·감사 하네스가 같은 규율을 갖는다. 이관과 독립 | self-review (하네스 `.claude/**`) | **즉시** | `git revert` 1회 | **N16 = 14줄 + 6곳** + 룰 문구 2건 |
| **H-1** | pleiades 신규 작성 — Codex 대응 문서 1개 (fin 87 + fit 113 을 **입력으로**). **이름 제약 §4-5** | pleiades 세션이 Codex 응답 패턴·루프 절차를 갖는다. **원본 무변경** | self-review (pleiades `.claude/**`) | **즉시** | pleiades 쪽 파일 삭제 | 입력 **200 LOC / 2파일**. **산출 LOC 는 산출하지 않는다** |
| **H-1b** | `orphan-check` 를 pleiades 로 (형태 B = 복사) | pleiades 가 orphan 검사를 갖는다. fit 무변경 | self-review (pleiades `.claude/**`) | **즉시** | pleiades 쪽 파일 삭제 | **113 LOC 복사 · 참조 재작성 0줄** (형태 A·C 면 **N13 = 10줄**) |
| **H-2 (fin)** | `git rm` | — | — | — | — | **범위 0 — 2회차에서 소멸** (§4-8) |
| **H-2 (fit)** | 원본 파일 삭제·축약 | — | — | — | — | **범위 0** (형태 B 권고) |
| **H-3 (fin)** | #8 fin 25줄 — 원본 `~/workspace/myFinance` · base `dev` · **모드 S** · 이슈는 `fomalhaut84/myFinance` | fin 의 활성 리뷰 결함이 사라진다 | **에이전트 사전 리뷰 필수** — 9-0 *"대상 저장소 변경 — 경로 무관"* | **즉시** | `git revert` 1회 | **25줄** (P 22 + 릴리즈 3) |
| **H-3 (fit)** | #8 fit 33줄 — 원본 로컬 편집 (PR 경로 없음 · §4-9 a-2) | fit 의 활성 리뷰 결함이 사라진다 | **에이전트 사전 리뷰 필수** — 같은 행 (PR 이 없어도 대상 저장소 변경이다) | **Q32-i 면 편도 · ii 이후 즉시** | i: 복원 소스 없음 / ii: `git revert` | **33줄** (P 30 + 릴리즈 3) |
| **H-4** *(권고 · Q32-ii)* | fit `.gitignore:35,36` 2줄 제거 + `git add .claude CLAUDE.md` — **모드 S · 승인 게이트 · 이슈는 `fomalhaut84/myFitness`** | fit 하네스 **18파일**이 이력을 갖는다. **H-3(fit) 의 등급이 내려간다** | **에이전트 사전 리뷰 필수** — fit 소스(`.gitignore`) 변경 | **중간** | revert 커밋 (push 된 이력엔 남는다) | **2줄 + 18파일 tracked 화** |
| **H-5** *(조건부 · Q30)* | `--add-dir` 저장소별 래퍼 (pleiades `bin/`) | 유지 33파일이 1명령으로 보인다 | self-review (config 소규모) | **즉시** | 파일 삭제 | **1파일** |

> **정정 H (감사 2회차 정정 ④ · T6).** 2회차 단계 표에는 **에이전트 사전 리뷰 필수 표기가 한 곳도 없었다.**
> 1회차에서 그 표기를 달고 있던 것은 H-2 하나였는데, Q31 로 **H-2 가 소멸하면서 표기도 함께 사라졌다.**
> H-3(fin) 은 실서비스 저장소 `myFinance` 의 `dev` 로 PR 을 열고, H-3(fit)·H-4 는 원본 fit 을 고친다 —
> `.claude/rules/workflow.md` 9-0 표의 **"대상 저장소 변경 — 경로 무관(`repos/**` 또는 원본 `~/workspace/myF*`) → 에이전트 필수"**
> 행에 정확히 걸린다. 그 행은 *"이전 행이 `repos/**` 만 걸어 모드 S·H 가 리뷰 필수에서 빠졌다"* 는 결함을
> **이미 한 번 고친 이력**이 있고, 이 문서가 같은 실수를 반복할 뻔했다.

> **정정 I (감사 2회차 정정 ⑤ — 이미 닫혔다).** 2회차 H-0 범위에는 `docs/research/measured-facts.md` 가 없었고,
> 그 정본은 `H4 활성 5건` · `H5 11파일` 이라는 **정정 전 값**을 갖고 있었다.
> `.claude/rules/workflow.md` 2절이 *"`measured-facts.md` 를 먼저 읽고 있는 값은 재측정하지 않는다"* 를 요구하므로,
> 그대로면 다음 세션이 옛 값을 읽는다. **오케스트레이터가 커밋 `2dfcf6f` 로 정정 블록을 append 했다**
> (`## 정정 — 하네스 참조 그래프 실측 H4·H5`, N14=6 · N17=13). **따라서 H-0 범위에 추가하지 않는다** —
> 이 문서는 그 블록을 참조만 한다. 대장 N14·N17 과 정본이 이제 일치한다.

**순서 제약 (2회차에서 하나로 줄었다):** **H-4 → H-3(fit).** tracked 화를 먼저 하면 fit 의 #8 수정이
편도에서 즉시로 내려간다. 1회차의 *"H-1 → H-2·H-3(fit)"* 제약은 **삭제가 사라져 소멸했다.**
H-0 · H-1b · H-3(fin) 은 서로 독립이며 어느 것을 먼저 해도 된다.
**H-1 만 선결이 하나 있다 — 산출물 이름(Q41).** 겹치지 않는 이름을 고르면 그 자리에서 해소되고,
겹치는 이름을 고르면 skill 충돌 런타임 측정 1회가 앞선다 (§4-5 정정 G).

#### 작업 표면 — 산출식 폐기

> **정정 (감사 정정 ②).** 1회차의 `H-1 = (87+113) × 1.60 + 113 = 433 LOC` 는 **성립하지 않는다.**
> 배율 1.60 은 **유니크 비공백 줄 공간**(215 → 344)에서 유도됐고 `87`·`113` 은 **raw `wc -l`** 이다.
> 두 공간이 달라 곱할 수 없다. 같은 선례를 raw 로 재면 486/(241+264) = **0.96** 으로 값이 뒤집힌다.
> 게다가 정정 A 로 **Codex 쌍은 공유 줄이 0** 이라 선례(공유 118줄 병합)와 **종류가 다르다.**
> **→ 산출 LOC 를 적지 않는다.** 시간에 적용한 처리(측정 근거 없음을 명시)를 LOC 에도 적용한다.

```
확정값 (전부 열거에서 나온 것 — 유도 0)
  H-0 = 14줄 + 6곳                (N16)
  H-1 입력 = 200 LOC / 2파일       (§4-2 10·28행)   · 산출 = 미산출
  H-1b = 113 LOC 복사 · 참조 0줄   (§4-4 형태 B)
  H-3 = 58줄 (fin 25 · fit 33)     (N15)
  H-4 = 2줄 + 18파일               (§4-7 ii)
  형태 A·C 를 택할 경우에만 추가 = 10줄  (N13 — 부록 C-4 ※ 재산출)
  원본 workflow.md 를 축약할 경우에만 추가 = 6줄  (N11)
```

> **시간 환산은 여전히 적지 않는다.** 이 저장소에 실작업 시간 기록이 없다.
> R2(§4-10)가 이 이탈을 규율 안으로 들인다. **미측정 — 요청: H-0 집행 시 실작업 시간을 기록해
> 줄당 환산율의 첫 표본을 만든다** (H-0 은 14줄 + 6곳으로 표본 크기가 작고 위험이 즉시 등급이다).

---

## 5. 판단이 바뀐 것

| # | 이전 서술 | 어디 | 지금 | 근거 |
|---|---|---|---|---|
| 1 | *"fit 18 + fin 16 을 함께 pleiades 로 이관"* | 004 Q21 | **파일 이동은 1개.** 33파일은 저장소에 남고 통합은 **역할 이관**으로 실현 | N2 · §4-3 |
| 2 | *"역할 쌍둥이라 합치면 한 벌"* | [MF] · 1회차 §4-3 | **10쌍 중 실질 중복 1쌍.** Codex 쌍은 공유 내용줄 **0** | **N4** (1회차의 "11쌍 중 2쌍"을 정정) |
| 3 | *"`git rm` 활성 파급 5건, 1건이 `src/` 실행 파일"* | [참조] | **6건**(N14)이고 그 1건은 **주석**이다. 리뷰 필수의 근거는 처음부터 9-0 "경로 무관"이었다 — **결론도 근거도 바뀌지 않았다** | [감사] 정정 ⑥⑨ |
| 4 | *"`--add-dir` 외에 영속 설정이 있을 수 있다"* | 004 Q20 | 없다. 그리고 **rules 는 환경변수까지 필요**하며 **방향은 대칭** | **N19** |
| 5 | *"이름 충돌 4건이 67줄을 모호하게 한다"* | [참조] §1-F | **종류가 둘이다** — rule 은 드롭 없이 3벌 공존, agent 만 조용한 드롭 | **N18** |
| 6 | *"#8 = 43줄"* | [참조] §6 | **58줄** (P 52 + 릴리즈 6) | N15 |
| 7 | *"결합도 0건 = 무파라미터 공통화"* | [내용] §3 | 리터럴 축의 값. `orphan-check` 는 **참조 10지점**으로 fit 에 묶여 있다 | N13 |
| 8 | *"#8 을 두 저장소에 대칭 적용"* | 이슈 #8 | **fit 은 PR 로 고칠 수 없다** | [감사] 확인 6 |
| **9** | *"pleiades `workflow.md` 가 원본을 대체한다"* | **1회차 §4-2 9·25행** | **불가.** (c) 35줄 · 위임 1줄 | **N5~N8** |
| **10** | *"Codex 쌍은 병합 가능"* | **1회차 §4-3** | **공유 내용줄 0 — 병합이 아니라 신규 작성** | **N4** |
| **11** | *"C(스텁)는 참조 재작성 0줄"* | **1회차 §4-4** | **최소 6줄**(N11). 단 2회차에서는 원본 유지로 이 비용이 **발생하지 않는다** | [감사] 정정 ③ |
| **12** | *"auto memory 조치 4줄"* | **1회차 §4-12** | **확정 1줄 + 조건부 1건** — 유지 처분이 stale 을 없앴다 | N17 · §4-12 |
| **13** | *"이관 판정 = ①없음 + ②0건 + **③낮음**"* | **2회차 §4-3** | **③ 을 판정에서 뺐다.** N3-b 3파일 → 축4(N8)가 fin `workflow` 를, 축3 이 fit `api-routes` 를 뺀다 → `orphan-check` 1개. **참조 결합은 형태만 가른다** | 정정 E |
| **14** | *"(c) 귀속은 공통 30 · fin 전용 5"* | **2회차 N7** | 출처에 귀속이 **둘**이고 그중 **줄 단위 fit 전용 4줄**이 누락됐었다. Q39 W-1 의 fit 잔여가 0으로 읽혔다 | 정정 ② · [W-diff] §5 |
| **15** | *"H-2 소멸 → 대상 저장소 리뷰 요건 없음"* | **2회차 §4-13** | **H-3(fin)·H-3(fit)·H-4 는 전부 9-0 의 "대상 저장소 변경 — 경로 무관" 에 걸려 에이전트 사전 리뷰 필수** | 정정 H |

---

## 6. 남은 미결 질문

**해소된 것 3건은 §2 로 옮겼다** — Q31(측정) · Q33(측정) · Q37(측정).
**Q35 는 삭제됐다**(§4-8 에서 `git rm` 범위가 0이 되어 질문이 사라졌다). **Q41 은 3회차 신설이다.**

| | 질문 | 무엇을 가르는가 | 우선 |
|---|---|---|---|
| **Q29** | `orphan-check` 이관 형태 — **B 복사**(권고) / A 이동 / C 이동+스텁 | **fit 단독 세션이 그 스킬을 잃는지**와 **113줄 중복을 받아들이는지.** A·C 는 fit 쪽 되돌리기가 편도·중간이고 재작성 10줄(N13)이 붙는다. B 는 즉시·0줄 | **높음 — H-1b 착수 전** |
| **Q30** | `--add-dir` 부착 정책 — 안내만 / 셸 별칭 / **저장소별 래퍼**(권고) | **유지 33파일이 pleiades 세션에서 보이는지**, 그리고 **환경변수 누락으로 rules 만 빠진 상태**(N19)를 막는지. `repos/myFitness/CLAUDE.md` 사본 처리도 여기 붙는다 | **높음 — 통합 작업이 대상 저장소 규범을 요구하는 1a-1 전** |
| **Q39** *(신설 · 2회차 · 3회차 비용 보정)* | `workflow.md` 중복을 줄이나 — **W-1** pleiades 에 "단독 작업" 절 신설(주제 단위 공통 **30줄**, N7) 후 **원본을 fin 잔여 5줄 + 라벨 1 · fit 잔여 4줄 + 라벨 1 로 축약** / **W-2** 원본 2파일 유지 + #8 부분 갱신만(**권고**) | **원본 2파일(505 LOC)이 남는지.** W-1 비용은 세 겹이다 — ⓐ pleiades 신설 30줄 ⓑ **N11 = 6줄** 절 번호 재작성 ⓒ **재귀분: 절을 추가하면 pleiades 번호가 또 밀려 그 6줄이 다시 어긋난다**(§7 이 같은 위험을 등재). 되돌리기 W-1 **중간** — *되돌리는 행위*: pleiades 절 삭제 + 원본 2파일을 백업에서 복원(fit 은 Q32-i 면 **편도**). W-2 **즉시** — *되돌리는 행위*: `git revert` 1회(fin) · 로컬 되돌림(fit) | **높음 — H-3 착수 전** (같은 파일을 건드린다) |
| **Q32** | fit 하네스 버전 관리 — i 그대로 / **ii tracked 화**(권고) / iii pleiades 사본 | **fit 파일 변경의 되돌리기 등급.** 지금은 **편도**다. H-3(fit) 33줄이 여기 걸려 있다. **Q33 해소로 공개 장애물은 사라졌다**(N20) | **높음 — H-3(fit) 전** |
| **Q34** | #8 의 fit 쪽 수정 방식 | Q32 에 종속. ii 이후면 a-2 가 즉시 등급이 된다 | 중간 |
| **Q36** | `settings.local.json` 저장소 무관 규칙(N21)을 pleiades 로 이식하나 | **pleiades 세션의 권한 프롬프트 횟수.** pleiades 에는 settings 파일이 0개 | 낮음 |
| **Q38** | 004 Q22 재검증 범위를 *"ignored 경로를 포함하는 모든 grep 측정"* 으로 넓히나 | **M4 *"배포는 로컬과 무관"*** 등 기존 0건 결론의 유효성. 의심한 원인(binary)과 실측 원인(gitignore)이 다르다 | 중간 |
| **Q41** *(신설 · 3회차)* | H-1 산출물 이름을 **fin·fit 기존 이름과 겹치지 않게** 지을 것인가(권고), 아니면 겹치는 이름을 쓰고 **skill 충돌 동작을 먼저 측정**할 것인가 | **H-1 의 착수 가능 여부.** 겹치지 않으면 측정 불필요·즉시 착수. 겹치는 이름을 고르면 **런타임 1회(N18 과 같은 방식)가 선결**이고, `--add-dir` 세션에서 skill 이 rule 처럼 공존할지 agent 처럼 조용히 드롭될지 모른 채 문서를 배치하게 된다 | **중간 — H-1 착수 전** |
| **Q40** *(신설 · 2회차)* | 이 문서를 정본화할 때 **1회차의 폐기·이관 서술을 어떻게 남기나** | 정정 규약상 지우지 않는다. §0-0-1·§0-1·§5 9~15행이 그 기록이다 — **정본에서도 유지할지** | 낮음 |


---

## 7. 유효기간

| 조건 | 무엇이 바뀌나 |
|---|---|
| **Q29·Q30·Q32·Q39·Q41 에 답이 나올 때** | H-1·H-1b·H-3·H-4 가 착수 가능해지고 이 문서가 정본 후보가 된다 (H-0 은 지금도 착수 가능) |
| **H-1 산출물 이름이 정해질 때** | §4-5 의 skill 층 행이 확정된다. 겹치는 이름을 골랐다면 **Q41 의 런타임 측정 1회가 선결**이 된다 |
| **`workflow.md` 3벌 중 하나가 크게 바뀔 때** | N4 의 118줄과 N6 의 등급 분류를 다시 잰다. 특히 pleiades 에 절이 추가되면 **N11 이 커진다** |
| **`--add-dir` 없이 통합 작업이 대상 저장소 규범을 필요로 하는 상황이 반복될 때** | Q30 의 권고가 ④에서 더 강한 안(예: 규범만 pleiades 로 승격)으로 올라간다 |
| **Codex 대응 문서(H-1)를 쓴 뒤 fin·fit 원본이 갱신될 때** | 3벌이 되므로 그때 처음으로 실질 드리프트가 생긴다. §4-3 Codex 행을 다시 본다 |
| **002 단계 4(모노레포) 진입** | `repos/*` 가 사라지고 `apps/*` 가 tracked 경로가 된다 — 중첩 `.claude/` 문제의 형태가 바뀌어 §4-2 전체를 다시 본다 |
| **#17 감사 에이전트 신설** | 이 문서의 정합성 검사가 그 에이전트를 거친다 |

---

## 8. 제외 사항

| 항목 | 왜 제외 | 어디로 |
|---|---|---|
| **#17 문서 정합성 감사 에이전트** | 별도 이슈. 착수 조건이 **Codex 쿼터 회복(#14)** | 이슈 #17 |
| **#14 봇 리뷰 불가 시 대체 경로** | 룰 보완 건 | 이슈 #14 |
| **002 단계 0 · 1a 전체** | 축 B. 이 작업은 1a-1 의 **선결 조건**일 뿐 | 004 Q23 · 003 |
| **Q22 재검증 실행** | 룰 문구만 고친다 | Q38 |
| **하네스 내용의 품질 개선** | 이 문서는 **어디에 무엇이 있는가**만 다룬다. #8 만 예외 | — |
| **(d) 15줄 (codex-cli MCP 운영 파라미터)** | 안 쓰는 경로라 필요 여부를 측정으로 판정 불가 | [W-diff] §6 |

---

## 부록 A — `CLAUDE.md` 동기화 초안 (H-0/H-1 머지 시 반영)

**A-1. "하네스는 아직 통합되지 않았다" 절 교체안**

> ### 하네스: 어디에 무엇이 있나
>
> **공통은 pleiades, 저장소별 특수는 그 저장소에 있다** (Q20 확정 2026-09-07 · `docs/specs/005-harness-integration.md`).
> **통합은 파일 이동이 아니라 역할 이관으로 실현된다** — 원본 34파일 중 pleiades 로 옮겨오는 것은 1개다.
>
> | | 어디 | 무엇 |
> |---|---|---|
> | 공통 | `~/workspace/pleiades/.claude/` | 워크플로우 룰(통합 작업) · Codex 대응 · orphan 검사 · 측정/감사 하네스 |
> | fin 특수 | 원본 `~/workspace/myFinance/.claude/` (tracked **16파일**) | 세금·매매 룰 · 에이전트 4 + 오케스트레이터 · 세션 스킬 · **단독 작업 워크플로우** |
> | fit 특수 | 원본 `~/workspace/myFitness/.claude/` (**18파일** — H-4 전까지 **버전 관리 밖**) | Prisma·pm2 운영 · 에이전트 5 · 세션 스킬 · **단독 작업 워크플로우** |
>
> **pleiades `workflow.md` 는 단독 작업 절차를 원본에 위임한다**(`:165`). 원본 `workflow.md` 2파일은
> 폐기 대상이 아니다 — pleiades 판이 버린 181줄 중 **35줄이 단독 작업에만 필요**하다.
>
> **저장소별 특수 하네스는 pleiades 세션에서 자동 로드되지 않는다.** `--add-dir` 가 유일한 경로이고,
> **환경변수를 빼면 skills·agents 만 보이고 rules·CLAUDE.md 는 조용히 빠진다:**
>
> ```bash
> CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1 \
>   claude --add-dir ~/workspace/myFinance      # 한 번에 한 저장소 — 둘 다 붙이면 agent 가 조용히 사라진다
> ```
>
> `--add-dir` 는 `--resume` 시 복원되지 않는다. 대상은 **항상 원본**(worktree 아님).
> 붙이는 순간 rule `workflow` 가 **2벌 공존**한다 — pleiades 것이 통합 작업의 정본이다.
> **fit `.claude/` 는 git 이력이 없다.** 지우면 복원 수단이 없다 (005 §4-7).

**A-2. 문서 지도 표에 추가할 행**

> | `docs/specs/005-harness-integration.md` | **하네스 처분 정본** — 37행 처분 표, 역할 이관, `--add-dir` 운영 규약(로딩 범위·이름 충돌), #8·#10 처리, 미결 **Q29~Q41**(Q35 삭제 · Q31·Q33·Q37 해소) | 하네스가 어디에 있어야 하는지 판단할 때 |

**A-3. 작업 규칙 절 grep 줄 교체안 (`CLAUDE.md:106`)**

> - **측정 grep 은 두 가지 오탐을 막는다.** ① binary 스킵 — `--binary-files=text`(GNU) / `--text`(git grep).
>   ② **gitignore 스킵** — 이 환경의 `grep` 은 ugrep 래퍼라 ignored **디렉터리**를 통째로 건너뛴다.
>   셋 중 하나: **`--no-ignore-files`** · **`/usr/bin/grep`** · **경로 직접 지정**.
>   `git grep` 은 인덱스만 보므로 **ignored 경로에는 쓸 수 없다** (005 §4-11)

**A-4. 작업 규칙 절에 추가 (R1 — §4-10)**

> - **열거와 합계가 함께 있으면 합계를 신뢰하지 말고 열거를 센다.** 요약 숫자에는
>   열거의 출처(파일 · 절 · 행 범위)를 붙인다. **대장에 없는 숫자를 본문에 새로 만들지 않는다.**
>   같은 숫자를 문서 안에서 두 번 적지 않는다

---

## 부록 B — 실행한 명령 (읽기 전용 · 대상 저장소 쓰기 0건)

**B-1 ~ B-5 는 1회차와 동일하며 [감사] 확인 12 에서 전부 재현됐다.** 요지만 남긴다:
B-1 `git rm -n` dry-run 2파일 · B-2 프로세스 치환 동작 · B-3 grep 래퍼 0/3/3/3 ·
B-4 #8 줄 재검산 fin **22** · fit **30** (줄 번호 52개 열거는 [감사] 확인 4 에서 전수 일치) ·
B-5 세 저장소 PUBLIC.

**B-6. Q33 — 1회차에서 막혔던 민감 문자열 스캔은 [MF] 에서 수행됐다** (N20). 이 세션 재실행 없음.

**B-8. `repos/myFitness/CLAUDE.md` 사본 (정정 D)**
```bash
ls -la ~/workspace/pleiades/repos/myFitness/CLAUDE.md ~/workspace/myFitness/CLAUDE.md
#   양쪽 8,621 B · mtime 동일 (Sep 3 14:45)
git -C ~/workspace/pleiades/repos/myFitness check-ignore -v CLAUDE.md   # .gitignore:36
diff <두 파일> | wc -l                                                   # → 0
```

---

## 부록 C — 열거 전수 (대장 N9~N16 의 근거)

**C-1. N9 — fin → `workflow.md` 8줄**
```bash
git -C repos/myFinance grep --text -nE 'workflow\.md|workflow 룰' integration/pleiades -- '.claude' 'CLAUDE.md' \
  | cut -d: -f2- | grep -v '^\.claude/rules/workflow\.md'
```
`agents/quality-guardian.md:86` · `agents/spec-planner.md:19,49` · `skills/milestone-workflow/SKILL.md:121` ·
`skills/project-verify/SKILL.md:98` · `skills/release-publisher/SKILL.md:58` · `CLAUDE.md:128,145`

**C-2. N10 — fit → `workflow.md` 11줄** (`git grep` 은 원리상 0건이므로 `/usr/bin/grep`)
```bash
cd ~/workspace/myFitness && /usr/bin/grep -rn --binary-files=text -E 'workflow\.md|rules/workflow' .claude CLAUDE.md \
  | grep -v '^\.claude/rules/workflow\.md'
```
`agents/codex-liaison.md:23` · `agents/workflow-conductor.md:3,10,20` ·
`skills/branch-workflow/SKILL.md:3,8,83` · `skills/myfitness-orchestrator/SKILL.md:103,115` · `CLAUDE.md:92,108`

**C-3. N11 — 절 번호·단계 목록 인용 6줄** (원본을 축약할 때만 비용)

| 줄 | 인용 | pleiades 에서 |
|---|---|---|
| fin `quality-guardian.md:86` | `workflow.md §8` | §8 = 검증 (리뷰는 §9) |
| fin `project-verify/SKILL.md:98` | `workflow.md §7~8` | 한 절씩 밀렸다 |
| fin `spec-planner.md:19` | 10단계 열거에 **UI 디자인** 포함 | 삭제되고 `되돌리기 비용 산정` 으로 교체 |
| fit `codex-liaison.md:23` | `workflow.md 8절` | 9절 |
| fit `workflow-conductor.md:20` | `workflow.md 8-6` | 9-6 |
| fit `branch-workflow/SKILL.md:83` | `workflow.md 8-1` | 9-1 |

**C-4. N12 — 1회차 이관 범위(3파일) 기준 재작성: 지점 14 · 줄 13** (2회차 값은 ※ 참조)

| 대상 | 줄 | 담긴 이름 |
|---|---|---|
| fit `agents/codex-liaison.md:30` | 1 | codex-review-loop |
| fit `agents/codex-liaison.md:31` | 1 | orphan-check |
| fit `agents/workflow-conductor.md:34` | 1 | orphan-check |
| fit `skills/branch-workflow/SKILL.md:156` | 1 | codex-review-loop |
| fit `skills/branch-workflow/SKILL.md:175` | 1 | orphan-check |
| **fit `skills/myfitness-orchestrator/SKILL.md:32`** | 1 | **둘 다 (지점 2 · 줄 1)** |
| fit `skills/myfitness-orchestrator/SKILL.md:41` | 1 | orphan-check |
| fit `CLAUDE.md:115` | 1 | codex-review-loop |
| fit `CLAUDE.md:117` | 1 | orphan-check |
| fit `skills/orphan-check/SKILL.md:96,98` (outbound → release-flow · workflow-conductor) | 2 | — |
| fin `skills/session-boundary/SKILL.md:49` | 1 | codex-response-patterns |
| fin `skills/codex-response-patterns/SKILL.md:8` (outbound → release-manager) | 1 | — |
| **합** | **13줄 / 14지점** | |

※ **2회차 재산출.** Codex 쌍이 §4-2 에서 **유지**로 바뀌면서 이 표의 구성이 달라진다 —
`codex-response-patterns`·`codex-review-loop` 관련 줄은 재작성 대상에서 빠지는 대신,
**1회차에서 "이관 파일끼리라 대상 아님"으로 제외했던 2줄이 대상으로 들어온다**
(`orphan-check:97` → 이제 fit 에 남는 `codex-review-loop` 를 가리킨다 · `codex-review-loop:96` → 옮겨간 `orphan-check` 를 가리킨다).
**H-1b 형태 A·C 의 재작성 = N13 그대로 10지점 / 10줄.** 형태 B(권고)면 **0줄.**

**C-5. N13 — `orphan-check` 총 참조 10지점**
inbound 7: 위 6 + `skills/codex-review-loop/SKILL.md:96` · outbound 3: `:96` release-flow · `:97` codex-review-loop · `:98` workflow-conductor

**C-6. N14 — fin 16파일 전부 `git rm` 시 활성 파급 6건**
```bash
git -C repos/myFinance show integration/pleiades:CLAUDE.md | /usr/bin/grep -n -- '\.claude\|workflow\.md'
#   62 · 118 · 128 · 140 · 145
```
`+ src/app/api/alerts/history/export/route.ts:5` (JSDoc 주석 · `api-routes.md` 를 부른다)

**C-7. N16 — H-0 대상 14줄 + 6곳**

> **정정 J (감사 2회차 정정 ⑧).** 아래 세 명령 중 앞의 둘은 **상위집합을 반환한다** —
> `binary-files=text` 는 명령 예시까지 잡아 **10줄**, `체크아웃` 은 다른 맥락까지 잡아 **18줄**을 출력한다
> (`/tmp/` 만 정확히 6). **명령은 후보를 모으는 데 쓰고, 아래 열거가 분류 결과다.**
> 분류 기준: **같은 보일러플레이트 문단(`> **grep 에는 반드시 …`)** 과 **게이트 지시문**만 센다.

```bash
cd ~/workspace/pleiades
/usr/bin/grep -rn --binary-files=text -- 'binary-files=text' CLAUDE.md .claude/     # grep 문구
/usr/bin/grep -rn --binary-files=text -- '체크아웃' .claude/                        # 게이트
/usr/bin/grep -rn --binary-files=text -- '/tmp/' .claude/                           # 임시파일
```
게이트 8: `skills/repo-measure/SKILL.md:22,92,94` · `agents/repo-surveyor.md:24,70,72` ·
`agents/reversibility-auditor.md:47` · `skills/reversibility-audit/SKILL.md:45`
`/tmp` 6: `repo-measure:117-119` · `repo-surveyor:84-86`
grep 문구 6곳: `CLAUDE.md:106` · `rules/workflow.md:91` · `repo-surveyor.md:25` ·
`repo-measure/SKILL.md:23` · `reversibility-auditor.md:20` · `reversibility-audit/SKILL.md:18`
