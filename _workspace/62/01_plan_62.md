# 01 — 집행 계획: #62 fin·fit `workflow.md` 8-4 `model` 예시 `gpt-4o` 정정

**작성:** 2026-09-14 · **이슈:** `fomalhaut84/pleiades#62`(myFinance#493 Codex P2 파생) · **모드 I · 대칭 변경**(이슈 1 · PR 2 · `Refs` · 둘 다 머지돼야 종료 · 5절 예외 표)

## 실측 (2026-09-14 · 쿼터 소비 없음)

| 항목 | 값 | 명령 |
|---|---|---|
| 지원 모델 | **`gpt-5.5` · `gpt-5.4` · `gpt-5.4-mini`**(`visibility: list`) + `codex-auto-review`(`hide`). **`gpt-4o` 없음** | `python3 -c "import json;[print(m['slug'],m['visibility']) for m in json.load(open('$HOME/.codex/models_cache.json'))['models']]"` |
| 캐시 출처 | `~/.codex/models_cache.json` · `fetched_at 2026-07-02T01:56Z` · `client_version` = codex-cli **0.142.4**(`codex --version`) · `config.toml` 의 `tui.model_availability_nux."gpt-5.5"=1` 이 gpt-5.5 노출을 뒷받침 | `codex --version` · `head -30 ~/.codex/config.toml` |
| MCP 도구 실체 | `mcp__codex-cli__codex` = `npx -y codex-mcp-server`(stdio · fin·fit 프로젝트에 등록 · `~/.claude.json`) — `model` 파라미터는 codex CLI 로 전달되므로 지원 목록은 위와 같다 | `~/.claude.json` mcpServers 순회(비밀값 제외) |
| `gpt-4o` 언급 | **2곳** — `repos/myFinance/.claude/rules/workflow.md:219` · `repos/myFitness/.claude/rules/workflow.md:257`. pleiades 하네스 0 | `/usr/bin/grep -rn gpt-4o <3 하네스>` |
| 원본 도달 대상 | fin 원본 `~/workspace/myFinance/.claude/rules/workflow.md:199`(`dev` · tracked) · fit 원본 `~/workspace/myFitness/.claude/rules/workflow.md:257`(`main` · ignored) | `grep -n gpt-4o` |

한정: 캐시는 2026-07-02 갱신본이다. codex CLI 는 기동 시 이 캐시를 쓰고 etag 로 갱신하므로 현재 지원 목록과 다를 수 있다 — 그래서 **문안은 모델명을 예시로 박지 않고 캐시 파일을 출처로 가리킨다**(실측값은 날짜와 함께 병기).

## 결정 — 예시 교체 vs 하드코딩 제거 (이슈의 미결)

**혼합안(권고):** 하드코딩 예시를 빼고 **출처**(`~/.codex/models_cache.json` 의 `slug`)를 적되, 실측 결과를 **날짜·버전과 함께** 병기한다.
- 예시만 바꾸면(`gpt-5.5`) 다음 모델 개편 때 같은 P2 가 재발한다
- 출처만 적으면 운영자가 파일을 열어야 한다 — 실측값 병기가 그 비용을 없앤다
- 되돌리기: **worktree 즉시** · **fit 원본 동기화분 중간**(ignored · git 이력 없음 · 사전 사본으로 복원) · **fin 미러 즉시**(릴리즈까지 갔으면 모드 H 경로) — 5-1 원칙 4: 원본 도달분까지 포함해 매긴다 (재감사 C-1)

## 건드릴 파일

| 저장소 | 파일 | 변경 |
|---|---|---|
| `repos/myFinance`(worktree · `integration/pleiades` `ef43c03`) | `.claude/rules/workflow.md:219` | 1줄 교체 + 8-4 끝에 정정 블록 3줄 |
| `repos/myFitness`(worktree · `integration/pleiades` `3818208`) | `.claude/rules/workflow.md:257` | 위와 동일 문안 |

교체 문안 (사전 리뷰 major 1 · info 2 반영 — 펜스 안은 짧게, 출처·실측은 펜스 아래 산문으로 · `visibility: "list"` 필터 · 캐시 `fetched_at` 한정):

```
- model 파라미터는 생략 (기본 model 미지원 오류 시 지정 필요 — 지원 모델은 아래 참조)
```
펜스 아래 산문: **지원 모델 출처:** `~/.codex/models_cache.json` 에서 `"visibility": "list"` 인 `slug` 중에서 고른다(`hide` 항목 `codex-auto-review` 제외). 로컬 파일이라 쿼터 소비 없음. 실측 2026-09-14(codex-cli 0.142.4 · 캐시 `fetched_at` 2026-07-02 — etag 로 갱신되므로 바뀌었을 수 있다): `gpt-5.5` · `gpt-5.4` · `gpt-5.4-mini`. **`gpt-4o` 는 목록에 없다.**

정정 블록(8-4 절 끝 · 기존 정정 블록 뒤):

```
> **정정 (2026-09-14 · pleiades#62 · myFinance#493 Codex P2).** 이전 예시 `"gpt-4o"` 는 ChatGPT 인증 Codex 가 지원하지 않는 모델이라
> *"model 미지원이면 지원 모델로 지정해 재시도"* 안내를 따라도 같은 오류가 났다. 하드코딩 예시를 빼고 로컬 모델 캐시(쿼터 소비 없음)를 출처로 바꿨다 — 실측값은 날짜·버전과 함께. 되돌리기: 즉시.
```

## 반영에 필요한 것

- 빌드: **불필요** · 재시작: **없음** · 세션 초기화: **없음** — 문서 1파일 ×2. `integration/pleiades` 는 배포·CI 트리거 밖(1a-2 실측). β2 병행 인스턴스 **없음**(서버 미착수).
- 검증(8절): 문서만 → 4종 **해당 없음**. `git diff` 육안 + 인용 블록 형식 대조.
- 리뷰(9-0): 대상 저장소 변경 → **에이전트 사전 리뷰 필수** → PR 2개(`Refs fomalhaut84/pleiades#62`) → 봇 루프(30분 컷오프).

## 원본 도달 (G-2 선례 · 머지 후 · 별도 승인 없음 — 이 게이트에 포함)

| 저장소 | 경로 | 방법 |
|---|---|---|
| fit | 원본 `~/workspace/myFitness/.claude/rules/workflow.md`(ignored) | 머지 후 `git -C ~/workspace/myFitness archive integration/pleiades .claude/rules/workflow.md \| tar -x -C ~/workspace/myFitness` — **동기화 전 사전 사본 + 부재 목록을 `_workspace/62/backup/` 에**(5-1 원칙 3 · 첫 적용) |
| fin | 원본 `~/workspace/myFinance` `dev`(tracked) | **모드 S 미러 PR** — 이슈는 `fomalhaut84/myFinance` 에, 브랜치 `chore/<fin-issue>-1`, base `dev`, 본문에 원 PR 링크. 문장 단위 적용(원본 `dev` 8-4 는 worktree 판과 정정 블록이 다르다 · cherry-pick 불가 · #51 선례) |

## 서비스 중단 가능성 — **없음**

## 롤백 — `_workspace/62/04_operator_rollback.md`(5-1 형식 · #66 첫 적용)
