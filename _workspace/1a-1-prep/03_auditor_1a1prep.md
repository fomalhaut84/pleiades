# 감사 결과 — `_workspace/1a-1-prep/02_writer_1a1prep.md` (1회차 / 3회 상한)

> 감사관(`reversibility-auditor`, 읽기 전용)이 2026-09-09 에 반환한 결과를 오케스트레이터가 그대로 저장했다.
> 대상 저장소 2곳은 worktree `integration/pleiades` 읽기만(clean).

**정정 11건 · 미확인 1건(유지) · 나머지 확인.** 정정이 나왔으므로 문서는 초안 상태다.
**산출물 형태 판정: (A) 아님 — 3안(003 §4-2 재작성 + 기존 표를 정정 블록으로 강등). 나머지 절·005·`workflow.md`·`CLAUDE.md` 는 (A) 방식 append 유지.**

## 정정 표

| # | 주장 (초안 원문 요지) | 판정 | 근거 / 실제 |
|---|---|---|---|
| **1** | Q19 근거 ① *"raw 는 상위집합이다 — raw 포트 위에서는 B 로 **언제든 4지점·즉시** 이행할 수 있다"* · B행 *"관측 변경 5 표면 전부 — **A 와 동일**"* | **정정** | `repos/myFinance` `src/bot/utils/error.ts:27-53` — `sanitizeError(err: unknown)` 이 `.error ?? .cause` 를 **깊이 5까지 순회해 join** 한다. 003 §4-2 포트는 `deliveries[].error?: string`. 1a-4 가 `alert-dispatcher.ts:122-130` 의 `for…catch (error)` 를 파사드로 흡수하면 **4 지점에 error 객체가 남지 않는다** → 호출부가 쓸 수 있는 것은 `sanitizeMessage(문자열)` 뿐이고 `Name:` 접두도 HttpError 체인도 재현 못 한다. 게다가 C-6 자신이 *"HttpError 는 내부 fetch 에러가 추가된다 — 정보 증가"* 라고 적었으므로 **raw 문자열은 sanitized 의 상위집합이 아니다**(두 집합은 비교 불가). measured-facts **C-7 의 "A 와 B 는 결과가 같다" 도 같은 정정 대상**이다. 권고 C 는 살아남지만 **근거 ①은 폐기**하고 *"A·B 모두 포트 계약을 건드려야 도달 가능 · C 만 관측 변경 0"* 으로 바꾼다 |
| **2** | 5-2 #7 *"`onError`(Q26 ②)가 Q19 B 안의 4 지점을 바꾸는지 — **미확인**"* | **정정** | 시그니처만으로 확정된다. 003 §4-2 정정 ③ 개정안의 `onError?: (target: string, error: unknown) => void` 가 **파사드가 error 객체를 넘기는 유일한 표면**이다. ∴ **설계 B 는 Q26 ② 를 고를 때만 성립**하고, Q26 ①(초안 권고)·③ 에서는 B 가 `sanitizeMessage(string)` 로 축퇴한다. 초안은 Q26 과 Q19 를 독립 질문으로 제시해 **사용자에게 실현 불가능한 조합(Q26 ① + 나중에 Q19 B)을 열어둔다.** 두 질문에 결합 조건을 명시해야 한다 |
| **3** | Q25·Q26·Q19·Q10-L 표의 되돌리기 등급 **"즉시"**(무조건) | **정정** | 003 §5-2 1a-1 행은 *"**즉시 — 단 1a-3 착수 전까지만**"* 이고 §8-1 은 그 뒤로 `npm ci` + 재빌드 + `pm2 restart`(병행 인스턴스 2개)가 붙어 **중간**이라고 적었다. 시점 한정어 없이 쓰면 1a-3/1a-4 이후의 같은 되돌리기를 즉시로 오독한다. 네 표의 등급 열에 **"(1a-3 착수 전까지)"** 를 붙인다. — **#32 I1 의 "즉시" 는 예외로 확인**: pleiades 안에 갇히고 `npm pack --dry-run` 4파일·소비자 누수 0/0 실측으로 계약 무영향이라 시점 무관 |
| **4** | Q10-L ② *"**코어에 길이 정책 축**이 생긴다. 그 축은 **1a-1 시그니처에 들어오고**"* · 권고 근거 ① *"003 §7-4 가 거부한 유형과 같은 형태"* | **정정** | 003 **§4-3 제약 2** 는 길이 분할을 **`Transport` 경계 안쪽**으로 못 박았다. ∴ 절단\|분할 축은 `TelegramTransport` 팩토리 옵션이지 `Notifier`/`Transport` 시그니처(§4-2)가 아니다 — **코어도 파사드 시그니처도 아니다.** 그리고 §7-4 의 거부 사유는 *"1건 때문에 **모든 어댑터가 구현 의무**를 진다"* 인데 어댑터-로컬 옵션은 `DiscordTransport` 에 아무 의무도 지우지 않는다 → 유비 **불성립**. ②의 실제 비용은 "공개 팩토리 옵션 1개 + 저장소별 값" 으로 초안 서술보다 작다. 권고 ① 은 근거 ②③④ 로 유지되나 **근거 ①은 교체**해야 한다 |
| **5** | §3 *"권고 (A) 정정 블록 append"* · *"003 §4-2 는 이미 정정 블록 **3개**(①②③)"* · *"(A) 는 R1 충돌 없음 — 각 숫자가 **원래 있던 표에서 갱신**된다"* | **정정** | 003 §4-2 = **117행**, 그중 **78행(66.7%)이 정정 블록**이고 블록은 3개가 아니라 **5개**(①·②·② 안의 PR #9 2개·③) — 합계≠열거(R1). 결정적으로 **맨 위 코드 블록이 이미 정본이 아니다**: `Notifier` 가 `notify(route, content)` + `targetCount` 둘뿐이고 `targets`·`ctx` 는 정정 ③ 안에만 있다. 4번째 블록을 얹으면 **1a-1 이 구현할 시그니처를 5개 블록에서 재구성**해야 한다. 따라서 (A) 행의 *"원래 있던 표에서 갱신"* 은 §4-2 에 대해 **거짓**(맨 위는 갱신되지 않고 새 블록이 다시 적는다 = R1 위반) |
| **6** | §4 정본 정정 대장 13행이 완전 목록 | **정정** | 초안 §3 이 *"(A) 가 반드시 함께 해야 하는 것"* 으로 지목한 것 중 **대장에 없는 행 5개**: ① 003 §10 **Q25 행**(미결→확정) ② 003 §10 **Q26 행** ③ 003 §11 유효기간 *"Q25·Q26·Q19 방향에 답이 나올 때"* 행 ④ **005 §6 Q41 행** ⑤ 005 §7 유효기간 *"H-1 산출물 이름이 정해질 때"* 행. 추가로 **003 §5-2 조건부 항목**(*"Q10 이 '예'일 때만 1a 에 포함"*)도 Q10-L/Q10-P 분리로 갱신 대상인데 없다 |
| **7** | 대장 #9 *"005 §4-13 '입력 200 LOC' → **204** (fin 91 + fit 113)"* | **정정** | 기준(basis)이 섞인다. 실측(모드 I): `git diff --stat dev integration/pleiades -- .claude` → **9파일 변경 · +108/−75**. fin `.claude/` = **16파일 / 1,662 LOC**(005 기재 1,629). 200 의 **출처 열이 "§4-2 10·28행"** 인데 10행의 `87` 을 그대로 두고 합계만 204 로 고치면 합계≠열거(R1). 그리고 005 의 fit 값(18파일/1,885)은 **원본 기준**이다 — worktree tracked 는 **17파일 / 1,774 LOC**(`settings.local.json` 111줄 부재, 실측). ∴ 정정 문구에 **기준 라벨**을 박아야 한다: *"H-1 입력(모드 I · `integration/pleiades`) = 91 + 113 = **204**. §4-2 10행의 87 은 측정 시점(`dev`, #492 이전) 값"*, 그리고 N1·N2 를 재기준화할지 여부를 명시 |
| **8** | Q25 하위 선택 *"fit ADMIN 을 기존 `TELEGRAM_ALLOWED_CHAT_IDS` 에 매핑해야 fit 동작 변경이 0"* | **정정** | 매핑 자체는 **확인**(fit env 실측: `TELEGRAM_ALLOWED_CHAT_IDS` 4곳뿐 · `TELEGRAM_ADMIN_CHAT_IDS` **0건**). 그러나 빠진 것: fin `advisor-monitor.ts:190-194` 주석이 *"`TELEGRAM_ALLOWED_CHAT_IDS` 첫 값 재사용 같은 heuristic 은 **secure default 원칙 위반**"* 이라고 **명시 금지**하고, 003 **§7-6** 은 fit `admin-alerts.ts:232` 가 정확히 그 동작을 한다고 결함으로 등재했다. ADMIN-실값 + 이 매핑은 **`Route.ADMIN` 의 의미를 저장소마다 다르게 만든다**(fin=관리자 전용 / fit=전체 화이트리스트) — 공유 인터페이스가 §7-6 결함을 **의미론으로 승격**한다. 그 사실과 *"되돌리는 유일한 경로가 Q13"* 을 초안에 적어야 한다. 부수: fin 의 미설정 시 `console.warn` + 스킵 fail-safe(`:205-211`)를 파사드가 어떻게 보존하는지도 없다 |
| **9** | #32 I1 권고 근거 ① *"(a) 는 파일 1개·10줄·**+0.421 s** 를 더 쓰고 더 잡는 오류는 0건"* | **정정** | **추가 게이트 프레이밍에서만 참**이다. 실측 §5 (a) 행이 제시한 대체 형태(`packages/notify` 의 `test` 를 `vitest run --typecheck` 로)에서는 (a) = **+0.413 s** 로 (b) 의 **+0.620 s** 보다 **싸다**. 그리고 **`+0.421 s` 는 대장에 없는 파생 숫자**다(1.041−0.620). (b) 권고는 근거 ②(F6 요약줄 오표기)·③(F3 node 종속)으로 유지되므로 **근거 ①은 삭제하거나 프레이밍을 명시**한다 |
| **10** | Q10-L ③ Q14 최소 질의 *"단일 테이블·단일 컬럼 길이 집계 하나. **chat id·개인 식별자 불필요**"* | **정정** | 식별자 부분은 **확인** — `prisma/schema.prisma` `model AIAdvice` = `id·category·reportDate·prompt·response·createdAt`, 사용자 식별 열 자체가 없다. 그러나 `response` 는 **AI 가 쓴 개인 건강 리포트 본문**이다. 초안은 질의가 **`length()` 집계만 반환하고 `response`·`prompt` 본문을 반출하지 않는다**는 것을 계약으로 못 박지 않았다. Q14 가 실서비스 DB 읽기 **승인 게이트**이므로 그 한 줄이 있어야 한다. — 부수 **확인**: *"`length(response)` 는 하한"* 성립. `utils/telegram.ts:6-15 mdToHtml` 7치환이 전부 길이 증가(`### `→`<b></b>` +3 … `` ` ``→`<code>` +11) 또는 유지(`\n- `→`\n• ` ±0)이고 전송본이 `${emoji} <b>${label}</b>\n\n` 를 앞에 붙인다 |
| **11** | Q41 부수 (i) *"측정은 H-1 과 독립이고 (iii) 은 H-1 을 막을 이유가 없는 것에 막는다"* / E·S 판정 분모 "35개 이름" | **정정** | (i) 결론은 타당하나 **per-name 가정 위에 있다** — N18 은 rule·agent 만 쟀고 초안 5-2 #2 도 *skill 충돌 해소가 이름 단위인지 층 단위인지 미측정*이라고 적었다. *"H-1 이 막히지 않는다"* 는 agent 선례의 유비이지 측정이 아니므로 그 가정을 근거문에 적는다. 그리고 E/S 분모를 **"35개 이름"(skill·agent)** 으로만 정의해 **rule 9엔트리/유니크 5 가 분모 밖**이라는 사실이 안 보인다 — 실측 확인 결과 `pleiades-codex-loop` 는 rule 5 이름(`api-routes`·`components`·`stock-trading-method`·`tax-logic`·`workflow`)과도 **E·S 모두 0** 이므로 결론은 불변. 한 줄 명시 권고 |

## 확인 (재검증 불요)

| 주장 | 근거 |
|---|---|
| Q10-P ① 단서 — 라이브 두 구현 차이는 정규식 수량자뿐, 차이 나는 입력은 **리터럴 `<>` 하나**, 발생 여부 **미측정** | fin `bot/utils/telegram.ts:44,67` = `/<[^>]+>/g` · fit `bot/notifications/send.ts:45` = `/<[^>]*>/g` (실측). 차집합 = `<>` 정확. 초안이 단정하지 않고 미측정으로 적어 **통과** |
| `bot/utils/markdown.ts:97 stripHtml` = importer 0 (죽은 export) | `git grep stripHtml integration/pleiades -- src` 전수: 정의 1 + **동명 웹 UI 자체 정의**(`app/alerts/history/client-utils.ts:27`) + 그 테스트 + `stripHtmlForCsv`(다른 이름)뿐. 봇 폴백 경로 importer 0 |
| Q10-L 근거 ③ — 절단이 폴백을 유발 | `send.ts:44` `truncate(htmlText)` (HTML 먼저 자름) vs `:45` `truncate(htmlText.replace(/<[^>]*>/g,""))` (태그 제거 후 자름). 성립 |
| `advisor-monitor:199` 호출부가 22 루프 중 하나 | `advisor-monitor.ts:217-226` = `for (const chatId of chatIds) { try { await sendHtml(...) } catch ... }`. 003 §4-2 PR #9 정정·발견 16·17 과 일치 |
| #32 I1 — `noEmit` 파일 내 고정이 `extends` 순서상 우선한다 | 자식 `compilerOptions` 가 base 를 키 단위로 덮는다. base(`packages/notify/tsconfig.json`)에 `noEmit` 없음·`outDir`/`declaration` 있음 → 파일 내 `noEmit:true` 유효. 실측 dist 미생성과 일치 |
| #32 I1 — `@types/node ^20` 이 소비자 설치에 새지 않는다 | 루트 `files` = `packages/notify/dist` + `packages/notify/package.json`, 루트에 **`workspaces` 없음** → 서브 devDep 이 임시 클론에서도 설치되지 않는다. e2e 실측 `@types/node` 0 · `vitest` 0 |
| #32 I1 — `workflow.md` 8절 갱신안 1줄 | 실측 §5 (b) 행과 문자열 일치. 선행 조건 `npm --prefix packages/notify install` 유효 — `@types/node` 는 `packages/notify/node_modules/@types` 에서만 해석된다(루트 tsc 가 tsconfig 디렉터리부터 상향 탐색) |
| Q19 위험 등급 하향 · `sensitiveLogs` 단서 · 표면 4→5 | C-5·C-6 실측 인용 정확. 단서가 되돌리기 열과 §6 유효기간 양쪽에 있다 |

## 미확인 (유지)

`bin/claude-with fit` 세션에서 **skill 이름 충돌이 이름 단위인지 층 단위인지** — 런타임 세션 1회 필요. 초안 5-2 #2 의 미확인 표기는 정확하고, 정정 11 은 그 미확인이 (i) 권고문에도 반영돼야 한다는 것뿐이다.

## 산출물 형태 판정 — **3안**

003 §4-2 를 **답 반영본으로 재작성**하고 기존 시그니처 블록 + 5개 정정 블록을 **그 아래 정정 블록으로 강등**한다(삭제 금지 — 003 §5-2 2026-09-08 정정의 *"지우지 않고 그 위에 정정을 얹는다"* 규율 준수). 되돌리기 **즉시**(git revert · pleiades 안에 갇힘), 정본 분산 **0**.
근거: §4-2 는 **117행 중 78행(66.7%)이 정정 블록**이고 **맨 위 코드 블록이 이미 stale** 하다 — 1a-1 이 그대로 구현하면 `targets`·`ctx` 가 빠진다. (B) 006 신설은 초안의 기각 근거 셋이 그대로 유효하므로 **기각**.
**나머지(003 §3-1·§5-2·§9·§10·§10-1·§11 · 005 §3-1·§4-5·§4-13·§6·§7 · `workflow.md` 8절 · `CLAUDE.md`)는 (A) append 유지.**

## `decision-writer` 에게 (2회차 착수 조건)

정정 1·2 가 **Q19·Q26 의 선택지 구조 자체를 바꾼다** — 두 질문을 결합 조건과 함께 다시 제시해야 하므로 §1 Q19·Q26 절은 문구 수정이 아니라 **재작성**이다. 정정 5 는 §3 의 판정을, 정정 6·7 은 §4 대장을 바꾼다. 정정 3 은 네 개 표의 등급 열 일괄 수정이다.

---

# 감사 결과 — 2회차 / 3회 상한 (2026-09-09)

> 감사관 반환 결과를 오케스트레이터가 그대로 저장했다. 읽기 전용 — worktree `repos/{myFinance,myFitness}` (`integration/pleiades`) 에서 `git show`/`git grep --text` 만.

**정정 10건 · 확인 12건 · 미확인 1건(유지) · 권고 3건.** 정정이 나왔으므로 문서는 **초안**이다.

**산출물 형태 재판정: 3안 유지 (확인).** §4-2 재작성 + 기존 5블록 강등, 나머지 (A) append.
2회차 재확인: `003:597~713 = 117행` · 정정 블록 `636~713 = 78행` · 블록 5개(`636`·`645`·`659`·`668`·`682`) · 맨 위 `interface Notifier` 에 `targets`·`ctx` 부재. **단 아래 정정 5·7·8 을 반영하지 않으면 3안을 해도 정본이 갈라진다.**

**반복 유형 경고.** 이번 10건 중 4건(정정 1·3·5·7)이 같은 뿌리다 — **정본 문구를 요약해 인용하면서 한정어를 떨어뜨린다.** 3회차에서는 ① 정본 인용은 **원문 그대로 붙여넣고**(요지 요약 금지), ② 대장 각 행의 "현재 문구" 에 **`파일:줄`** 을 박을 것을 권고한다.

## A. 1회차 정정 11건 반영 판정

| # | 판정 | 근거 (2회차 재확인) |
|---|---|---|
| 1 | **부분** | 근거 ① 폐기·대장 #20 신설 ✔. 그러나 교체 문구 *"관측 변경 0 인 것은 **C 뿐**"*(`:234`)이 같은 절 `:237` *"관측 변경을 피하는 안은 **C 와 B′**"* · 조합표 B′ 행(`:160` "현 상황 0")과 **자기모순** → **B-정정 1** |
| 2 | **반영** | 조합표(`:155-163`) + §5-2 #7 취소선(`:593`) + Q26 표 대가 열 + §6 유효기간 행. `onError` 개정안은 `003:694-697` 확인 ✔. 단 무조건 서술 → **B-정정 2** |
| 3 | **반영** | `"즉시 (1a-3 착수 전까지"` **15곳** 실측(grep -c = 15). 근거 `003:837` · `003 §8-1` ✔. Q41(`:369-370`)·#32 I1(`:427-429`) 예외 사유 명시 ✔ |
| 4 | **부분** | ② 재서술·§7-4 유비 삭제·권고 재검토 블록(`:297-299`) ✔. 그러나 `003 §4-3` 제약 2 **오인용** → **B-정정 3** |
| 5 | **반영** | §3 전면 개정, 3안 채택, (A)의 *"원래 있던 표에서 갱신"* 근거 철회 ✔ |
| 6 | **반영(불충분)** | 6행 추가로 13 → 20 ✔. 그러나 **여전히 빠진 대상 3군** → **B-정정 7·8·9** |
| 7 | **반영** | 기준 라벨 + 각주(N1·N2 라벨만) ✔. `005:312-313` 의 `1,629 + 1,885 = 3,514` 검산 성립 확인 |
| 8 | **부분** | 3안 분리 + B 대가 명시 + §5-2 #12 미확인 등재 ✔. fin 주석(`advisor-monitor.ts:187-194`) · fail-safe(`:205-211`) 재확인 ✔. 그러나 **A↔C 차이 서술이 과소** → **B-정정 4** |
| 9 | **반영** | `+0.421 s` 삭제 · 프레이밍 비교 블록(`:420-425`) ✔. measured-facts #32 I1 §4 와 `+0.620 / +0.413 / +1.041 / +1.661 / +0.34` 전부 일치 |
| 10 | **반영** | 질의 계약 블록(`:281-286`) ✔. *"본문 반출 0"* 이 §2 Q14 자리·§6 유효기간에도 전파됨 |
| 11 | **반영** | 분모 40(= 유니크 35 + rule 5, `measured-facts Q41-1`) · per-name 가정 블록(`:395-399`) ✔ |

**미반영 0건.** 부분 4건은 전부 아래 B 에서 다룬다.

## B. 새 주장 — 판정

| # | 주장 (초안 원문 요지) | 판정 | 근거 / 실제 |
|---|---|---|---|
| **1** | Q19 근거 ① 교체본 — *"관측 변경 0 인 것은 **C 뿐**"* (`:234`) | **정정** | 초안 자신이 B′ 를 *"현 상황 관측 변경 0"*(`:160`·`:213`·`:237`)으로 세 번 적었다. 정확한 서술: ***"무조건 0 은 C 뿐 · B′ 는 현 상황(마스킹 대상 0건 — C-6)에서만 0 이고 `sensitiveLogs` 를 켜는 순간 갈린다. 포트를 건드리지 않는 것은 C·B′ 둘 다."*** 이 문장은 대장 #5 로 **003 §10 Q19 정본에 그대로 들어간다** |
| **2** | 조합표 *"Q26 ①·③ + Q19 B = **✕ 불가**"* (`:159`) · *"`onError` 는 파사드가 error 객체를 넘기는 **유일한** 표면"* (`:145`·`:188`) | **정정** | **`deliveries[].error?: string` 를 유지하는 한** 참이다. 그 필드는 `003:614` 에 있고, **초안 §3 이 그 블록을 "이미 stale · 재작성 대상" 으로 판정**했다 — 즉 1a-1 이 **바로 그 필드의 타입을 정하는 단계**다. 필드를 `error?: unknown`(raw 객체)으로 넓히면 Q26 과 무관하게 호출부 `sanitizeError` 재계산이 성립한다(B″). 고칠 곳: 조합표 셋째 행에 **`(전제: deliveries[].error?: string)`** 을 박고, 각주로 *"필드를 객체로 넓히는 B″ 는 별개의 포트 변경이며 이 문서는 §4-2 의 `string` 을 유지하기로 한다"* 를 명시 |
| **3** | Q10-L — *"003 **§4-3 제약 2** 가 길이 분할을 `Transport` 경계 **안쪽으로 못 박았다**"* (`:258`·`:262-263`·`:491`) | **정정** | 제약 2 원문(`003:721`): *"길이 분할은 변환 뒤, 즉 `Transport` 경계 안쪽에 **있거나, 포트가 "변환 후 길이 예산"을 코어에 알려주는 형태**여야 한다"* — **두 형태를 허용**한다. **결론(② 는 `Notifier` 시그니처 무영향 · Q10-L 은 1a-1 을 막지 않는다)은 유지된다.** 고칠 곳: *"제약 2 는 두 형태를 허용하며, 어느 쪽이든 절단\|분할 정책은 `Notifier`(§4-2)에 오지 않는다"* |
| **4** | Q25 ADMIN — *"C(자리만)와의 차이는 **fin 파싱 1지점뿐**"* (`:123`) · C 행 *"부분 드리프트 1지점 존치"*(`:96`) | **정정** | 차이가 하나 더 있다. 파사드의 `notify` 는 **Route 만** 받는다(`003:606`). C 에는 ADMIN 매핑이 없으므로 `advisor-monitor:217-226` 의 per-chat 루프를 **`notify(ADMIN,…)` 로 표현할 방법이 없고**, `targets()` 도 임의 대상 전송을 제공하지 않는다. ∴ **C 에서는 그 루프 1개 + per-chat catch 1개가 파사드 밖에 남는다** — `003:749`(*"루프 21곳 제거"*)와 §5-1 개정 청구서가 **20 이 되고**, `003:659-666` 의 *"22곳 중 하나라 1a-4 가 이전한다"* 전제가 C 에서만 깨진다. 부수: C 행 설명 *"타입 축만 두고 **값은 기존 env**"*(`:96`)는 §4-4 원문(`003:730`)을 그대로 옮긴 것이라 **A 와 구별되지 않는다** — *"ADMIN 은 타입 멤버로만 두고 수신자 매핑을 정의하지 않는다"* 로 바꿔야 한다 |
| **5** | Q25 A 성립 근거 — *"fit 에서 `Route.ADMIN` 을 넘기는 지점이 생기지 않는다"* | **확인** | fit 아웃바운드 6 호출(C-2) 전수: `admin-alerts.ts:232 sendToAll(bot, message)` → `send.ts:24-29 getChatIds()` = `TELEGRAM_ALLOWED_CHAT_IDS`. `TELEGRAM_ADMIN\|ADMIN_CHAT` → **0건**. 나머지 5 호출도 전부 같은 화이트리스트 |
| **6** | Q19 A = 파사드가 `sanitizeError` 적용 | **확인 (단서 권고)** | `003 §3-1`(`:551`)이 `sanitizeError` 정본을 **myFinance** 로 이미 골랐고, 그 함수는 흡수 대상 초크포인트 안에서 쓰인다(`telegram.ts:113`). ∴ **함수는 Q19 답과 무관하게 패키지에 들어오고 A 의 구현 비용은 호출 1개**다 (→ 권고 2) |
| **7** | §4 대장 20행이 완전 목록 | **정정** | Q25·Q26 답이 직접 무효화하는데 대장에 없는 정본 4곳: **`003:749`** *"`advisor-monitor.ts:199` 의 5번째 파싱 지점(ADMIN)은 **별건이다**"* · **`003:781`** *"`getAllowedChatIds` 정의 제거 **4 중 1~3**(Q25 에 종속)"* · **`003:795-796`** §5-1 정정 표 2행 · **`003:399`·`:463`** *"결정은 **Q25**/**Q26**"* 포인터 |
| **8** | 같은 대장 — 숫자 서술 | **정정** | 답 이후 **`003:1260`** *"1a 착수를 막는 결정은 **8개**"* · **`:1273`** · **`:1387`** 가 전부 거짓이 된다(남는 것은 Q16·Q17·Q18·Q27 **4개**). **R1 유형이 정본에 3곳** 남는다. `003:1368` §11 *"Q10 에 답이 나올 때"* 행도 소진 표기 필요 |
| **9** | 대장 #13 (`CLAUDE.md` 상태 문단) | **정정** | 이슈 #37 범위가 *"#35·#36 머지 반영"* 을 명시하는데 대장 #13 에 없다. 실측: `CLAUDE.md:7` = *"열린 PR: **#35** · 인계 **#34**"* / **#35 MERGED · #36 MERGED**(`362e6e8`·`2e07093`) |
| **10** | 대장 #10 *"**005 §3-1** 숫자 대장(pleiades 행) — skill 7 · `.claude/` 12"* | **정정** | **005 §3-1 대장(N1~N22)에 pleiades skill/파일 수 행은 없다.** 실제 위치는 **`measured-facts.md:1596`·`:1601`** 이고 **이미 정정 블록이 있다**(Q41-2) — *"이미 정정됨 · 인용만"* 행이어야 한다. 대신 **정말 stale 한 곳이 빠졌다: `005:427-428` 정정 G 의 pleiades skill **7개 열거**(`orphan-check` 누락)**. `ls .claude/skills` = **8** · `find .claude -type f` = **13** |
| **11** | 대장 #8 (005 §4-5 + 정정 G) | **정정** | `005:423` 은 *"이름을 겹치게 지으면 **5번째** 충돌"* 이라고도 적는다. 현 충돌이 5건이면 새 충돌은 **6번째**다 |
| **12** | *"이 문서는 새 숫자를 만들지 않는다"* (`:11`) | **정정** | 대장·measured-facts 어디에도 없는 숫자 6개: **117행 · 78행 · 66.7% · 블록 5개 · 1,662 · 1,774**. 값 자체는 전부 정확(2회차 재측정). ① `measured-facts` 에 **2026-09-09 감사 실측 절**로 명령과 함께 추가하고 인용하거나 ② `[감사 실측]` 라벨. **`66.7%` 는 파생값(78/117)** — 삭제하거나 `78/117` 병기 |
| **13** | 되돌리기 등급 시점 한정 15곳 — Q10-P 적용 근거 | **확인** | Q10-P 는 코어 정본(`003:743`). 1a-3 이후 되돌리기에 `npm ci`+재빌드+`pm2 restart` 가 붙는다 ✔ |
| **14** | Q41 / #32 I1 을 시점 한정에서 제외 | **확인** | Q41 = `.claude/` 디렉터리명 + frontmatter 1줄 ✔. #32 I1 = `npm pack --dry-run` 4파일 · 누수 0·0 · `prepare` 미독 ✔ |
| **15** | Q10-L ② 재서술 뒤에도 **권고 ① 유지** | **확인** | `003:551` 정본 = fin 분할 · C-1 4지점 무음 · `send.ts:44/45` 절단→폴백 유발. 표면 비용과 독립 → 뒤집히지 않는다 ✔ |
| **16** | §3 3안 — 되돌리기 즉시 · 정본 유일화 · 참조 무파손 | **확인 (권고 1 동반)** | `003` 내 §4-2 참조 6곳 전부 레이블 참조. 타 문서에서 행 번호 참조 **0건** ✔ |
| **17** | 조합표 (Q26 ② + Q19 C) = ○ | **확인** | `onError` 는 옵션 필드이고 `deliveries[].error` 내용물과 독립 ✔ |
| **18** | §5-2 #12 (fin fail-safe 보존) 미확인 등재 | **확인** | `advisor-monitor.ts:205-211` 재확인. 보존 형태는 호출부 가드 = 설계 결정 ✔ |
| **19** | Q10-P 근거·단서(리터럴 `<>`) | **확인** | 1회차 유지 |
| **20** | #32 I1 (b) 권고 근거 F6·F3 | **확인** | measured-facts 원문 일치 |
| **21** | (h) H-1 입력 204 | **확인** | worktree 91 · `dev` 87 · fit 113 ✔ |
| **22** | (f) 현 충돌 5건 | **확인** | Q41-2 일치. `pleiades-codex-loop` × 40 이름 E·S = 0 ✔ |

### 미확인 (유지, 1건)
skill 충돌이 이름 단위인지 층 단위인지 — 런타임 세션 1회 필요.

## 권고 (정정 아님)

1. **강등 블록에 소진 헤더.** *"아래는 소진된 이력이다 — 값의 정본은 이 절 맨 위 시그니처다 (2026-09-09 · 이슈 #37)."*
2. **Q19 A 행에 구현 비용 한 줄.** *"`sanitizeError` 는 §3-1 정본으로 이미 패키지에 들어오므로 A 의 추가 구현은 호출 1개다 — A 를 고르지 않는 이유는 비용이 아니라 5 표면 관측 변경이다."*
3. **Phase 4 질의 묶음 — 7 질문 · 2배치.** 배치 1: Q25 본안 · Q25 ADMIN(A/B/C) · Q26×Q19 결합(`①+C` 권고 / `①+A` / `②+B` / `①+B′`) · Q10-L. 배치 2: Q10-P · Q41(설명란에 기준 S·부수 (i) 동봉) · #32 I1.

## 3회차 착수 조건

- **문구 1줄** — 정정 1 · 3 · 12 · 11
- **표 1행 수정** — 정정 2 · 4 · 10
- **대장 증행 (20 → 최소 23)** — 정정 7 · 8 · 9
- **권고가 뒤집히는 건은 없다.** 3회차 감사는 위 10건 반영 여부 + 새 문장만 본다.

---

# 감사 결과 — 3회차 / 3회 상한 (2026-09-09)

> 감사관 반환 결과를 오케스트레이터가 저장했다. 범위 축소(2회차 정정 10건 + 권고 3건 반영 여부 · 새 문장 · 줄 번호 이견). worktree HEAD `6542152`·`626a201` 동일 → 2회차 확인 12건 재검증 생략.

**정정 6건 · 권고 2건 · 미확인 1건(유지).** → **미통과 — 단 남은 정정은 전원 "문안 수준"이라 오케스트레이터가 직접 반영한다. 4회차 감사 불필요, 스코프 축소 불필요.**
6건 중 4건이 줄 번호 오기, 1건이 빈 줄 1개, 1건이 표 1칸 누락. 논증·권고·판정은 하나도 뒤집히지 않았고, 초안이 새로 만든 주장·숫자는 전부 재현됐다.

## A. 2회차 정정 10건 + 권고 3건 반영 판정 — **미반영 0건**

| # | 판정 | 근거 |
|---|---|---|
| 1 | 반영 | 확정 문구가 `:303`·`:637` 에 문자 단위 동일. `:308` 한정어 복원 |
| 2 | 반영 | 조합표 `:218` 전제 · 각주 `:224-229` · "유일한 표면" 3곳(`:202`·`:254`·`:694`) 전부 전제 동반 |
| 3 | 반영 | `003:719` 원문 그대로 인용(`:334`·`:638`) |
| 4 | 반영 (줄 번호 1건 오류 → 정정 3) | C 행 `:138` · C 대가 블록 `:140-152` · `003:610`·`:730`·`:749`·`:659-666` 원문 일치 |
| 7 | 반영 | 대장 #21~#24. `003:749`·`:781`·`:795`·`:796`·`:399`·`:463` 원문 일치 |
| 8 | 반영 | #25(3곳 원문 일치 · Q16·Q17·Q18·Q27 열거 병기) · #26(`003:1368`) |
| 9 | 반영 | 대장 #13 `CLAUDE.md:7` 흡수. #35·#36 MERGED 재현 |
| 10 | 반영 | #10 재작성(`measured-facts:1596`·`:1601` · Q41-2 인용만) |
| 11 | 반영 | #8 ⓐ `005:423` "5번째" → 6번째 |
| 12 | 반영 | `measured-facts.md:3190` `## #37 감사 실측` 신설 · 명령 동봉 · `66.7%` 삭제 |
| 권고 1 | 반영 | 소진 헤더 `:78`·`:606`·`:660` 동일 |
| 권고 2 | 반영 (줄 번호 1건 오류 → 정정 1) | Q19 A 행 `:278` 구현 비용 한 줄 |
| 권고 3 | 반영 (1칸 누락 → 정정 6) | §7 7문항 2배치 · 설명란 21개 본문과 모순 0 |

## B. 줄 번호 이견 3곳 — **writer 가 전부 옳다** (2회차 감사관 오기)

| 대상 | 2회차 | writer | 판정 |
|---|---|---|---|
| §4-3 제약 2 | `003:721` | **`003:719`** | writer |
| `deliveries[].error?: string` | `003:614` | **`003:617`** | writer |
| `notify` 시그니처 | `003:606` | **`003:610`** | writer |

## C. 새 문장 판정

- **C-1 `measured-facts` `#37 감사 실측` 절** — 117 · 78 · 블록 5(636·645·659·668·682) · 절 경계 597/714 · fin 16/1662 · fit worktree 17/1774 · fit 원본 18/1885 · pleiades 8/13 · #35·#36 MERGED — **전수 재현 확인.** 파생값 `66.7%` 부재 확인.
- **C-2 대장 #21~#27 "현재 문구"** — 003·005·workflow.md·measured-facts 인용 32곳 전부 `sed -n` 원문 일치. 요약 인용 잔존 0.
- **C-3 Q25 C 대가 블록 · 조합표 전제·각주 · 소진 헤더 · §7 질의 표** — 논리 성립. 설명란 숫자 전부 본문·대장 값. `Q41-1` 유니크 40(skill·agent 35 + rule 5) 정확.

### C-4. 정정 6건

| # | 위치 | 현재 | 실측 정본 | 왜 정정인가 |
|---|---|---|---|---|
| **1** | `:79` · `:278` | `003:551` | **`003:552`** | `:551` 은 `isNetworkError` 행. `sanitizeError` 정본 행은 `:552`. 권고 2 문장의 유일한 근거 포인터가 다른 기능을 가리킨다 |
| **2** | `:203` | `003:697-698` | **`003:699`** | `699` = `onError?: (target: string, error: unknown) => void;`. 인용 범위가 `onError` 를 포함하지 않는다 |
| **3** | `:66` · `:142` · `:180` · **`:654`(대장 #21)** | `advisor-monitor.ts:217-226` | **`:220-227`** | `217` 은 `await import(…)`. 초안 `:23` 은 `:220` 으로 적어 자기모순. #21 은 003 §5-1 정본에 들어간다 |
| **4** | `:653` | 빈 줄 | 삭제 | 대장 #20·#21 사이 빈 줄로 markdown 표가 둘로 쪼개진다 |
| **5** | `:51` | 권고 유지 "6건" (열거 7개) | **7건** | R1 #11 유형(합계≠열거). `:82` 는 "7개 권고" |
| **6** | `:775-783` §7-4 | 소비처 26행(1~15·17~27) | **#16 추가** | 대장 #16(003 §11 `:1379` 소진 표기)이 어느 답의 소비처에도 없다 → ③ 행에 넣는다 |

## D. 권고 (정정 아님)

1. `:41` 개정 이력 1→2회차 #4 행의 "경계 안쪽으로 못 박았다" 에 *"→ 3회차 정정 3 으로 재교체(두 형태 허용)"* 구 추가.
2. §7-2 ③ `:745`·`:751` 의 "B 가 성립하는 유일한 조합" 에 전제 *"(포트가 error 를 문자열로 담는다는 이 문서의 전제 아래)"* 추가.

## E. 미확인 (유지, 1건)
skill 이름 충돌이 이름 단위인지 층 단위인지 — 런타임 세션 1회 필요.

## F. 처리
4회차 감사 없음. 오케스트레이터가 정정 6건 + 권고 2건을 직접 반영하고 아래로 자체 확인:
```bash
sed -n '552p' docs/specs/003-notify-package.md
sed -n '699p' docs/specs/003-notify-package.md
git -C repos/myFinance show integration/pleiades:src/lib/ai/advisor-monitor.ts | sed -n '220,227p'
awk 'NR>=631 && NR<=660 && /^$/ {print "빈 줄: "NR}' _workspace/1a-1-prep/02_writer_1a1prep.md
```
